# Glyph Remaining Functionality Gap Map (2026-05-26)

This map classifies remaining functionality for the Senscope/Glyph workstream while preserving the distinction between controller/backend behavior, Senscope neutral profile concepts, and game semantics.

## Gap Categories

### ALREADY_SUPPORTED

| Gap / capability | Relevant source files | Current evidence | Required decision/input | Proposed next implementation batch | Risk | Stop condition |
| --- | --- | --- | --- | --- | --- | --- |
| Native Ultimate mode baseline output path | `src/modes/Ultimate.cpp`, `src/core/ControllerMode.cpp` | source-confirmed `MODE_ULTIMATE` digital/analog flow exists and is selected from config. | none | Keep covered by source-shape checks before future patches. | Low | Stop if future tests reveal behavior differs from source expectations. |
| Post-remap logical Tilt1/Tilt2 runtime patch | `src/modes/Ultimate.cpp`, `docs/calibration/glyph_ultimate_tilt_hardware_test_result.md` | source-confirmed and hardware-observed native left-stick Tilt/Tilt2 behavior. | none for current MVP | Add broader regression tests before touching runtime again. | Low | Stop if hardware owner reports a regression in the tested Tilt table. |
| Many-to-one remap aliases | `src/core/InputMode.cpp` | source-confirmed OR behavior for multiple physical buttons activating one logical button. | none | Use in future profile adapter validation. | Medium | Stop if adapter would depend on macro/timing behavior rather than simple aliasing. |
| Duplicate physical remap guard | `src/core/InputMode.cpp` | source-confirmed first physical mapping wins; repeated physical mappings ignored. | none | Add fixture/check coverage if adapter writes remaps. | Medium | Stop if user asks for duplicate physical button behavior other than source-defined first-wins. |
| Per-mode SOCD pair execution after remap | `src/core/ControllerMode.cpp`, `src/core/InputMode.cpp`, `src/core/socd.cpp` | source-confirmed remap then SOCD then mode output. | none | Document as invariant in adapter design. | Medium | Stop if requested profile requires changing SOCD semantics. |
| On-device config get/set/storage backend | `HAL/pico/src/comms/ConfiguratorBackend.cpp`, `HAL/pico/src/core/Persistence.cpp` | source-confirmed protobuf config transport and LittleFS persistence. | none | Read-only adapter design can target protobuf config model, but no push automation. | Medium | Stop if task requires flashing/push-to-device workflow. |
| Manual FW update menu and bootloader entry | `config/glyph/common/src/config.cpp`, `HAL/pico/src/display/DefaultConfigMenu.cpp` | source-confirmed local bootloader calls; hardware-observed manual RPI-RP2 flow. | none | Keep as human-controlled hardware path. | Medium | Stop if asked to automate flashing. |

### SUPPORTED_WITH_PROFILE_CONFIG_ONLY

| Gap / capability | Relevant source files | Current evidence | Required decision/input | Proposed next implementation batch | Risk | Stop condition |
| --- | --- | --- | --- | --- | --- | --- |
| Mapping physical RF-style buttons to logical `LT1`/`LT2` | `src/core/InputMode.cpp`, `docs/calibration/glyph_ultimate_tilt_button_id_confirmation_2026-05-24.md`, `docs/calibration/fixtures/glyph_ultimate_tilt_domain_spec.json` | source-confirmed remap support; profile-specific source confirms `BTN_RF3 -> BTN_LT1`, `BTN_RF4 -> BTN_LT2` for the MVP layout. | User must confirm target physical layout/profile for any new profile. | Add adapter-side remap planner and validation fixtures, not runtime changes. | Medium | Stop if requested mapping is not source-backed by profile/layout evidence. |
| Disabling/unmapping physical buttons with `BTN_UNSPECIFIED` | `src/core/InputMode.cpp`, `HAL/pico/include/util/state_util.hpp`, `config/glyph/common/include/glyph_overrides.hpp` | source-confirmed firmware behavior for explicit `BTN_UNSPECIFIED`. | User/configurator decision on whether UI should expose disabled versus omitted states. | Add profile diff/checker for explicit disabled entries. | Medium | Stop if behavior depends on omitted JSON fields rather than explicit protobuf defaults. |
| Backend default mode selection | `HAL/pico/src/comms/backend_init.cpp`, `HAL/pico/src/display/DefaultConfigMenu.cpp`, `config/glyph/common/include/glyph_overrides.hpp` | source-confirmed defaults are config indices and menu can set them. | User must define desired default backend/profile behavior per target workflow. | Adapter/config docs can prescribe index updates; no runtime patch needed. | Medium | Stop if changing profile order would create ambiguous defaults. |
| Applicable backend filtering in on-device profile menu | `config/glyph/common/src/display/GlyphConfigMenu.cpp` | source-confirmed menu filters by `applicable_backends`. | User must decide which generated/adapted profiles should appear for each backend. | Include applicable backend planner in adapter design. | Low | Stop if requested menu behavior conflicts with source-backed filtering. |

### NEEDS_CONFIGURATOR_OR_PROFILE_ADAPTER

| Gap / capability | Relevant source files | Current evidence | Required decision/input | Proposed next implementation batch | Risk | Stop condition |
| --- | --- | --- | --- | --- | --- | --- |
| Senscope neutral profile to Glyph config/protobuf adapter | `tools/glyph_config_model.py`, `HAL/pico/src/comms/ConfiguratorBackend.cpp`, `docs/architecture/NEUTRAL_PROFILE_TO_GLYPH_REALIZATION.md` | source-confirmed distinction between Glyph config and Senscope concepts; no direct adapter exists. | User must approve adapter scope and confirm no neutral schema mutation. | Design-only adapter contract and fixture corpus; no runtime adapter yet. | High | Stop if implementation would change Senscope neutral schema or assert direct JSON equivalence. |
| JSON omission versus explicit disabled handling | `tools/glyph_config_model.py`, `docs/sources/raw/GlyphUserProfiles.json`, `src/core/InputMode.cpp` | parser preserves omitted `activates` as `None`; firmware behavior is confirmed for `BTN_UNSPECIFIED`, not external JSON omission. | User/domain input on intended configurator export/import semantics. | Add read-only roundtrip/normalization checker after source research. | High | Stop if adapter cannot distinguish omitted from disabled safely. |
| Button/layout naming bridge for UI and generated profiles | `config/glyph/glyph_mk6/include/matrix_definition.hpp`, `config/glyph/glyph_mk6/include/button_positions.hpp`, `docs/calibration/glyph_ultimate_layout_calibration_findings_2026-05-24.md` | source-confirmed matrix/display mappings, but user-facing physical names remain layout/profile-specific. | User must confirm canonical labels for the target Glyph layout. | Add source-backed physical/logical mapping reference doc or fixture. | Medium | Stop if asked to invent physical names from geometry alone. |
| Profile default/index safety checker | `HAL/pico/src/comms/backend_init.cpp`, `HAL/pico/src/comms/ConfiguratorBackend.cpp`, `tools/glyph_config_model.py` | configurator backend validates default mode indices but no repo helper validates generated JSON/protobuf profile defaults end-to-end. | User must decide generated profile ordering policy. | Add stdlib-only read-only checker for profile indices and remap semantics. | Medium | Stop if checker would require external configurator dependencies not present in repo. |
| Configurator-side export/import workflow documentation | `HAL/pico/src/comms/ConfiguratorBackend.cpp`, `docs/research/GLYPH_CONFIG_EXPORT_NOTES.md`, `docs/project/G8F8_CONFIGURATOR_BACKEND_SOURCE_AUDIT.md` | source-confirmed device backend, but external browser configurator source is not included. | User may need to provide external configurator source or domain notes. | Source-research batch before implementation. | High | Stop if workflow requires unsupported vendor export format or push-to-device behavior. |

### NEEDS_NARROW_RUNTIME_PATCH

| Gap / capability | Relevant source files | Current evidence | Required decision/input | Proposed next implementation batch | Risk | Stop condition |
| --- | --- | --- | --- | --- | --- | --- |
| Additional native Ultimate exact-coordinate modifier tables beyond current Tilt/Tilt2 | `src/modes/Ultimate.cpp`, `include/prototypes/senscope/SenscopePrototypeTypes.hpp` | current native Ultimate has hard-coded formulas; prototype has scaffold-only 9-way table types. | User must specify exact controller behavior independent of Smash semantics. | Design a reviewed, table-driven native Ultimate patch with source-backed fixtures. | High | Stop if behavior depends on inferred or undocumented firmware capability. |
| Native Ultimate arbitrary neutral/center output override | `src/core/ControllerMode.cpp`, `src/modes/Ultimate.cpp` | neutral exists by initialization and current formulas, but arbitrary center entry tables are not profile-configurable. | User must confirm desired neutral behavior for each modifier combination. | Add regression fixtures first; then narrow runtime table support if approved. | High | Stop if request implies game-semantic legality claims. |
| Native Ultimate force/special output combinations | `src/modes/Ultimate.cpp`, `src/prototypes/senscope/SenscopePrototypeForce.cpp`, `src/prototypes/senscope/SenscopePrototypeOutput.cpp` | prototype force/digital rule scaffolds exist; native Ultimate does not consume them. | User must define exact controller output behavior and priority rules. | Prototype-to-runtime design review, then small runtime patch only if source-backed. | High | Stop if requested behavior introduces macros, timing automation, toggles, or one-shots. |
| Wider runtime self-tests for selected native Ultimate invariants | `tools/check_glyph_ultimate_tilt_runtime_source.py`, `tools/check_glyph_ultimate_tilt_tables.py`, `src/modes/Ultimate.cpp` | current checkers cover Tilt/Tilt2 source shape and table math, not full native Ultimate branch matrix. | User can approve broader checker scope. | Add read-only/source-level checker before runtime changes. | Medium | Stop if checker starts encoding Smash semantics instead of controller output invariants. |

### NEEDS_SOURCE_RESEARCH

| Gap / capability | Relevant source files | Current evidence | Required decision/input | Proposed next implementation batch | Risk | Stop condition |
| --- | --- | --- | --- | --- | --- | --- |
| External HayBox proto/configurator schema authority | `platformio.ini`, `config/glyph/env.ini`, `.pio/libdeps/.../config.proto` if locally present | PlatformIO pins proto dependencies; generated proto files are local build/dependency artifacts, not normal repo source. | User may provide external source snapshot or approve targeted source capture docs. | Source-authority capture batch, no runtime edits. | High | Stop if private/encrypted format reverse engineering is needed. |
| Exhaustive current profile import/export behavior | `docs/sources/raw/GlyphUserProfiles.json`, `tools/glyph_config_model.py`, `HAL/pico/src/comms/ConfiguratorBackend.cpp` | raw fixture and parser exist, but external app JSON behavior is not fully in repo. | User should provide canonical exported profiles and expected normalization rules. | Read-only fixture analysis and adapter design doc. | Medium | Stop if omitted/default semantics are ambiguous. |
| Complete physical layout naming | `config/glyph/glyph_mk6/include/matrix_definition.hpp`, `config/glyph/glyph_mk6/include/button_positions.hpp` | source maps IDs to matrix/display positions; names like "Tilt1 button" are profile/domain annotations. | User/domain input for canonical labels. | Mapping handoff doc with source and user-labeled overlays. | Medium | Stop if hardware labels are not source-backed or user-confirmed. |
| Full backend transport output transforms | `HAL/pico/src/comms/*.cpp`, `include/core/state.hpp` | output state fields are clear; each backend may serialize/scale differently. | User must state which backends matter for Senscope/Glyph. | Source audit of selected backend report serialization. | Medium | Stop if target backend behavior appears undocumented or console-specific. |
| Nunchuk availability on Glyph MK6 | `HAL/pico/src/input/NunchukInput.cpp`, `config/glyph/common/src/config.cpp`, `config/glyph/glyph_mk6/include/glyph_pinout.hpp` | source contains Nunchuk input, but current Glyph config only instantiates matrix input. | User/hardware input if nunchuk is in scope. | Read-only build/source trace for active input sources and pinout. | Medium | Stop if hardware behavior must be claimed without evidence. |

### BLOCKED_BY_HARDWARE_EVIDENCE

| Gap / capability | Relevant source files | Current evidence | Required decision/input | Proposed next implementation batch | Risk | Stop condition |
| --- | --- | --- | --- | --- | --- | --- |
| Exhaustive C-stick/right-stick preservation after Tilt patch | `src/modes/Ultimate.cpp`, `docs/calibration/glyph_ultimate_tilt_hardware_test_result.md` | source says patch is left-stick only; hardware result was not exhaustive. | Hardware owner must run a focused C-stick/right-stick smoke matrix if this matters. | Hardware test checklist update only. | Medium | Stop if observed behavior diverges from source expectations. |
| Exhaustive trigger preservation | `src/modes/Ultimate.cpp`, `docs/calibration/glyph_ultimate_tilt_hardware_test_result.md` | source says triggers unchanged by patch; hardware result was not exhaustive. | Hardware owner test evidence. | Hardware test checklist update only. | Medium | Stop if trigger behavior differs on device. |
| Exhaustive SOCD preservation | `src/core/socd.cpp`, `src/core/InputMode.cpp`, `docs/calibration/glyph_ultimate_tilt_hardware_test_result.md` | source path unchanged; hardware result says no exhaustive SOCD matrix. | Hardware owner test evidence if future runtime patch touches directions/modifiers. | Add prehardware SOCD matrix checklist. | Medium | Stop if unexpected SOCD behavior appears. |
| RF5 negative verification | `docs/calibration/glyph_ultimate_tilt_hardware_test_result.md`, `config/glyph/glyph_mk6/include/button_positions.hpp` | hardware result marks RF5 identity ambiguous. | User/hardware owner must identify RF5 physically and retest. | Add layout-photo or button-ID retest protocol. | Low | Stop if future behavior depends on RF5 negative proof. |
| Nunchuk behavior preservation | `src/modes/Ultimate.cpp`, `HAL/pico/src/input/NunchukInput.cpp`, `docs/calibration/glyph_ultimate_tilt_hardware_test_result.md` | source contains behavior; hardware result says not tested. | Hardware owner must provide nunchuk test evidence if in scope. | Optional nunchuk test protocol. | Medium | Stop if claiming active nunchuk support on MK6 without evidence. |
| Profile/config preservation after flash | `HAL/pico/src/core/Persistence.cpp`, `docs/calibration/glyph_ultimate_tilt_hardware_test_result.md` | hardware result says profiles appeared preserved but not exhaustively verified. | Hardware owner or configurator readback evidence. | Add readback comparison procedure; no push automation. | Medium | Stop if write/read workflow would require unsupported automation. |

### OUT_OF_SCOPE_FOR_SENSCOPE

| Gap / capability | Relevant source files | Current evidence | Required decision/input | Proposed next implementation batch | Risk | Stop condition |
| --- | --- | --- | --- | --- | --- | --- |
| Smash gameplay semantic source authority | `AGENTS.md`, `docs/project/GLYPH_WORKSTREAM_BOUNDARIES.md` | repo contract forbids inventing game semantics. | none | Keep separate in Senscope source authority. | High | Stop if asked to choose/alter game-semantic sources here. |
| Raw coordinate legality/banned-coordinate claims | none | No repo source marks raw coordinates as banned/illegal. | none | Do not implement. | High | Stop if task requires legality claims. |
| Push-to-device/flashing automation | `HAL/pico/src/display/DefaultConfigMenu.cpp`, `HAL/pico/src/comms/ConfiguratorBackend.cpp`, docs hardware protocol | manual bootloader/configurator commands exist; automation is out of scope. | Explicit user approval would still hit repo stop conditions. | Do not implement. | High | Stop if asked to automate device flashing. |
| Macros, turbo, timing automation, one-shots, toggles | `src/core/InputMode.cpp` comments explicitly avoid macro remapping | Runtime remap guard is anti-macro; repo contract forbids these additions. | none | Do not implement. | High | Stop immediately if requested. |
| Direct Senscope neutral schema mutation | `AGENTS.md`, `docs/architecture/NEUTRAL_PROFILE_TO_GLYPH_REALIZATION.md` | boundary is explicit. | User approval required even for schema discussion. | Keep adapter layer separate. | High | Stop if implementation requires schema changes. |

## Recommended Next Batches

1. Source-authority capture for proto/configurator JSON semantics, especially omitted fields and generated/default values.
2. Read-only profile adapter design with fixtures: physical/logical mapping, explicit disabled entries, default index validation, and no runtime changes.
3. Expanded source-level and hardware checklist coverage for C-stick, triggers, SOCD, RF5 identity, and optional nunchuk behavior.
4. Only after review: narrow native Ultimate runtime table patch design for any additional exact-coordinate behavior the user approves.
