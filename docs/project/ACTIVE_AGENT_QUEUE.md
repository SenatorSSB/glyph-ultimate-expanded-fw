# Active Agent Queue

Current queue:

| Item | Status | Notes |
| --- | --- | --- |
| Baseline docs/setup scaffold | done | Repo structure initialized for agentic work. |
| Source material capture | blocked | `ESAM1.hpp`, `ESAM1.cpp`, and `GlyphUserProfiles.json` are missing locally. |
| Firmware realization work | not started | Requires explicit scope beyond docs/setup. |

Append new items here as the workstream grows, but keep the queue focused on this repo only.
