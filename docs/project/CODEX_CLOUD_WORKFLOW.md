# Legacy Codex Cloud Workflow

Status label: `SUPERSEDED_BY_AGENT_FRAMEWORK`.

The former ChatGPT-writes-the-queue loop is not current authority. Use
`AGENTS.md`, `docs/WORKFLOW.md`, `docs/agent_framework/README.md`, and
`docs/agent_framework/SCHEDULED_TASKS.md`. Only Curator or explicit user
work-order authority may create Ready/Preauthorized work; Planner remains
non-authoritative. History remains available in Git.
