# Legacy Codex Workflow

Status label: `SUPERSEDED_BY_AGENT_FRAMEWORK`.

This path is a compatibility pointer only. The current workflow is
`docs/WORKFLOW.md`; current roles and exact prompts are in
`docs/agent_framework/README.md` and
`docs/agent_framework/SCHEDULED_TASKS.md`. History remains available in Git.
