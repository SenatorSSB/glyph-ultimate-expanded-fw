# Glyph Portfolio Candidate Packet — 2026-08-23 23:49 EEST

```yaml
packet_id: glyph-portfolio-20260823-2349
packet_state: FRESH
planning_branch: planning/portfolio-20260823-2349
base_configurator_sha: 6b8ebcd404dcbfe9b579eed41fb35b889e9da598
base_live_verified: true
previous_planner_packet: glyph-portfolio-20260823-1450
previous_packet_branch: planning/portfolio-20260823-1450
previous_packet_commit: 03d5bea14cc8beaf0be1b58e713c3b2cbc9efcd1
previous_packet_freshness: PARTIALLY_CONSUMED_STILL_USEFUL
candidate_count: 11
new_candidate_count: 8
carried_forward_candidate_count: 3
curator_review_required: true
global_wait_proposed: false
material_events_since_publication: []
planner_authority: NON_AUTHORITATIVE_CANDIDATE_SUPPLY_ONLY
configurator_changed: false
```

## Result and freshness

This is a material supply refresh, not a duplicate caused by time or SHA
drift. The canonical queue reports `RUNWAY_SHORTFALL_CANDIDATE_SUPPLY`, which
requires a fresh broad audit. The preceding packet is partially consumed but
still useful:

- `GP-HW-001`, `GP-CTL-001`, `GP-VAL-001`, `GP-PROV-001`, `GP-SRC-002`, and
  `GP-VAL-002` shipped on `configurator`.
- `GP-SRC-003` remains canonically `READY`, but two separate canonical
  completion publications marked it Done without integrating its repaired
  implementation tree. Curator restored it to Ready after each false
  completion. Its repaired tip
  `81c91ba31784fa085e5e6f6c21286c2ff28f32f2` is not an ancestor of live
  `configurator`.
- `GP-AUTH-001` remains `USER_DECISION_GATED` and `GP-CONFIG-002` remains
  `EVIDENCE_GATED`.
- Effective authorized runway is one against the target of four, with
  canonical primary liveness `RUNWAY_LOW` and supporting signal
  `RUNWAY_SHORTFALL_CANDIDATE_SUPPLY`.

The refresh found eight new bounded candidates. The strongest material events
are repeated false completion publication, additional write-capable offline
tools outside the prepared-v2 guardrail scope, a real-capture checker path
that cannot compile its status regex, contradictory active-table-source
classification still enforced by current checkers, and incomplete CI
publication-surface correspondence. No new hardware result, production-owned
table set, runtime-loaded capability, device-write capability, Nunchuk result,
or proven low-level root cause was found.

## Live base and broad-audit evidence

Immediately before branch creation, `git fetch --prune origin` completed with
network access and the clean worktree `HEAD`, local `configurator`, and
`origin/configurator` all resolved to
`6b8ebcd404dcbfe9b579eed41fb35b889e9da598`. The planning branch was created
directly from that SHA.

The audit inspected:

- every required current governance, queue, workflow, user-direction,
  implementation-boundary, authorization/runway, and hardware-evidence
  document;
- the preceding packet, its Curator disposition, recent curation and
  implementation history, the two false `GP-SRC-003` completion publications,
  the repaired feature range, and live merged/unmerged Git ancestry;
- the active `Ultimate.cpp` publication and resolution path,
  `UltimateIdentityRuntimeTables.hpp`, the generated current baseline header,
  the interpreter, source-sync tooling, and dormant active-publication
  scaffolds;
- generator-v2 prepare/install, source-authority intake, the legacy
  source-owned generator, coordinate-native bridge, preview renderer,
  candidate preparation, fixtures, negative corpora, and output policies;
- checker census, curated validation manifest, validation health, aggregate,
  branch-context checker, agent-framework checker, docs navigation, and
  current-versus-historical classifications;
- official-configurator primary corpus, five-check current lane, manual-capture
  scaffold/checker, old inconclusive branch-only capture, and quarantined
  external-remapper evidence;
- both GitHub workflows, PlatformIO inputs, artifact postprocessor provenance,
  tracked `glyph_nuker` identity, UF2 read-only inspection, artifact
  preservation/recovery requirements, and forbidden write/flashing paths;
- current latest-Y2, Alternative B alias, and canonical-grid failure hardware
  evidence, including their identity and non-claim limits.

Observed checks on the live base:

- `python3 tools/run_glyph_runtime_config_validation.py --json`: PASS; census
  freshness plus 22 current load-bearing checks.
- `python3 tools/check_glyph_checker_census.py`: PASS; 189 discovered entries.
- `python3 tools/check_glyph_agent_framework_docs.py`: PASS.
- `python3 tools/check_glyph_docs_navigation.py`: PASS.
- `python3 tools/check_glyph_docs_agent_surface.py`: PASS.
- `python3 tools/check_glyph_official_configurator_validation.py`: PASS; five
  offline primary-corpus checks and no compatibility claim.
- `python3 tools/check_glyph_official_configurator_manual_capture_result.py`:
  PASS only with zero completed captures.
- `python3 tools/check_glyph_official_configurator_manual_capture_result.py
  --adversarial-test`: PASS without exercising a reviewed capture result.
- `python3 tools/check_glyph_artifact_postprocessor_provenance.py --check`:
  PASS; postprocessor execution not performed, transformation unknown, durable
  locator unresolved.
- source-owned generator modes, source-authority intake, prepared-v2 preview,
  coordinate-native contract/offline packages, source sync, and table-symbol
  checks: PASS.
- `python3 tools/check_glyph_source_owned_candidate_generation.py`: FAIL in
  canonical branch context; the current aggregate explicitly excludes this
  candidate-preparation checker as unsafe/mutating.

Direct read-only or temporary-only reproductions established:

- the framework checker passed at canonical commit
  `a6fd3b8344b9c98a1768d388d2f2b116a99fa3f2` while the queue said
  `GP-SRC-003` was Done and the repaired implementation tree was absent;
- the legacy generator accepted `.git/config`, `AGENTS.md`, and a temporary
  symlink alias resolving to the active generated baseline header;
- the coordinate-native bridge's `--output` path accepted the active baseline
  header, `src/modes/Ultimate.cpp`, and `.git/config` in a non-mutating mocked
  reproduction;
- compiling the manual-capture result status pattern
  `[A-Za-z0-9_- ]` raises `re.PatternError: bad character range`;
- `tools/uf2/inspect_uf2.py` records, but does not reject, declared-block-count
  mismatch and duplicate/missing block numbers in temporary mutated copies;
- `.github/workflows/build-device-config.yml` has build, artifact-upload, and
  release paths while the load-bearing publication checker and fixture inspect
  only `.github/workflows/build.yml`;
- the validation-health fixture reports 27 manifest entries and 22 current
  load-bearing checks while its current prose still says 26 and 21;
- the active generated baseline header and its current checker still require
  `inert generated-table placeholder` and `not wired into runtime selection`
  even though the source-backed include chain consumes its table bytes.

No repository file, active table byte, firmware source behavior, workflow, UF2,
or hardware state was changed during the audit.

## Largest current bottleneck

The largest bottleneck is **machine-verifiable correspondence between declared
state/capability and the content or route it is supposed to describe**.

The same class now appears across four planes: Done state can be published
without integrated implementation content; offline writers can reach protected
targets despite narrow safety labels; current checkers can certify prose or
capture scaffolds that do not correspond to the first real payload; and
artifact/publication tooling records metadata without complete route or
structure correspondence. Closing these H0/H1 trust boundaries is higher value
than adding new runtime semantics or more design fixtures.

## Horizon A — restore current correctness correspondence

### GP-SRC-003 — Complete prepared-packet integrity and output guardrails

- **Objective/value:** carry forward the existing strict prepared-packet and
  isolated-output recovery so generator-v2 preparation, install, and preview
  fail closed on tampering and protected targets.
- **Current source evidence:** live `source_owned_generator_modes.py` still
  does not recompute full packet/artifact/manifest correspondence and its
  installer accepts protected output; repaired tip `81c91ba...` is coherent
  evidence but is not integrated into live `configurator`.
- **Implementation seam:** exactly the canonical work order and cumulative
  repaired feature delta, reconciled onto the fresh canonical base with the
  recorded temporary-root portability correction.
- **Scope/exclusions:** existing host-tool/checker recovery only; no active
  source write, table-byte change, production authority, runtime path, build,
  artifact publication, device write, persistence, flashing, or game semantic.
- **Dependencies:** canonical `READY` work order controls execution; this
  packet neither reauthorizes nor broadens it.
- **Validation:** canonical tamper/path corpus, shared validator proof,
  temporary-root portability, atomic/non-mutating output, full aggregate,
  source/table semantic digests unchanged, live canonical integration proof.
- **Risk estimate:** H1.
- **Hardware/evidence/user gates:** no hardware when compiled source and table
  bytes remain unchanged; any such delta stops. Independent repaired-scope
  review remains required.
- **Confidence:** high.
- **Decision-aid score:** 100/100.
- **Non-authoritative readiness estimate:** `CURATION_READY` carry-forward
  estimate only; canonical status is already `READY`.

### GP-CTL-002 — Prove canonical Done/integration correspondence

- **Objective/value:** prevent a work order from becoming canonically Done
  unless a verifiable canonical integration commit contains the authorized
  implementation or an exact reviewed equivalent.
- **Current source evidence:** `GP-SRC-003` was published Done twice while its
  repaired feature tip was a sibling of `configurator`; the framework checker
  passed the first false completion. `done_evidence` is currently only a
  non-empty string and no generic gate verifies canonical integration.
- **Implementation seam:** structured completion evidence recording work-order
  ID, implementation base, reviewed implementation/equivalence identity,
  canonical integration SHA, completion-publication SHA, and validation
  provenance. Verify Git objects and ancestry plus exact authorized content or
  patch-equivalence. Do not require only original feature-tip ancestry because
  reviewed rebases/cherry-picks may be legitimate.
- **Scope/exclusions:** agent-framework docs/schema/checker and synthetic Git
  fixtures only. No queue promotion, product/runtime checker, implementation
  replay, merge, firmware, hardware, or user-direction change.
- **Dependencies:** define a migration-safe representation for existing Done
  items without retroactively inventing evidence. Curator independently
  authorizes any queue schema transition.
- **Validation:** false status-only completion, missing integration object,
  sibling feature, wrong base, unrelated commit, partial patch, extra
  unauthorized delta, and mutable-ref-only evidence fail; direct merge and
  exact reviewed replay pass; current framework/sequence/docs gates pass.
- **Risk estimate:** H0.
- **Hardware/evidence/user gates:** Curator authorization and focused
  independent governance review; no hardware gate.
- **Confidence:** high on the gap, medium-high on final migration shape.
- **Decision-aid score:** 98/100.
- **Non-authoritative readiness estimate:** `CURATION_READY`.

### GP-SRC-004 — Make active baseline classification machine-true

- **Objective/value:** remove machine-enforced claims that the generated
  current baseline is inert or not wired while preserving the exact distinction
  between active compile-time table-content inclusion and unchanged
  source-owned RuntimeConfigView publication.
- **Current source evidence:** `UltimateIdentityRuntimeTables.hpp` includes
  `GeneratedRuntimeConfigBaseline.current.hpp` and consumes its 28 arrays, but
  the header says `inert generated-table placeholder` and `not wired into
  runtime selection`; the current baseline checker and validation manifest
  require or describe the same inert classification.
- **Implementation seam:** emit current-baseline-specific classification
  comments/metadata separately from inert example/layout-spec output; update
  fixture/checker/manifest language to assert active table-content source and
  unchanged active-view publication independently.
- **Scope/exclusions:** classification, generator emission metadata, docs,
  fixtures, and checkers. Exact 28 table points, symbols used by firmware,
  routing, RuntimeConfigView publication, and controller behavior remain
  unchanged. No production profile or runtime-loaded path.
- **Dependencies:** preserve `GP-SRC-001` active-source truth and do not overlap
  `GP-SRC-003` packet/output recovery.
- **Validation:** before/after 28-table semantic digest and generated array
  values identical; source/include chain checker proves active table-content
  use; example outputs retain inert markers; current aggregate/docs pass. If
  the compiled header changes, run canonical build proof; no hardware is
  required only when semantic bytes/runtime behavior are proven unchanged.
- **Risk estimate:** H1 because an active compiled header is touched even
  though intended behavior is unchanged.
- **Hardware/evidence/user gates:** Curator authorization, independent source
  classification review, build if compiled header changes; stop on any table,
  symbol, routing, or runtime semantic delta.
- **Confidence:** high.
- **Decision-aid score:** 95/100.
- **Non-authoritative readiness estimate:** `CURATION_READY`.

### GP-CONFIG-004 — Complete reviewed manual-capture correspondence

- **Objective/value:** make the first real official-configurator capture
  reviewable instead of crashing or accepting mutually inconsistent metadata,
  result rows, files, hashes, and checker claims.
- **Current source evidence:** the reviewed-result status regex is invalid and
  is never reached by zero-capture tests. Current validation also lacks exact
  result-row uniqueness/status/pass correspondence, accepted-output versus
  rejection-note correspondence, complete `hashes.txt` verification, and
  machine validation of the claimed comparison output.
- **Implementation seam:** fix the regex and define exact reviewed capture
  schemas/keys; cross-check metadata/result document, unique required rows,
  overall result, app acceptance, output-or-rejection choice, every artifact
  hash, and actual comparison-checker output. Add a complete synthetic reviewed
  capture plus adversarial corpus.
- **Scope/exclusions:** checker, synthetic fixtures, and capture docs only. No
  app interaction, new capture, compatibility interpretation, production
  export, firmware, runtime, device write, or hardware claim.
- **Dependencies:** precedes external evidence candidate `GP-CONFIG-002`; it
  does not satisfy that evidence gate.
- **Validation:** accepted, rejected, partial, and inconclusive synthetic
  packets; malformed regex/status, duplicate/missing rows, boolean/status
  mismatch, wrong overall result, output/rejection mismatch, missing/stale hash,
  fabricated checker status/output, symlink/unknown-file, and positive-claim
  cases fail; zero-capture scaffold remains valid.
- **Risk estimate:** H0.
- **Hardware/evidence/user gates:** Curator authorization and independent
  checker review; no hardware or app operation.
- **Confidence:** high.
- **Decision-aid score:** 94/100.
- **Non-authoritative readiness estimate:** `CURATION_READY`.

### GP-VAL-003 — Census every CI publication route and derived claim

- **Objective/value:** prevent current validation from claiming complete
  publication gating when an unclassified workflow publishes artifacts or when
  its health prose contradicts machine counts.
- **Current source evidence:** the load-bearing checker and fixture inspect
  only `.github/workflows/build.yml`; tracked
  `.github/workflows/build-device-config.yml` also builds, uploads, and can
  release. Its ownership/caller/meta contract is unresolved. Separately, the
  validation-health fixture reports 27/22 while current prose reports 26/21.
- **Implementation seam:** deterministic static census of all tracked workflow
  build/postprocess/upload/release routes plus a curated applicability/ownership
  manifest; fail on an unclassified route or overbroad claim. Derive or
  validate health prose counts from the curated machine record.
- **Scope/exclusions:** workflow census, manifest, current claim correction,
  docs, fixtures, and checkers only. Do not edit or activate
  `build-device-config.yml`, select its caller/owner/meta source, change
  permissions/secrets/releases, build firmware, or upload anything.
- **Dependencies:** current `build.yml` gate remains intact. Any remediation of
  the externally coupled workflow requires separate source-authority and risk
  judgment.
- **Validation:** added/removed/renamed workflow, alternate upload/release
  action, shell publication route, unclassified caller, stale manifest/hash,
  and prose count drift fail; historical/dormant classification requires
  explicit evidence; current aggregate/census/docs checks pass.
- **Risk estimate:** H0.
- **Hardware/evidence/user gates:** Curator authorization and independent CI
  governance review; no hardware.
- **Confidence:** high on the census/claim gap, medium on eventual ownership
  disposition.
- **Decision-aid score:** 92/100.
- **Non-authoritative readiness estimate:** `CURATION_READY` for census and
  claim-bounding only.

## Horizon B — dependent writer and artifact provenance successors

### GP-SRC-005 — Apply one isolated-output policy to remaining offline writers

- **Objective/value:** prevent the legacy source-owned generator and
  coordinate-native bridge from overwriting repository governance, Git
  metadata, active source, or aliased targets while preserving safe stdout and
  explicit inert review output.
- **Current source evidence:** lexical pre-resolution checks in
  `generate_source_owned_runtime_config.py` accept `.git/config`, `AGENTS.md`,
  and an alias to the active header; the coordinate bridge accepts arbitrary
  relative/absolute `--output` and writes directly/non-atomically. Focused
  checkers exercise only a lexical temp `src` case or stdout.
- **Implementation seam:** reuse the resolved isolated-temporary-root and
  atomic output policy supplied by `GP-SRC-003`; apply it to generic legacy
  generator and coordinate bridge output while retaining their non-mutating
  stdout modes and the separately guarded inert-source install contract.
- **Scope/exclusions:** host tooling/checkers/docs only. No active/repository
  write, table byte, ownership, profile intent, runtime, device/protobuf write,
  persistence, flashing, or game-semantic change.
- **Dependencies:** `GP-SRC-003` canonically integrated with stable shared
  policy. Any policy drift or command-specific durable-output requirement
  returns to Curator.
- **Validation:** repository roots, `.git`, relative paths, traversal, case
  variants, symlink/alias, active-header alias, input overwrite, non-temporary
  roots, and non-atomic failure cases reject; safe isolated output is atomic;
  repository and active table digests remain unchanged; generator,
  coordinate-native, source-sync, and aggregate checks pass.
- **Risk estimate:** H1 host-side mutation safety.
- **Hardware/evidence/user gates:** no hardware if compiled source and active
  bytes stay unchanged; mechanically activatable only after the exact shared
  policy prerequisite remains satisfied without new judgment.
- **Confidence:** high.
- **Decision-aid score:** 91/100.
- **Non-authoritative readiness estimate:** `PREAUTHORIZABLE` after
  `GP-SRC-003`.

### GP-PROV-002 — Emit a real observed-only CI artifact sidecar

- **Objective/value:** make the bounded canonical build route emit full source
  and final postprocessed artifact identity instead of only a short-SHA
  filename.
- **Current source evidence:** `build.yml` builds, copies, runs the tracked
  postprocessor, and uploads immediately. It emits no full candidate SHA,
  final size/SHA-256, postprocessor SHA, or sidecar. `GP-PROV-001` provides an
  inert synthetic schema/verifier but expressly did not integrate CI.
- **Implementation seam:** after existing postprocessing, generate and verify
  an observed-only sidecar containing full Git SHA, final filename/size/hash,
  tracked postprocessor identity, `purpose: UNKNOWN`,
  `byte_transformation: UNKNOWN`, and `immutable_locator: null`; upload it only
  alongside the bounded current artifact route.
- **Scope/exclusions:** no postprocessor execution outside existing CI, no
  purpose/effect inference, store/retention decision, immutable-locator claim,
  release, artifact acceptance, device write, flashing, or hardware PASS.
- **Dependencies:** `GP-VAL-003` classifies the applicable current publication
  route and no unresolved route is silently covered by this sidecar.
- **Validation:** synthetic generation/verifier corpus; full SHA and final
  postprocessed hash/size correspondence; changed/missing postprocessor,
  pre-post hash confusion, short SHA, false locator, and sidecar-not-uploaded
  cases fail; static workflow gate proves ordering after postprocessing and
  before upload.
- **Risk estimate:** H1 CI/artifact metadata behavior.
- **Hardware/evidence/user gates:** independent Curator and CI review; it
  cannot satisfy immutable preservation or hardware handoff.
- **Confidence:** high on value, medium-high on final workflow shape.
- **Decision-aid score:** 86/100.
- **Non-authoritative readiness estimate:** `PREAUTHORIZABLE` after
  `GP-VAL-003`.

### GP-UF2-001 — Research fail-closed UF2 structural acceptance

- **Objective/value:** replace a metadata-only inspector that exits success on
  internally inconsistent block numbering/counts with an authority-bounded
  structural validation design for later candidate/recovery use.
- **Current source evidence:** `inspect_uf2.py` reports declared counts,
  duplicates, and missing numbers but appends no errors for those conditions;
  temporary mutations retained valid magic and exited success. Historical
  read-only UF2 packets require generated artifacts to be independently parsed,
  but they do not by themselves define every generic, RP2040, Glyph,
  postprocessor, or hardware-acceptance invariant.
- **Implementation seam:** first classify candidate invariants into generic UF2
  self-consistency, RP2040 source-backed constraints, observed official-Glyph
  reference shape, candidate-local expected ranges, and claims that remain
  hardware/unknown. Define a synthetic invalid corpus before promoting a strict
  checker.
- **Scope/exclusions:** read-only research, docs, and synthetic bytes only. No
  real build, `glyph_nuker` execution, artifact approval, device copy/write,
  flashing, safety claim, or hardware acceptance.
- **Dependencies:** authoritative UF2 format and RP2040 sources plus a bounded
  decision on which candidate-local ranges belong in a work order rather than
  a universal checker.
- **Validation:** research must specify declared/physical count, unique complete
  numbering, payload/flag/family/address/alignment and overlap rules with exact
  authority labels; unknown Glyph/postprocessor behavior stays unknown.
- **Risk estimate:** H1 decision aid for firmware artifacts; research itself is
  H0/non-mutating.
- **Hardware/evidence/user gates:** source evidence and Curator design review
  before any acceptance checker; no hardware claim.
- **Confidence:** high on the fail-open condition, medium on authoritative
  final invariant set.
- **Decision-aid score:** 79/100.
- **Non-authoritative readiness estimate:** `RESEARCH`.

### GP-PROV-003 — Inventory build-input provenance without pin changes

- **Objective/value:** make toolchain, workflow-action, dependency, source-tree,
  and postprocessor identity gaps explicit before anyone claims reproducible or
  exact candidate builds.
- **Current source evidence:** workflows use mutable action major tags and
  `pip install --upgrade platformio`; `platformio.ini` contains registry caret
  ranges and several Git/tag references. The repository currently records the
  postprocessor file identity but not a complete resolved build-input set.
- **Implementation seam:** deterministic static census/manifest and checker for
  declared inputs, pin class, recorded resolution evidence, and explicit
  unknowns. It must not resolve from the network or alter dependency versions.
- **Scope/exclusions:** H0 research/docs/checker only; no package install,
  dependency update, action pin change, build, artifact comparison,
  reproducibility claim, postprocessor execution, or hardware action.
- **Dependencies:** define the inventory boundary across PlatformIO registry,
  Git dependencies, workflow actions, platform/framework packages, source SHA,
  and tracked postprocessor. Any remediation becomes separate H1/H2 work.
- **Validation:** static discovery and manifest freshness; added/removed/mutable
  input drift fails; exact pins versus ranges/tags are classified without
  claiming resolution or reproducibility.
- **Risk estimate:** H0.
- **Hardware/evidence/user gates:** Curator review; build/hardware judgment is
  deferred to any later pin/remediation candidate.
- **Confidence:** high on the inventory gap, medium on initial scope.
- **Decision-aid score:** 73/100.
- **Non-authoritative readiness estimate:** `RESEARCH`.

## Horizon C — external authority and evidence supply

### GP-AUTH-001 — Source-authorized production table delta intake

- **Objective/value:** supply an explicit production-owned table set and exact
  replacement content for the completed source-owned lane.
- **Current source evidence:** the current intake remains source-equivalent and
  `NONE_AUTHORIZED`; no owned production delta exists.
- **Implementation seam:** user/Senscope-provided versioned intake with exact
  symbols, points, authority references, intent, approval, acknowledgements,
  and current baseline identity.
- **Scope/exclusions:** Planner/Curator may not invent ownership, mapping,
  coordinates, game semantics, or approval. Intake alone is not a firmware
  candidate or hardware result.
- **Dependencies:** explicit human/Senscope authority. Internal output
  guardrails are downstream of evidence supply.
- **Validation:** current intake and negative corpus, then separate build,
  immutable exact artifact, and H2/H3 physical PASS for any later active delta.
- **Risk estimate:** H3 for eventual active behavior; intake is evidence only.
- **Hardware/evidence/user gates:** explicit user/source authority and exact
  content.
- **Confidence:** high on the missing gate, unknown on future content.
- **Decision-aid score:** 70/100.
- **Non-authoritative readiness estimate:** `USER_DECISION_GATED` carry-forward.

### GP-CONFIG-002 — Exact official-configurator manual capture

- **Objective/value:** replace unknown app version/route/acceptance evidence
  with a new hashed operator record.
- **Current source evidence:** the five-check offline lane passes, current live
  tree has zero completed captures, and an old unmerged branch contains only an
  inconclusive capture with unrecorded app version/routes. That branch is
  evidence history, not current completion.
- **Implementation seam:** operator-performed capture using the current
  procedure after `GP-CONFIG-004` makes the reviewed-result checker complete.
- **Scope/exclusions:** no automated app interaction, universal compatibility,
  production exporter, firmware/runtime change, or device write.
- **Dependencies:** `GP-CONFIG-004` complete; then an external operator with
  official-configurator access.
- **Validation:** corrected manual-capture checker, exact file hashes, actual
  comparison output, and bounded reviewed result.
- **Risk estimate:** H1 evidence operation; no firmware hardware claim.
- **Hardware/evidence/user gates:** exact operator capture.
- **Confidence:** medium-high.
- **Decision-aid score:** 62/100.
- **Non-authoritative readiness estimate:** `EVIDENCE_GATED` carry-forward.

## User-direction and evidence effects

- `GLYPH-UD-001` keeps every candidate within Glyph/HayBox realization and
  excludes Senscope-owned game semantics, datasets, solver behavior, and
  neutral profile intent.
- `GLYPH-UD-002` fixes `configurator` as the live comparison branch and
  `pio run -e glyph_mk6` as the canonical build when a candidate requires it.
- `GLYPH-UD-003` prevents UF2/provenance/checker results from becoming hardware
  PASS, device-write authority, or an immutable artifact locator by assertion.
- `GLYPH-UD-004` supports bounded H0/H1 correctness supply while keeping
  Planner and Curator separated.
- `GLYPH-UD-005` does not supply missing production table ownership and does
  not waive exact-snapshot physical PASS for future H2/H3 work.
- Latest Y2 hardware evidence supports only the accepted source-owned baseline.
  Alternative B supports source-aligned generated-table aliasing while the
  source-owned publication path remains unchanged. Canonical-grid evidence is
  a failure, not replacement authority or root-cause proof. Legacy missing
  artifact identity cannot satisfy Revision-2 acceptance. Nunchuk remains
  `NOT_TESTED`.

## Rejected or held alternatives

- **Treat the two false `GP-SRC-003` Done publications as harmless prose drift
  — REJECT.** Canonical execution state changed while implementation content
  was absent; a generic integration-correspondence gate is warranted.
- **Require only original feature-tip ancestry for completion — REJECT.** A
  reviewed replay/rebase may be legitimate; canonical integration identity and
  exact equivalence must be representable.
- **Broaden `GP-SRC-003` to repair every writer or active-header label —
  REJECT.** Its exact recovery authority is already bounded. `GP-SRC-004` and
  `GP-SRC-005` remain separate candidates.
- **Treat the active baseline as inert because RuntimeConfigView publication is
  source-owned — REJECT.** Active table-content inclusion and active-view
  publication are different facts and both must be stated accurately.
- **Create another prepared-packet candidate — REJECT.** `GP-SRC-003` owns that
  exact gap.
- **Tighten source-authority-intake output roots immediately — HOLD.** It
  rejects active code trees but accepts governance/docs/checker paths; the
  command-specific durable review-output contract needs a substantive root
  policy before implementation.
- **Treat `GP-CONFIG-004` as duplicate of the external capture — REJECT.** The
  checker must process a synthetic real-result shape before an operator can
  provide trustworthy evidence.
- **Repair the historical import/export compatibility chain — REJECT.** It is
  explicitly historical; the current primary-corpus lane passes.
- **Change or delete `build-device-config.yml` now — REJECT.** Caller,
  ownership, PAT, `meta.yaml`, and release authority are unresolved. Census and
  claim-bounding may proceed without changing it.
- **Publish a one-line 26/21 count correction — REJECT as standalone.** Fold
  generic derived-count parity into `GP-VAL-003`.
- **Treat upload-artifact or a sidecar as immutable hardware storage — REJECT.**
  The durable locator remains unresolved.
- **Run `glyph_nuker` locally to infer purpose or bytes — REJECT.** File
  identity and workflow text do not authorize execution or a behavior claim.
- **Promote UF2 observations directly to universal Glyph acceptance rules —
  REJECT.** Separate generic UF2, RP2040, observed official, candidate-local,
  and hardware claims first.
- **Update PlatformIO dependencies or action pins inside provenance research —
  REJECT.** Such changes can alter firmware bytes and need separate risk,
  build, and possibly hardware judgment.
- **Remove dormant candidate/active-storage scaffolds — HOLD.** Establish
  compiled-source footprint and separate build/risk authority first.
- **Add more generator modes, coordinate fixtures, or intake schemas — REJECT
  as duplicate.** Current focused checks pass outside the isolated output and
  classification gaps.
- **Repair the canonical-grid candidate — REJECT.** It is `HARDWARE_FAIL`,
  lacks replacement authority, and must not merge.
- **Implement runtime-loaded config, persistence, WebSerial/device write,
  protobuf write, release upload, or flashing automation — REJECT.** Current
  approval/source/architecture requirements are absent.
- **Claim Nunchuk validation or a low-level root cause — REJECT.** Direct
  evidence is absent.

## Global evidence wait assessment

`GLOBAL_EVIDENCE_WAIT_SUPPORTED` is **not proposed**
(`global_wait_proposed: false`). The mandatory fresh broad audit looked outside
the user/evidence-gated survivors and found independent H0/H1 correctness,
writer-safety, validation, recovery, configurator-evidence, and provenance
supply. External production-table authority, operator capture, and immutable
artifact storage do not establish portfolio-global scarcity.

## Fresh adversarial review and Curator handoff

A separate read-only adversarial review challenged materiality, duplication,
scope, risk, and readiness. Its corrections are incorporated:

- keep `GP-SRC-003` canonical Ready and do not count it as new supply;
- use integration identity plus exact equivalence rather than feature-tip
  ancestry alone for `GP-CTL-002`;
- separate active-header truth (`GP-SRC-004`) from remaining writer safety
  (`GP-SRC-005`) and from exact `GP-SRC-003` recovery;
- keep `GP-CONFIG-004` independent of the external capture;
- consolidate workflow census and validation-health parity into `GP-VAL-003`;
- make `GP-PROV-002` dependent on publication-route classification;
- keep UF2 and build-input candidates at `RESEARCH` until authority/scope are
  fully bounded;
- reject direct remediation of the externally coupled workflow.

This packet authorizes and implements nothing. Curator must independently
reproduce or validate each candidate and is the only role that may create a
complete Ready/Preauthorized work order or clear `curator_review_required`.
Suggested review order: `GP-CTL-002`, `GP-SRC-004`, `GP-CONFIG-004`,
`GP-VAL-003`, then `GP-SRC-005` and `GP-PROV-002`. Existing canonical execution
authority for `GP-SRC-003` remains owned exclusively by the queue.

## Planning-branch validation

- `git diff --cached --check`: PASS.
- `python3 tools/check_glyph_docs_navigation.py`: PASS.
- `python3 tools/check_glyph_docs_agent_surface.py`: PASS on the planning
  branch.
- `python3 tools/check_glyph_agent_framework_docs.py`: PASS.
- The full runtime-config aggregate remains PASS on the recorded live
  `configurator` base. On this planning branch it reports FAIL only because the
  four current `content_and_scope` generator/coordinate checkers reject the
  new `docs/planning/portfolio_20260823_2349.md` path as outside their allowed
  implementation-branch scope. Planner did not edit or weaken those runtime
  tests; this packet changes no runtime/configurator product surface and is not
  a merge candidate for `configurator`.
