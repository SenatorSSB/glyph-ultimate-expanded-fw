# Glyph Portfolio Candidate Packet — 2026-09-20 14:57 EEST

This is an immutable, non-authoritative Revision-2 Planner publication. It
records candidate supply for independent Work-Order Curator review only. It
does not authorize implementation, change the canonical queue, create a
`READY` or `PREAUTHORIZED` item, alter firmware behavior, or claim hardware
validation.

```yaml
packet_id: glyph-portfolio-20260920-1457
packet_state: FRESH
planning_branch: planning/portfolio-20260920-1457
base_configurator_sha: 37b6d7e5573703f9854674e424b526cf7f5dd1a8
base_live_verified: true
previous_planner_packet: glyph-portfolio-20260920-0227
previous_packet_commit: 6f7be340568a03069f605503c5ad67dc7f692bda
previous_packet_freshness: CONSUMED
candidate_count: 8
new_candidate_count: 7
materially_refined_candidate_count: 1
carried_forward_without_new_evidence_count: 0
current_ready_work_order_count: 0
curator_review_required: true
global_wait_proposed: false
material_events_since_publication: []
planner_authority: NON_AUTHORITATIVE_CANDIDATE_SUPPLY_ONLY
configurator_changed: false
```

## Audit basis and freshness

This audit is anchored at clean canonical
`37b6d7e5573703f9854674e424b526cf7f5dd1a8`. The initial ordinary live fetch
failed with sandbox DNS resolution, so it was inconclusive. The required
minimal read-only network-enabled retry then verified live
`refs/heads/configurator` at that exact SHA. A final ordinary
`git ls-remote` immediately before branch creation reconfirmed the live SHA
and that `planning/portfolio-20260920-1457` did not exist remotely; local
`configurator`, `origin/configurator`, and `HEAD` matched the same SHA. Other
registered worktrees were prunable missing-gitdir records; no legitimate
concurrent canonical writer was found.

Packet `glyph-portfolio-20260920-0227` is materially `CONSUMED`. Its five
executable candidates are now `DONE`: GP-SRC-009, GP-VAL-021, GP-VAL-022,
GP-VAL-023, and GP-CONFIG-009. Its research candidate GP-PERSIST-003 remains
nonexecutable because the recorded fault model, target ABI, and dependency
identity questions were not resolved. The authoritative queue markers now
record `planner_packet.state: CONSUMED`, zero candidates and survivors, zero
Ready, zero recorded or mechanically activatable Preauthorized, zero
hardware-pending, and effective authorized runway zero against target four.
Nearby older prose still describes a partially consumed packet and
GP-VAL-023 as preauthorized; the machine-readable markers and newer canonical
state take precedence, so this packet does not treat that prose as authority.

## Broad audit scope and result

The broad refresh covered queue and branch history; runtime mode selection,
custom-mode caches, menu and SetConfig paths; source-owned generation,
installation, candidate preparation, extraction, and authority intake;
persistence research; build identity and dependency/provenance records; CI
validation, build, checkout, postprocessing, sidecar, and upload ordering;
hardware-work-order/evidence correspondence; checker census, manifest, health,
fixtures, source-dependency policy, and aggregate launch behavior; current
hardware evidence; user direction; and forbidden runtime/device-write paths.

Framework, navigation, checker-census, validation-health, artifact-workflow,
validation-publication, generator-mode, source-authority-intake, and identity
runtime-table source-sync checks pass with 203 census entries and 42 manifest
entries. The full aggregate remains unavailable because the already
owner-deferred GP-VAL-011 preflight rejects the ignored nested
`.pio/libdeps/glyph_mk6/Adafruit_TinyUSB_XInput/` repository. This audit does
not reopen or reclassify that lane. No firmware build or hardware action was
performed because the Planner cycle is read-heavy and non-authoritative.

Fresh source review and adversarial probes found independent gaps:

- the current default declares 13 mode configurations while mode activation
  storage has 10 elements and the exact production loops use the declared
  count without a local capacity bound;
- a clean clone cannot run the advertised manifest aggregate because the
  table-replacement checker directly imports an undeclared helper;
- PATH substitution can turn every manifest `python3` checker into a
  zero-output success and the aggregate accepts it;
- the provenance gate accepts an untracked source file and CI has no
  immediately-pre/post-build worktree-integrity proof;
- three offline/source-owned output paths bypass the shared exact-target,
  symlink, alias, and atomic-write protections;
- candidate-local hardware protocol references are string-bound but not
  content-bound;
- CustomControllerMode has 10 cached modifier masks while the schema currently
  resolved in the local build cache permits 20 modifiers, without a local
  capacity check; and
- four GitHub Actions steps remain major-tag references rather than immutable
  commits.

The dominant engineering bottleneck is exact capacity and correspondence
across active runtime state and its validation, build, and evidence boundaries.
The most immediate source-level risk is the current 13-entry count flowing
into 10-slot mode-activation storage. This is C++ out-of-bounds behavior; its
physical symptom and relationship to any reported device issue remain
unknown.

## Horizon A — immediate runtime, validation, source, and build-integrity candidates

### GP-CONFIG-010 — Repair mode-activation capacity correspondence

- **Objective/value:** Remove the active 13-entry/10-slot out-of-bounds seam
  while preserving every current default mode, its order, bindings, and
  selection behavior.
- **Current source evidence:** `glyph_overrides.hpp` declares
  `game_mode_configs_count = 13`; `config.cpp` passes that count to
  `setup_mode_activation_bindings`; `mode_selection.cpp` defines
  `mode_activation_masks[10]` and both populates and reads it for every index
  below the supplied/configured count. Revision `5687c9d` previously reverted
  a Config-sized array after a separate issue, so blindly restoring that old
  shape is not an acceptable design shortcut.
- **Implementation seam:** Introduce one ABI-safe compile-time capacity
  contract for the activation cache, prove current configured count fits it,
  and bind the exact production loops to that contract without changing the
  persisted Config layout.
- **Scope/exclusions:** Mode-selection storage, focused compile-time/runtime
  bounds assertions, exact-production host sanitizer regression, build proof,
  and documentation/checker consequences only. No Config/protobuf/schema
  change, new mode, reordered or removed current mode, gameplay semantic
  choice, runtime-loaded config, persistence, device write, Nunchuk claim, or
  root-cause claim.
- **Dependencies:** Preserve all 13 current entries and their exact behavior.
  Curator must require an implementation that does not depend on an unstable
  generated C++ object layout for persistent ABI.
- **Validation:** Exercise initialization and selection for all 13 indices,
  boundary and invalid counts, duplicate/zero masks already permitted by
  source, and exact production bodies under ASan/UBSan or equivalent. Run the
  canonical Glyph build. Because active firmware behavior/storage changes,
  merge requires exact candidate Git SHA and exact tested artifact SHA-256
  hardware PASS under the accepted protocol.
- **Risk/gates/confidence/readiness:** H3 active firmware safety repair; build
  and exact-snapshot hardware required before merge; high source confidence;
  `CURATION_READY`.

### GP-VAL-024 — Declare the manifest helper closure

- **Objective/value:** Make the advertised validation manifest runnable from a
  clean clone by declaring every direct checker helper that its dependency
  preflight requires.
- **Current source evidence:** In a clean clone,
  `run_glyph_runtime_config_validation.py --check-manifest --json` returns
  `SETUP_FAILURE` because `table_replacement_contract` directly imports
  `tools/extract_glyph_identity_runtime_tables.py`, while that manifest entry
  declares only `tools/generate_source_owned_runtime_config.py`.
- **Implementation seam:** Add the exact extractor path to that entry's
  `source_dependencies`; retain the current dependency-closure and canonical
  proof semantics.
- **Scope/exclusions:** One manifest dependency declaration and deterministic
  manifest/census/health verification only. No checker logic, applicability or
  classification change, runner isolation, workflow, build, runtime, source,
  device, hardware, or GP-VAL-011 change.
- **Dependencies:** Preserve the current checker command, expected output,
  source dependency policy, and 42-entry manifest inventory.
- **Validation:** Prove `--check-manifest --json` succeeds in a clean clone and
  rejects removing or misspelling the helper; run manifest, census, health,
  framework, navigation, and independent review gates.
- **Risk/gates/confidence/readiness:** H0 manifest repair; no build or hardware;
  high confidence; `CURATION_READY`.

### GP-VAL-025 — Bind Python validation to the invoking interpreter

- **Objective/value:** Prevent an ambient PATH program named `python3` from
  replacing manifest checkers and producing a false-green aggregate.
- **Current source evidence:** Manifest commands begin with `python3`, the
  runner preserves ambient PATH and executes them through `execvpe`, and
  success is based on zero exit plus process/isolation/fingerprint success;
  checker output is summarized but not matched, so zero output is accepted. A
  disposable PATH-first `python3` shim that invokes `/usr/bin/true` made a
  representative checker return zero with empty output and the aggregate
  accepted it.
  GP-VAL-010 explicitly excluded PATH-resolved interpreter trust.
- **Implementation seam:** Normalize Python manifest commands to the invoking
  `sys.executable` inside the existing isolated launch path, while keeping the
  manifest's portable command representation and all non-Python command
  semantics unchanged.
- **Scope/exclusions:** Aggregate runner, focused isolation tests,
  documentation, and deterministic manifest/census/health consequences only.
  No generic OS/Git/package trust policy, dependency pinning, checker semantic
  change, workflow change, firmware, artifact, device, hardware, or
  GP-VAL-011 work.
- **Dependencies:** Preserve GP-VAL-010 environment normalization, current
  timeout/result classification, and direct checker usability.
- **Validation:** Reject PATH-first success, failure, and output-forging Python
  shims; prove the invoking interpreter is used for every Python manifest
  entry; retain exact success/failure/output behavior; run isolation tests,
  aggregate-focused tests, manifest/census/health, framework, navigation, and
  independent review.
- **Risk/gates/confidence/readiness:** H1 validation trust-boundary hardening;
  no build or hardware; high confidence; `CURATION_READY`.

### GP-SRC-010 — Close residual offline writer bypasses

- **Objective/value:** Make every offline/source-owned output path honor the
  exact-target, repository-containment, symlink/alias, and atomic-replacement
  invariant already established by GP-SRC-005.
- **Current source evidence:** `install_generated_source_owned_runtime_config.py`
  performs a lexical inert-path assertion and then `mkdir`/`write_text`;
  `prepare_source_owned_candidate_branch.py` writes its output directly; and
  `generate_source_owned_generator_modes.py prepare --output` accepts a
  repository file such as `AGENTS.md` and reaches `Path.write_text`. Exact
  target or ancestor symlinks can therefore bypass the intended boundary.
  This materially refines the held alternative from the prior packet after
  reconciliation against GP-SRC-005's recorded invariant.
- **Implementation seam:** Route all three high-level operations through one
  shared exact inert/offline target validator and atomic writer after
  operation-specific content generation.
- **Scope/exclusions:** The three wrappers, shared helper, adversarial
  filesystem fixtures, documentation, and deterministic manifest/census/health
  consequences only. No active source/table semantics or authority, generated
  values, runtime publication, build, persistence, device write, or hardware.
- **Dependencies:** Preserve GP-SRC-005's approved inert install exception and
  existing generated-content/output contracts. Do not widen authorized roots.
- **Validation:** Reject repo targets, target/ancestor symlinks, hard-link or
  inode aliases where observable, path traversal, wrong suffix/root, existing
  unsafe targets, and partial-write replacement; accept exact temporary inert
  destinations and prove byte-identical output. Run all three wrapper
  checkers, atomic-target tests, manifest/census/health, framework, navigation,
  and independent review.
- **Risk/gates/confidence/readiness:** H1 offline file-boundary hardening; no
  build or hardware; high confidence; `CURATION_READY`.

### GP-PROV-012 — Bind CI build provenance to a clean tracked worktree

- **Objective/value:** Prevent build inputs from diverging from the recorded
  candidate SHA through untracked, modified, or generated source immediately
  before or during the CI build.
- **Current source evidence:** `verify_checkout` compares candidate SHA with
  HEAD and verifies one postprocessor hash but does not inspect worktree
  integrity. A disposable clean clone with an untracked `src` C++ file still
  passes that gate. Workflow validation runs before Python/dependency setup and
  there is no clean-state proof adjacent to the build. The builder may stamp
  `-DIRTY` while the sidecar still records clean `$GITHUB_SHA`.
- **Implementation seam:** Reuse the repository's tracked-worktree integrity
  model in exact, failure-bearing workflow checks immediately before build and
  after build/before postprocessing, with generated/build outputs explicitly
  classified rather than globally ignored.
- **Scope/exclusions:** Provenance helper/checker, build workflow, focused
  dirty-state fixtures, docs, and deterministic manifest/census/health
  consequences. No action/dependency pins, reproducibility or race-free
  blanket claim, artifact custody/store, postprocessor byte semantics,
  firmware behavior, device, or hardware.
- **Dependencies:** Preserve GP-PROV-009 dirty-state definitions, exact
  candidate/HEAD binding, current build command, and sidecar fields. Curator
  must require a bounded allowed-output model rather than broad `git status`
  suppression.
- **Validation:** Reject modified tracked source, staged changes, untracked
  source/header/config/linker inputs, generated inputs, and post-build tracked
  mutation; accept exact current clean workflow and enumerated build outputs.
  Run provenance and both workflow checkers, focused disposable-repository
  cases, manifest/census/health, framework, navigation, and a build smoke if
  the workflow contract changes materially.
- **Risk/gates/confidence/readiness:** H1 CI provenance hardening; no hardware;
  high confidence; `CURATION_READY`.

## Horizon B — bounded evidence and runtime characterization

### GP-HW-002 — Content-bind candidate-local hardware protocols

- **Objective/value:** Ensure a hardware PASS names the exact reviewed protocol
  bytes rather than an arbitrary or later-mutated path string.
- **Current source evidence:** Work-order validation requires only nonblank
  protocol reference/version, and evidence validation string-matches those
  fields. A synthetic accepted work order can reference nonexistent
  `docs/test_protocol.md`. GP-HW-001 binds the evidence record bytes but not the
  protocol bytes.
- **Implementation seam:** Require a normalized tracked regular `100644`
  protocol path plus SHA-256 in the work order and evidence record, verified
  against the current reviewed tree without introducing candidate/base Git
  object requirements.
- **Scope/exclusions:** Queue/work-order/evidence schema and validators,
  normalized protocol fixtures, migration of the exact GP-CONFIG-005 and X1
  protocol records, docs, and deterministic manifest/census/health
  consequences. No reinterpretation of tests/results, candidate-source or
  artifact verification, Git-object/topology expansion, device action,
  firmware, or hardware execution.
- **Dependencies:** Preserve GP-HW-001 evidence-record correspondence and
  GLYPH-UD-013's prohibition on expanding GP-VAL-011 topology. Curator must
  confirm the bounded current-tree representation before authorizing schema
  migration.
- **Validation:** Reject missing, absolute, traversal, symlink, executable,
  untracked, wrong-digest, wrong-version, and post-evidence-mutated protocols;
  accept the exact normalized current protocols and preserve historical
  evidence semantics. Run queue/work-order/evidence fixtures,
  manifest/census/health, framework, navigation, and independent review.
- **Risk/gates/confidence/readiness:** H0 governance/evidence hardening; no
  build or hardware; high confidence conditional on representation review;
  `CURATION_READY`.

### GP-CONFIG-011 — Characterize custom-modifier cache capacity

- **Objective/value:** Establish exact-production behavior and ABI
  correspondence before deciding whether the 10-slot runtime modifier cache
  can safely consume schema-valid configurations with up to 20 modifiers.
- **Current source evidence:** `CustomControllerMode.hpp` declares
  `_modifier_button_masks[10]`; its constructor and processing loop use
  `custom_mode_config.modifiers_count` without a local bound. The tracked
  dependency selector is only the abbreviated
  `GregTurbo/HayBox-proto#db4e2f6`; the current ignored local build cache
  resolves that selector to observed commit
  `db4e2f68b5c4ddd407e7c11050a920c4b4ec54c8`, whose `config.options` declares
  `max_count: 20` for the modifier field. That observation is not a durable
  pin or independent upstream identity proof. Current SetConfig checks
  references but not this cache/count correspondence.
- **Implementation seam:** Build an exact-production host/ASan characterization
  that compiles against the build-resolved schema and binds the cache capacity,
  decoded maximum, constructor, and processing loop without selecting repair
  behavior.
- **Scope/exclusions:** Host harness, exact-source/dependency drift binding,
  fixtures, documentation/checker, and deterministic manifest/census/health
  consequences only. No cache/schema/count repair, modifier or gameplay
  semantic decision, SetConfig policy, transport, persistence, device access,
  firmware behavior, Nunchuk, or root-cause claim.
- **Dependencies:** Preserve current schema and runtime source. Any later
  repair that changes active behavior is separately H2/H3 and evidence-gated.
- **Validation:** Characterize counts 0, 10, 11, and 20; constructor writes,
  processing reads, decoded-array availability, and source/schema drift under
  sanitizers. Run current SetConfig/runtime checks, build-derived dependency
  verification, manifest/census/health, framework, navigation, and independent
  review.
- **Risk/gates/confidence/readiness:** H1 host characterization; no firmware
  build or hardware for characterization; medium-high confidence;
  `CURATION_READY` for characterization only.

## Horizon C — external-evidence-gated provenance

### GP-PROV-013 — Pin reviewed GitHub Actions to immutable commits

- **Objective/value:** Replace mutable major-tag action references in the
  firmware build workflow with reviewed immutable upstream commits.
- **Current source evidence:** `build.yml` uses `actions/checkout@v4` twice,
  `actions/setup-python@v5`, and `actions/upload-artifact@v4`; the current
  provenance inventory classifies these as
  `DECLARED_MOVABLE_NOT_RESOLVED`. GP-VAL-021 explicitly excluded dependency
  pins.
- **Implementation seam:** Resolve Curator-selected releases within the
  currently declared major lines through live upstream evidence, pin each
  exact action invocation to a full commit SHA, and retain human-readable
  release annotations plus a checker-owned mapping.
- **Scope/exclusions:** Those four existing build-workflow action references,
  bounded provenance fixture/checker, docs, and deterministic
  manifest/census/health consequences only. No runner image, pip/PlatformIO,
  library/toolchain pin, build-device/config unresolved route, release/store/
  custody change, firmware, device, or hardware.
- **Dependencies:** Requires successful live upstream tag-to-commit
  verification and Curator acceptance of the selected release identities.
  Stale local or remembered SHAs are not authority.
- **Validation:** Verify every selected full SHA against its official upstream
  tag/ref, reject tags/short SHAs/unapproved repos/annotation mismatch, accept
  the exact pinned workflow, and run workflow/provenance,
  manifest/census/health, framework, navigation, and independent review.
- **Risk/gates/confidence/readiness:** H1 supply-chain metadata change; no
  hardware; high need confidence but externally evidence-gated;
  `EVIDENCE_GATED`.

## User-direction and evidence effects

- GLYPH-UD-001/005 preserve the Glyph/Senscope split. No candidate invents
  modifier, layout, routing, or game semantics.
- GLYPH-UD-013 keeps GP-VAL-011 owner-deferred and nonexecutable. No candidate
  changes aggregate ignored-state topology, fingerprinting, budgets, or
  candidate/base Git-object roots.
- GLYPH-UD-015 preserves exact source/build/artifact correspondence but does
  not authorize release, external storage, flashing, or device write.
- GLYPH-UD-016 means the consumed prior packet correctly returned to planning.
  This material packet creates a fresh Curator-review obligation only when
  published; it does not authorize its own candidates.
- Existing GP-CONFIG-005 and X1 exact hardware PASS records remain unchanged.
  GP-HW-002 proposes future content binding without invalidating or
  reinterpreting those results. Nunchuk remains `NOT_TESTED`, and root cause
  remains unproven.
- GP-CONFIG-010 is the sole active firmware repair candidate. Its source-level
  out-of-bounds finding does not itself establish a physical symptom, device
  acceptance, or causal explanation.

## Rejected and held alternatives

| Alternative | Disposition | Evidence-based reason |
| --- | --- | --- |
| Reopen GP-VAL-011 | REJECT | GLYPH-UD-013 makes aggregate isolation/topology repair owner-deferred and nonexecutable. |
| Republish GP-PERSIST-003 | HOLD | Exact target `size_t`/ConfigHeader ABI, nanopb/CRC identity, and accepted injectable fault model remain unresolved; no new evidence closes them. |
| Add candidate/base Git-object verification to GP-HW-002 | HOLD | Making object availability load-bearing expands the forbidden GP-VAL-011 isolated-clone/topology lane. |
| Repair zero/default-mode menu behavior now | SUBSTANTIVE_DEPENDENCY_GATED | GP-CONFIG-009 supplied characterization; expected empty/no-mode UI behavior remains a product decision. |
| Repair the custom-modifier cache now | HOLD | Characterization must first establish build-resolved ABI behavior; any repair is H2/H3 and needs separately curated expected behavior. |
| Immediate SetConfig live-rebind repair | SUBSTANTIVE_DEPENDENCY_GATED | Reboot versus in-place reinitialization and backend/display/RGB/cache publication remain unresolved. |
| Treat failed SetConfig acknowledgment as a retry fix | SUBSTANTIVE_DEPENDENCY_GATED | Persistence or live publication may already have succeeded; retry/idempotency semantics need a decision. |
| Implement atomic persistence/recovery | REJECT | Old-or-new guarantees, boot/default/autoformat, compatibility, diagnostics, concurrency, wear, and recovery UX remain unresolved H3 decisions. |
| Resolve build-device/config provenance in GP-PROV-013 | HOLD | The required source/release authority is broader than the bounded action-pin candidate. |
| Recover old planning, curation, result, or failed branches | REJECT | They are integrated, consumed, superseded, historical evidence, or explicitly nonexecutable; branch names do not authorize work. |
| Activate candidate/active-storage/runtime-loaded/device-write paths | REJECT | These paths remain forbidden and active storage has separate hardware-failure evidence. |
| Add modifier/table values or infer gameplay semantics | REJECT | Owner/Senscope source authority is absent. |
| Propose portfolio-global evidence wait | REJECT | Seven independent `CURATION_READY` candidates exist outside owner-gated product and persistence-repair lanes. |

## Global wait assessment

`global_wait_proposed: false`. The fresh broad audit found independent active
runtime safety, manifest closure, interpreter trust, offline file-boundary,
build provenance, hardware-evidence, and host characterization work that does
not depend on new owner/Senscope modifier data or physical testing. Live
upstream evidence is candidate-local to GP-PROV-013. Owner/Senscope delivery of
exact layout values, owned table/routing scope, and expected outputs remains
the event that can resume the next H2 product lane, but it does not establish
portfolio-global scarcity.

## Curator handoff

The Work-Order Curator must independently review this exact packet as:

- packet ID `glyph-portfolio-20260920-1457`;
- planning branch `planning/portfolio-20260920-1457`;
- base `37b6d7e5573703f9854674e424b526cf7f5dd1a8`;
- exact immutable planning commit supplied by the published branch;
- initial inventory of exactly eight candidate headings;
- `curator_review_required: true`;
- `global_wait_proposed: false`.

No score, ranking, or readiness estimate above is authorization. The Curator
must independently verify every candidate, choose dispositions, create the
required immutable receipt, and adopt any queue state through the separate
canonical curation sequence.
