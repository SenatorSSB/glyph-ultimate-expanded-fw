# Glyph Portfolio Candidate Packet — 2026-08-24 23:49 EEST

```yaml
packet_id: glyph-portfolio-20260824-2349
packet_state: FRESH
planning_branch: planning/portfolio-20260824-2349
base_configurator_sha: caf37e10673896b3bf5e2815875a93310b3f3ac1
base_live_verified: true
previous_planner_packet: glyph-portfolio-20260823-2349
previous_packet_branch: planning/portfolio-20260823-2349
previous_packet_commit: 387a2a7b27d11b81c3c571aaf07cf543af626757
previous_packet_freshness: PARTIALLY_CONSUMED_STILL_USEFUL
candidate_count: 5
new_candidate_count: 0
carried_forward_candidate_count: 4
repair_reactivation_candidate_count: 1
curator_review_required: true
global_wait_proposed: false
material_events_since_publication: []
planner_authority: NON_AUTHORITATIVE_CANDIDATE_SUPPLY_ONLY
configurator_changed: false
```

## Result and freshness

This is a material correctness refresh, not a timer- or SHA-drift duplicate.
The previous ten-candidate packet is substantially consumed but remains useful:

- `GP-SRC-003`, `GP-SRC-004`, `GP-CONFIG-004`, `GP-VAL-003`, and
  `GP-SRC-005` are integrated and recorded Done. `GP-VAL-004` was separately
  curated and completed after that packet.
- `GP-PROV-002` is recorded Done on live `configurator`, but reviewed feature
  tip `9c94b5449b8065cb02aa0689ca0564720238b80c` is not an ancestor of live
  `configurator`. That is a third false completion-publication event and
  requires recovery, not a new Planner candidate.
- Previous candidate `GP-CTL-002` directly covers that completion/integration
  correspondence class. A clean local Curator branch proposes its
  Preauthorization and a separate local `configurator` worktree contains the
  reviewed `GP-PROV-002` merge, but both are legitimate concurrent local work
  pending publication; neither is substituted for live canonical authority.
  Planner carries `GP-CTL-002` without duplicating recovery or curation.
- `GP-PROV-003`, `GP-AUTH-001`, and `GP-CONFIG-002` survive as research,
  user/source-authority, and external-evidence supply respectively.

The material new event is a reproduced fail-open correspondence gap in the
just-shipped `GP-CONFIG-004` reviewed-capture checker. On exact live base
`caf37e1`, a synthetic accepted-shape packet passed while its diff row said
`FAIL`, its comparison record said `PASS`, `result.md` said `PARTIAL`, its
top-level artifact paths and hashes contradicted the nested artifact record,
and two nested checker/comparison paths named nonexistent files. The current
zero-capture and adversarial tests still pass because they do not exercise
those contradictions.

This new correctness evidence changes candidate supply even though the queue's
primary state remains `CURATION_REQUIRED`. The refresh reactivates exactly one
bounded H0 repair against the original `GP-CONFIG-004` identity and carries the
four live-base survivors. It authorizes and implements nothing.

## Live base and broad-audit evidence

The restricted sandbox could not resolve GitHub. The required network-enabled
read-only retry succeeded. Immediately before packet authoring, live
`refs/heads/configurator` remained
`caf37e10673896b3bf5e2815875a93310b3f3ac1`; the planning worktree was created
directly from that SHA. Live previous planning ref
`planning/portfolio-20260823-2349` remained at
`387a2a7b27d11b81c3c571aaf07cf543af626757`, and live reviewed
`GP-PROV-002` feature ref remained at
`9c94b5449b8065cb02aa0689ca0564720238b80c`.

The audit inspected:

- every Planner-required current governance, queue, workflow,
  implementation-boundary, user-direction, authorization/runway, and hardware
  evidence document;
- the full previous packet, current Curator dispositions, live ancestry,
  current worktrees, recent curation/implementation history, and the exact
  `6b8ebcd..caf37e1` changed-file surface;
- active table-source inclusion/publication boundaries, generator-v2 and
  remaining offline writer paths, source-authority intake, coordinate-native
  bridge, fixtures, negative corpora, and source-sync checks;
- official-configurator primary corpus, five-check current lane,
  manual-capture schema/checker/template, synthetic reviewed packets, and the
  absence of a real completed operator capture;
- checker census, curated validation manifest, validation health, aggregate,
  branch context, agent framework, docs navigation, and agent surface;
- both tracked workflows, current publication-route classifications,
  observed-only postprocessor provenance, PlatformIO/build-input declarations,
  and immutable-artifact non-claims;
- current hardware evidence boundaries: no new exact-snapshot result, Nunchuk
  remains `NOT_TESTED`, and low-level root cause remains unproven.

Observed checks on an isolated exact-live-base clone with both local and
remote-tracking `configurator` pinned to `caf37e1`:

- `python3 tools/run_glyph_runtime_config_validation.py --json`: PASS; census
  freshness plus every current load-bearing check.
- `python3 tools/check_glyph_checker_census.py`: PASS; 189 entries.
- `python3 tools/check_glyph_agent_framework_docs.py`: PASS.
- `python3 tools/check_glyph_docs_navigation.py`: PASS.
- `python3 tools/check_glyph_docs_agent_surface.py`: PASS.
- `python3 tools/check_glyph_official_configurator_validation.py`: PASS; five
  offline primary-corpus checks and no compatibility claim.
- `python3 tools/check_glyph_official_configurator_manual_capture_result.py`:
  PASS with zero completed captures.
- manual-capture `--adversarial-test`: PASS, but it lacks contradictory-field
  coverage.
- `python3 tools/check_glyph_artifact_postprocessor_provenance.py --check`:
  PASS with postprocessor execution not performed, transformation unknown, and
  immutable locator unresolved.
- a temporary monkeypatched synthetic reviewed-packet reproduction:
  `accepted_contradictory_reviewed_capture=true`.

No repository product file, active table byte, workflow, firmware artifact, or
hardware state was changed during this audit.

## Largest current bottleneck

The largest bottleneck remains **machine-verifiable correspondence between a
declared result/state and the exact content integrated or reviewed**.

The third false Done publication shows the control-plane form and is already
under recovery/Curator treatment. The new independent instance is inside the
official-configurator evidence lane: strict-looking duplicate fields and
status records can disagree while the checker certifies the packet. Closing
that exact offline evidence gap is higher value than adding app automation,
runtime semantics, more generator modes, or another provenance inventory.

## Horizon A — repair current correspondence failures

### GP-CONFIG-004 repair/revalidation — Bind every reviewed-capture status and artifact field

- **Objective/value:** make a reviewed official-configurator capture pass only
  when metadata status, result markdown, required result rows, comparison
  record, exact artifacts, paths, and hashes describe one internally
  consistent capture packet.
- **Current source evidence:** live
  `tools/check_glyph_official_configurator_manual_capture_result.py` validates
  the comparison-file status against
  `metadata.comparison.checker_output_status` but never binds either value to
  the `post_capture_json_diff_review` row. `validate_result_doc()` parses an
  overall status but returns nothing and never compares it with metadata
  `status`/`result_status`. File validation prefers filled `artifacts.*` values
  through `or`, so contradictory top-level paths/hashes are ignored.
  `artifacts.checker_output_path`, `comparison_path`, and
  `rejection_note_path` are type-checked but not resolved and shape-bound. The
  accepted synthetic contradiction reproduces all four gaps on `caf37e1`.
- **Implementation seam:** reopen and strengthen the existing reviewed-capture checker
  and its synthetic/adversarial corpus. Require the result-document status to
  equal both metadata overall fields; require metadata comparison status and
  comparison-file status to equal the diff row; require duplicate top-level
  and nested artifact path/hash fields to be exact equals for the selected
  accepted/rejected shape; require the exact semantic basenames
  `input_candidate.json`, `output_export.json`, `metadata.json`, `notes.md`,
  `comparison.json`, and `rejection_note.md` for their respective non-null
  fields; require executed diff `checker_output_path` and `comparison_path` to
  both equal `comparison.json`; resolve each path to that exact capture-local
  regular file; bind gaps to every unexecuted/missing row; and require exact
  null/non-null combinations for accepted-output, rejected-note, and
  executed-diff shapes already defined by schema v2.
- **Scope/exclusions:** offline checker, synthetic fixtures, template/docs, and
  deterministic checker-census/health consequences only. No official app
  launch, operator capture, compatibility judgment, production exporter,
  workflow, firmware/runtime source, device/protobuf write, persistence,
  flashing, hardware result, or game-semantic claim.
- **Dependencies:** the canonical `GP-CONFIG-004` work order supplies the
  intended schema-v2 matrix, but its Done claim is incomplete under the new
  reproduction. Curator must decide the canonical reopen/repair transition.
  The live capture tree still has zero completed captures, so no real evidence
  migration or reinterpretation is authorized. `GP-CONFIG-002` remains
  external-evidence gated after this repair.
- **Validation:** retain complete synthetic PASS/FAIL/PARTIAL/INCONCLUSIVE and
  rejected shapes; reject comparison-vs-row status mismatch,
  comparison-file-vs-row mismatch, result-markdown-vs-metadata mismatch,
  top-level-vs-nested path/hash mismatch, nonexistent, aliased, or
  wrong-existing-file nested paths, wrong semantic basename,
  output/rejection/comparison nullability drift, incomplete/extraneous gaps,
  stale/extra/missing hashes, symlinks, unknown files, and positive claims.
  Mutate each contradiction independently as well as in one combined packet.
  Run the manual-capture checker and adversarial test, five-check official lane,
  checker census, current aggregate, framework, navigation, and agent-surface
  checks.
- **Risk estimate:** H0.
- **Hardware/evidence/user gates:** Curator authorization and independent
  offline-evidence checker review; no hardware or app operation.
- **Confidence:** high.
- **Decision-aid score:** 99/100.
- **Non-authoritative readiness estimate:** `CURATION_READY`.

### GP-CTL-002 — Prove canonical Done/integration correspondence

- **Objective/value:** prevent canonical Done publication unless immutable Git
  evidence proves that the reviewed authorized implementation, or one exact
  reviewed replay, is already integrated in canonical history.
- **Current source evidence:** `GP-SRC-003` was twice published Done without
  its repaired tree. Live `GP-PROV-002` repeats the failure: queue publication
  commit `dfc92adf2910532e24f429f61ea3c1fe7026425a` records Done, but reviewed
  feature tip `9c94b5449b8065cb02aa0689ca0564720238b80c` is not an ancestor of live
  `caf37e1`. The current framework accepts prose `done_evidence` without Git
  integration proof.
- **Implementation seam:** preserve a migration-safe immutable legacy Done set,
  then require structured full-SHA Git-object-backed completion evidence for
  every later transition. Direct integration must prove ancestry; reviewed
  replay must prove one dedicated integration commit with exact changed path
  set, modes, blob identities, and deletions, without patch-id or semantic
  equivalence guesses. Completion publication must descend from the prior
  integration commit.
- **Scope/exclusions:** agent-framework docs, schema/queue contract, ordinary
  governance checker, and synthetic temporary Git fixtures only. No automatic
  merge/replay/recovery, queue promotion, product/runtime checker, workflow,
  firmware, artifact, hardware, or device action.
- **Dependencies:** legitimate `GP-PROV-002` publication recovery comes first.
  The clean local Curator and local `configurator` worktrees are concurrent
  evidence, not live authority; Curator must reconcile or publish them. Any
  different replay/equivalence shape requires renewed judgment.
- **Validation:** direct integration and exact dedicated replay positive cases;
  missing object, abbreviated/mutable ref, wrong base, sibling feature, partial
  replay, extra path, changed mode/blob, deletion mismatch, changed legacy set,
  and integration-after-publication negatives; framework, sequence, aggregate,
  census, navigation, and surface checks.
- **Risk estimate:** H0.
- **Hardware/evidence/user gates:** Curator authorization/reconciliation and
  focused independent governance review; no hardware.
- **Confidence:** high on the gap, medium-high on migration/replay shape until
  canonical curation is published.
- **Decision-aid score:** 98/100.
- **Non-authoritative readiness estimate:** `CURATION_READY`; canonical queue
  remains `SUBSTANTIVE_DEPENDENCY_GATED` until Curator publishes otherwise.

## Horizon B — bounded provenance research

### GP-PROV-003 — Inventory build-input provenance without pin changes

- **Objective/value:** keep the declared toolchain, workflow-action,
  dependency, source-tree, and postprocessor identity boundary explicit before
  any reproducibility or exact-build claim.
- **Current source evidence:** workflows and `platformio.ini` retain mutable
  selectors; no complete resolved build-input set is recorded. Existing
  postprocessor identity and observed-only sidecar work do not resolve the
  toolchain/dependency graph.
- **Implementation seam:** deterministic static census/manifest/checker for
  declared inputs, selector class, recorded resolution evidence, and explicit
  unknowns. Do not resolve from the network and do not change pins.
- **Scope/exclusions:** docs/research/checker only; no install, dependency or
  action update, build, artifact comparison, reproducibility claim,
  postprocessor execution, upload, device action, or hardware claim.
- **Dependencies:** Curator must first bind the inventory boundary across
  PlatformIO registry/Git dependencies, workflow actions, platform/framework
  packages, source SHA, and tracked postprocessor. Any remediation is a
  separate risk-bearing candidate.
- **Validation:** static discovery and manifest freshness; added, removed, or
  changed mutable selectors fail; exact pins, tags, ranges, moving runner
  labels, and unknown resolution remain accurately classified.
- **Risk estimate:** H0.
- **Hardware/evidence/user gates:** Curator research-scope review; later pin or
  build changes require separate build/hardware judgment.
- **Confidence:** high on the gap, medium on initial schema boundary.
- **Decision-aid score:** 73/100.
- **Non-authoritative readiness estimate:** `RESEARCH`.

## Horizon C — external authority and evidence supply

### GP-AUTH-001 — Source-authorized production table delta intake

- **Objective/value:** obtain an explicit production-owned table set and exact
  replacement content for the completed source-owned lane.
- **Current source evidence:** current intake remains source-equivalent and
  `NONE_AUTHORIZED`; no owned production delta exists.
- **Implementation seam:** user/Senscope-provided versioned intake with exact
  symbols, points, authority references, intent, approval, acknowledgements,
  and current baseline identity.
- **Scope/exclusions:** Planner/Curator may not invent ownership, mapping,
  coordinates, game semantics, or approval. Intake is not a firmware candidate
  or hardware result.
- **Dependencies:** explicit human/Senscope authority and exact content.
- **Validation:** intake and negative corpus first; any later active delta needs
  separate candidate review, canonical build, immutable exact artifact, and
  H2/H3 physical PASS.
- **Risk estimate:** H3 for eventual active behavior; intake itself is evidence.
- **Hardware/evidence/user gates:** explicit user/source authority.
- **Confidence:** high on the missing gate, unknown on future content.
- **Decision-aid score:** 70/100.
- **Non-authoritative readiness estimate:** `USER_DECISION_GATED`.

### GP-CONFIG-002 — Exact official-configurator manual capture

- **Objective/value:** replace unknown app version/route/acceptance evidence
  with a new hashed operator record.
- **Current source evidence:** the five-check offline lane passes and the live
  tree has zero completed captures. The existing procedure now has a schema-v2
  checker, but the `GP-CONFIG-004` repair must close its reproduced internal
  correspondence gaps before operator evidence is trusted.
- **Implementation seam:** operator-performed capture using the current
  procedure only after the checker repair is independently reviewed and
  canonically integrated.
- **Scope/exclusions:** no automated app interaction, universal compatibility,
  production exporter, firmware/runtime change, or device write.
- **Dependencies:** repaired and revalidated `GP-CONFIG-004` complete, then an
  external operator with official-configurator access.
- **Validation:** corrected manual-capture checker, exact file hashes,
  capture-local comparison, bounded reviewed result, and explicit gaps for any
  partial/inconclusive outcome.
- **Risk estimate:** H1 evidence operation; no firmware hardware claim.
- **Hardware/evidence/user gates:** exact external operator capture.
- **Confidence:** medium-high.
- **Decision-aid score:** 62/100.
- **Non-authoritative readiness estimate:** `SUBSTANTIVE_DEPENDENCY_GATED`
  behind `GP-CONFIG-004` repair, then `EVIDENCE_GATED` on external capture.

## User-direction and evidence effects

- `GLYPH-UD-001` keeps the packet inside Glyph/HayBox realization and excludes
  Senscope-owned game semantics, datasets, and solver authority.
- `GLYPH-UD-002` fixes `configurator` as the live comparison branch and the
  canonical build command when compiled behavior requires it.
- `GLYPH-UD-003` prevents checker or artifact metadata from becoming hardware
  PASS or device-update authority by assertion.
- `GLYPH-UD-004` preserves Planner/Curator separation; this packet is candidate
  supply only.
- `GLYPH-UD-005` does not supply missing production table ownership and does
  not waive H2/H3 physical acceptance.
- `GLYPH-UD-006` is satisfied only by the separate validation repair/recovery
  sequence; it does not broaden this packet.
- No new hardware result, runtime-loaded capability, production-owned table
  set, device-write capability, Nunchuk result, or proven root cause was found.

## Rejected or held alternatives

- **Return `NO_MATERIAL_PORTFOLIO_UPDATE` because four old candidates survive
  — REJECT.** A shipped checker introduced new reproducible correctness
  evidence after packet publication; this is not time drift or waiting for
  Curator.
- **Create a second `GP-CTL-002` or `GP-PROV-002` identity — REJECT.** The
  previous packet already identified completion/integration correspondence,
  and legitimate recovery/Curator work exists locally pending publication.
  Planner carries the live-base candidate but must not duplicate or authorize
  it.
- **Treat the false Done record as proof that the reviewed feature is
  integrated — REJECT.** The exact feature tip is not an ancestor of live
  configurator.
- **Mint `GP-CONFIG-005` for the checker failure — REJECT.** The canonical
  `GP-CONFIG-004` work order already required these exact correspondence and
  adversarial invariants; a new ID would conceal incomplete Done evidence.
- **Fix only the comparison-row mismatch — REJECT.** One accepted packet
  simultaneously proved result-document, duplicate artifact, and unbound-path
  contradictions in the same schema-v2 correspondence boundary.
- **Delete duplicate schema fields without migration review — REJECT.** The
  current template exposes both representations; the bounded correction is to
  bind them exactly unless Curator separately authorizes schema migration.
- **Perform a real operator capture before checker repair — REJECT.** That
  would create evidence the current checker cannot bind fail closed.
- **Fold app automation, production export, or compatibility interpretation
  into the `GP-CONFIG-004` repair — REJECT.** The repair is offline checker
  work only.
- **Treat `GP-VAL-004` temp-root behavior as a new policy gap — REJECT.** The
  current change repaired stale checker assumptions while preserving the
  already-authorized isolated-output policy.
- **Repair `build-device-config.yml` or mutable build pins now — REJECT.**
  Caller/ownership remains unresolved, and provenance remediation needs
  separate risk and source-authority judgment.
- **Add runtime-loaded config, persistence, WebSerial/device write, protobuf
  write, release upload, or flashing automation — REJECT.** Current approval,
  source, and architecture requirements are absent.
- **Claim Nunchuk validation or low-level root cause — REJECT.** Direct
  evidence remains absent.

## Global evidence wait assessment

`GLOBAL_EVIDENCE_WAIT_SUPPORTED` is **not proposed**
(`global_wait_proposed: false`). The fresh audit found an independent H0
correctness repair while separate recovery/Curator work is pending. External
production authority and operator capture therefore do not establish
portfolio-global scarcity.

## Fresh adversarial review

A separate read-only adversarial review challenged materiality, candidate
identity, accounting, readiness, local-concurrency treatment, repair scope, and
global-wait disposition. Its required corrections are incorporated:

- preserve the failure as `GP-CONFIG-004` repair/revalidation rather than mint
  a new ID that would conceal incomplete Done evidence;
- count all four live-base survivors, including `GP-CTL-002`, even though
  legitimate local recovery/Curator work is pending publication;
- require exact semantic basenames and reject wrong-existing-file targets, not
  merely nonexistent or escaping paths;
- retain `PARTIALLY_CONSUMED_STILL_USEFUL` rather than implying absent/stale
  supply; and
- keep local concurrent work explicitly non-authoritative until live canonical
  publication.

Repaired-scope adversarial verdict: PASS.

## Planning-branch validation

- `git diff --check`: PASS.
- `python3 tools/check_glyph_implementation_planning_packets.py`: PASS.
- `python3 tools/check_glyph_docs_navigation.py`: PASS.
- `python3 tools/check_glyph_docs_agent_surface.py`: PASS.
- `python3 tools/check_glyph_agent_framework_docs.py`: PASS.
- `python3 tools/check_glyph_checker_census.py`: PASS; 189 entries.
- The full runtime-config aggregate passes on the exact recorded live base.
  On the planning branch, expected content-and-scope guards reject the new
  `docs/planning/` path; Planner did not edit or weaken those runtime checkers,
  and this packet is not a merge candidate for `configurator`.
- No build or hardware run is required for a non-authoritative planning-only
  document.

## Curator handoff

This packet authorizes and implements nothing. Curator must independently
reproduce the `GP-CONFIG-004` completion failure, decide its canonical
reopen/repair transition, and create or restore a complete work order before
implementation. Suggested review order: finish or reconcile the legitimate
local `GP-PROV-002` recovery and `GP-CTL-002` curation pending publication,
then review the `GP-CONFIG-004` repair. `GP-PROV-003`, `GP-AUTH-001`, and
`GP-CONFIG-002` remain lower-horizon supply.
