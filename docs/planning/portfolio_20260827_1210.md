# Glyph Portfolio Candidate Packet — 2026-08-27 12:10 EEST

```yaml
packet_id: glyph-portfolio-20260827-1210
packet_state: FRESH
planning_branch: planning/portfolio-20260827-1210
base_configurator_sha: 8c04262c66613d46b933b1b739c01c575cb0c580
base_live_verified: true
previous_planner_packet: glyph-portfolio-20260827-0132
previous_packet_branch: planning/portfolio-20260827-0132
previous_packet_commit: 174cac2a61b39de543d110fb9319465961501812
previous_packet_base: a747dd54b02b207483142331d8b5be1113fc951e
previous_packet_freshness: MATERIALLY_CONSUMED
candidate_count: 10
repair_reactivation_count: 3
new_candidate_count: 5
carried_forward_candidate_count: 2
current_ready_work_order_count: 1
curator_review_required: true
global_wait_proposed: false
planner_authority: NON_AUTHORITATIVE_CANDIDATE_SUPPLY_ONLY
configurator_changed: false
```

## Result, freshness, and portfolio state

This is a material broad-audit refresh of live `configurator`, not a timer-only
or SHA-drift packet. Ordinary sandboxed GitHub resolution failed, and the
required permitted read-only network retry established live
`refs/heads/configurator` at
`8c04262c66613d46b933b1b739c01c575cb0c580`. The packet branch is based on
that exact SHA.

The previous packet is materially consumed:

- `GP-PROV-004` was curated, implemented, merged, and status-published `DONE`.
  This audit found direct source-correspondence defects in that result, so the
  same work identity needs repair/revalidation rather than a duplicate ID.
- `GP-PROV-005` is the only current `READY` work order and supplies one unit of
  immediate runway.
- `GP-ART-001` and `GP-X1-001` remain genuinely user-decision-gated; neither
  became executable.
- current canonical queue markers report one `READY`, zero recorded
  `PREAUTHORIZED`, zero mechanically activatable, zero hardware-pending, and
  effective runway one against target four.
- both `RUNWAY_SHORTFALL_CANDIDATE_SUPPLY` and
  `RUNWAY_SHORTFALL_USER_DECISION_GATED` are present, requiring this broad
  candidate-supply audit.

The dominant portfolio bottleneck is now **machine-verifiable correspondence**:
several checkers prove internal fixture consistency while failing to prove that
the fixture or prose still corresponds to the current source and canonical
machine state. The strongest evidence is the wrong workflow expressions
accepted by the completed `GP-PROV-004` checker, plus two independent stale
human-readable count/runway surfaces accepted by current framework and health
checks.

`GLOBAL_EVIDENCE_WAIT_SUPPORTED` is not proposed. One bounded current work
order is already `READY`, and multiple H0 repair or validation candidates can
be curated without game-semantic, firmware-behavior, hardware, external-store,
or user-product decisions.

## Broad-audit evidence

The audit read the current agent/context/state/roadmap/workflow/boundary and
framework authority documents, the complete active queue, current user
directions and decision records, current and historical hardware evidence, the
latest planning packet, runtime-validation manifest/health/census, source-
owned candidate tooling, current tests and fixtures, tracked CI workflows,
PlatformIO build configuration, postprocessor/provenance records, and current
firmware publication surfaces.

Observed current facts on the exact base:

- the current runtime-config validation aggregate passes with 30 manifest
  entries, 25 load-bearing entries, and checker census 192;
- all three explicit offline coordinate/package modes pass when invoked
  manually, but the aggregate does not execute them;
- the excluded source-owned candidate-generation checker currently fails its
  active-table refusal assertion because its branch guard preempts the intended
  active-target guard; it leaves the audited worktree unchanged;
- the canonical active source/table state was not edited, and no new exact-
  snapshot hardware result exists;
- the canonical-grid candidate remains failed, legacy Y2 and Alternative-B
  results apply only to their accepted merged mechanisms, and Nunchuk remains
  `NOT_TESTED`;
- `GLYPH-UD-007` continues to retire the official-configurator dependency;
  `GLYPH-UD-008` and `GLYPH-UD-009` authorize only the baseline-equivalent
  sole-`kX1Table` no-op intake and no different X1 bytes;
- no runtime-loaded profile/config, device write, protobuf binary write,
  persistent runtime-config storage, or flashing automation is authorized.

No product file, active firmware source, workflow, queue, hardware record, or
canonical document was changed by the Planner. This branch adds this packet
only.

## Horizon A — correctness repairs and immediate coverage

### GP-PROV-004 repair/revalidation — Bind observations to their exact sources

- **Identity:** reopen `GP-PROV-004`; do not mint a successor for incomplete
  acceptance of the same observational-resolution objective.
- **Direct defect:** `.github/workflows/build-device-config.yml` derives its
  external repository and revision through
  `fromJson(needs.metadata.outputs.meta_json).repo` and `.revision`, while
  `tools/check_glyph_build_input_resolution_observations.py` and its fixture
  assert invented `${{ inputs.repo }}` / `${{ inputs.revision }}` strings.
- **Additional gaps:** 31 observation records use the PlatformIO registry as
  an alleged authoritative locator for heterogeneous GitHub Actions, runner,
  Python, pip, release-action, and external-repository inputs; locator and
  lookup method are checked syntactically only; an arbitrary 40-hex observed
  identity can pass without equality to selector/ref/evidence; base SHA is
  shape-only; record ordering is unchecked; and the manifest omits the
  workflow/meta sources on which the result depends.
- **Objective:** repair the fixture/checker/docs so each claim is
  source-classified and exactly corresponds to current tracked declaration
  bytes, an appropriate upstream/evidence locator, the reported immutable
  identity when one is claimed, and the exact base commit/blob set.
- **Scope:** deterministic offline checker, observation fixture, provenance
  prose, manifest dependency metadata, and mechanically consequent census or
  health values only.
- **Exclusions:** no new external facts inferred from registry shape; no pins,
  dependency install, workflow/build input change, build, binary execution,
  artifact, device, or reproducibility claim.
- **Dependencies/gates:** current declaration bytes must remain unchanged;
  live facts admitted as evidence need reproducible authoritative locators or
  must be downgraded to explicit unresolved observations.
- **Validation:** deliberate wrong-expression, wrong-source-class,
  wrong-identity, wrong-base-blob, missing-dependency, and order-drift cases
  fail; all current aggregate/framework/navigation/surface checks pass.
- **Risk/readiness:** H0, `CURATION_READY`; confidence high.

### GP-CTL-001 repair/revalidation — Restore machine/prose runway parity

- **Identity:** reopen `GP-CTL-001`; its original scope required human-readable
  current-state summaries to agree mechanically with canonical runway markers.
- **Direct defect:** `docs/AGENT_CONTEXT.md` and `docs/ROADMAP.md` still say
  effective runway is two while current markers, `CURRENT_STATE.md`, and the
  queue report one. The queue's closing priority prose still orders completed
  `GP-PROV-004` before the sole Ready `GP-PROV-005`.
- **Objective:** make every current human-readable runway/count/priority claim
  derive from or mechanically agree with the canonical machine markers.
- **Scope:** current control-plane prose and the existing agent-framework
  checker/fixtures only.
- **Exclusions:** no queue status, candidate authority, threshold, milestone,
  or product-scope change.
- **Validation:** adversarial stale numeric and stale priority prose in every
  current mirror fails; historical packets remain historical; current
  framework/navigation/surface checks pass.
- **Risk/readiness:** H0, `CURATION_READY`; confidence high. The Curator may
  couple this with `GP-VAL-003` mechanically while preserving both identities.

### GP-VAL-003 repair/revalidation — Restore health prose/count parity

- **Identity:** reopen `GP-VAL-003`; its accepted validation-health objective
  required prose counts to agree mechanically with machine-derived state.
- **Direct defect:** `docs/runtime_config/runtime_config_validation_health.md`
  reports 29 manifest entries and 24 load-bearing entries, while its current
  machine fixture and the successful aggregate report 30 and 25. The health
  checker validates the JSON against the manifest but never reads the Markdown
  counts.
- **Objective:** bind current validation-health prose counts/classifications to
  the machine fixture and manifest-derived result.
- **Scope:** health Markdown, existing JSON/checker, and adversarial fixtures.
- **Exclusions:** no reclassification of a checker, manifest expansion, or
  source/runtime behavior change in this repair.
- **Validation:** stale prose count, class subtotal, and current-result summary
  each fail; historical records remain excluded; aggregate and docs checks
  pass.
- **Risk/readiness:** H0, `CURATION_READY`; confidence high.

### GP-VAL-005 — Make advertised offline packaging modes load-bearing

- **Gap:** the manifest runs the coordinate-native contract checker with no
  arguments. Its default path omits `--check-offline-pipeline`,
  `--check-offline-artifact-bundle-manifest`, and
  `--check-offline-export-package`, although current docs advertise those
  modes and all three pass manually.
- **Objective:** ensure each advertised offline packaging/pipeline contract is
  exercised by the canonical aggregate, either as explicit manifest entries
  or a deliberately expanded no-argument contract.
- **Scope:** manifest/default checker routing, exact dependency metadata,
  adversarial fixtures, and consequent machine/prose counts.
- **Exclusions:** no real artifact, device, WebSerial, protobuf write,
  persistence, flashing, vendor export, or neutral-profile schema change.
- **Validation:** each mode has a prove-it-ran sentinel; malformed bundle
  manifest, package layout, and offline pipeline cases fail independently;
  aggregate passes from a clean checkout.
- **Risk/readiness:** H0, `CURATION_READY`; confidence high.

## Horizon B — bounded hardening after urgent repairs

### GP-VAL-006 — Restore current candidate-preparation safety coverage

- **Gap:** `tools/check_glyph_source_owned_candidate_generation.py` is excluded
  as unsafe/mutating and currently fails on `configurator`: its test requests a
  synthetic candidate branch while the tool sees the real current branch, so
  the direct-configurator guard preempts the intended active-table-source
  refusal. The current run made no worktree change.
- **Objective:** make the checker context-independent and add a non-mutating,
  load-bearing safety lane for branch, active-target, scope, and dry-run
  refusal behavior.
- **Scope:** isolated temporary Git repositories/worktrees or static checks,
  dry-run-only default coverage, explicit classification of any materializing
  tests, manifest/health consequences.
- **Exclusions:** no candidate materialization in the canonical worktree, no
  active source or table edit, no firmware behavior, build, hardware, merge,
  or publication authority.
- **Dependencies/gates:** design must demonstrate that the aggregate cannot
  write the repository and that branch identity is fully controlled by the
  fixture.
- **Validation:** direct-configurator, wrong candidate branch, active target,
  unrelated path, dirty tree, and valid dry-run cases are independently
  asserted; before/after repository identity and status are byte-for-byte
  unchanged.
- **Risk/readiness:** H0 for a pure isolated/static lane, H1 if the selected
  design executes materialization even in temporary Git; `CURATION_READY`
  subject to the non-mutation gate; confidence medium-high.

### GP-PROV-006 — Census effective non-selector build configuration

- **Gap:** `GP-PROV-003` intentionally inventories selectors and excludes
  compiler/build-flag and other non-selector semantics. The effective
  `glyph_mk6` configuration also inherits literal build type, LDF mode,
  build/unbuild flags, board frequency/filesystem, archive, monitor/debug/
  upload settings, local environment flags, and unmatched-environment handling
  that have no deterministic current census.
- **Objective:** produce an ordered, exact-source census of effective literal
  non-selector build configuration for `glyph_mk6`, resolving only canonical
  inheritance/override order and classifying values without interpreting
  macro, protocol, device, or gameplay effect.
- **Implementation seam:** parse the literal non-selector assignments and
  canonical inheritance/override order in `platformio.ini` and
  `config/glyph/env.ini` into a deterministic fixture/checker/docs record, with
  mechanically consequent manifest/health values. Existing hook selector
  values may be source dependencies but their implementation is not census
  content.
- **Exclusions:** no setting change, compiler invocation, dependency install,
  build, semantic/reproducibility claim, device operation, or selector work
  already owned by `GP-PROV-003`/`004`.
- **Dependencies/gates:** activate after the `GP-PROV-004` source-
  correspondence repair establishes the checker pattern; invalidate on build-
  config source drift.
- **Validation:** exact effective-value/order correspondence, omitted and
  shadowed-setting adversarial cases, source dependency closure, stable
  ordering, and clean aggregate/framework checks.
- **Risk/readiness:** H0, `PREAUTHORIZABLE` only with the mechanical activation
  condition that `GP-PROV-004` is again truthfully `DONE` and both INI source
  blobs still match their curated identities; confidence high.

### GP-VAL-007 — Enforce validation-manifest dependency metadata

- **Gap:** validation health describes the manifest as a dependency graph, but
  the runner mainly requires field presence; it does not validate the contents
  of `source_dependencies` or `branch_policy`, and many current entries declare
  empty dependencies.
- **Objective:** define and enforce a bounded, truthful manifest metadata
  contract: normalized tracked repository paths, allowed branch-policy values,
  directly imported helper dependencies where mechanically discoverable, and
  explicit non-claims about complete semantic/transitive dependency closure.
- **Scope:** manifest schema/runner, deterministic dependency discovery,
  adversarial fixtures, docs, and consequent health/census values.
- **Exclusions:** no claim that static imports capture runtime data or all
  semantic dependencies; no arbitrary code execution, network, product source,
  or branch-policy change.
- **Dependencies/gates:** Curator must approve the exact bounded definition of
  a declared dependency before activation. Discovery must reject escaping,
  missing, untracked, symlinked-outside, duplicate, and non-normalized paths.
- **Validation:** field-content and direct-helper drift cases fail while
  explicitly documented dynamic/semantic exclusions remain truthful.
- **Risk/readiness:** H0, `PREAUTHORIZABLE` after the exact metadata contract is
  curated; confidence medium.

## Horizon C — research and externally gated survivors

### GP-VAL-008 — Research a current runtime-behavior regression lane

- **Gap:** current validation explicitly excludes the historical behavior
  evaluator. Historical behavior checkers fail against live source because
  their literal and routing anchors are stale, so they cannot silently become
  current coverage.
- **Objective:** identify a minimum current-source and directly accepted-
  evidence-backed behavior regression subset, with explicit provenance and
  historical/current separation.
- **Scope:** read-only source/evidence mapping and a proposed fixture/checker
  design; May-28 packets remain historical.
- **Exclusions:** no invented Ultimate semantics, Nunchuk claim, new expected
  output, table edit, routing decision, firmware behavior change, or hardware
  substitution.
- **Dependencies/gates:** any expected behavior not directly proved by current
  source/tests or explicit owner/domain authority returns to evidence or user
  decision; research may validly conclude no safe lane exists yet.
- **Validation:** provenance for every proposed case, deliberate stale-anchor
  rejection, historical fixture isolation, and zero product-source delta.
- **Risk/readiness:** H1 research, `EVIDENCE_GATED`; confidence medium on the
  gap and low on executable breadth.

### GP-ART-001 — Establish approved immutable firmware-artifact custody

- **Current state:** unchanged `USER_DECISION_GATED` survivor from the prior
  packet. No approved store, retention/custody policy, immutable locator, or
  readback authority exists.
- **Objective/value:** after an owner decision, define and prove one durable,
  content-addressed artifact custody path so future H2/H3 evidence can bind an
  exact candidate SHA to an exact retrievable artifact SHA-256 without treating
  a transient local path as acceptance evidence.
- **Current source evidence:**
  `docs/agent_framework/HARDWARE_EVIDENCE.md` requires durable exact-artifact
  identity and fail-closed handling; the current provenance surfaces record no
  established artifact store and no immutable locator.
- **Implementation seam:** repository custody contract, locator/retention
  schema, synthetic store/readback/tamper/expiry fixture and verifier, followed
  by separately authorized integration with the exact owner-selected store.
- **Activation:** project owner names the exact store, custody, retention,
  immutability, access, and expiry model; external permissions are verifiable.
- **Boundaries:** synthetic contract work may follow curation, but no real
  upload, build, candidate, device update, hardware PASS, or immutability claim
  is authorized by this packet.
- **Validation:** synthetic content-addressed write/readback/re-hash,
  wrong-content, mutable-locator, deletion/expiry, access-denial, and missing-
  custody cases; a separately authorized real-store proof is required before
  the store is described as established.
- **Risk/readiness:** H1/external state, `USER_DECISION_GATED`; confidence high
  on the gap and unknown on the store choice.

### GP-X1-001 — Realize a separately authorized behavior-changing X1 candidate

- **Current state:** unchanged `USER_DECISION_GATED` survivor. The accepted
  sole-`kX1Table` intake is deliberately baseline-equivalent `NO_OP`; no
  different X1 coordinates are authorized.
- **Objective/value:** once exact production-authorized replacement values
  exist, realize exactly one X1-table delta through the accepted source-owned
  path while preserving all other tables and enforcing the Revision-2
  build/artifact/hardware lifecycle before merge.
- **Current source evidence:** `GLYPH-UD-008` and `GLYPH-UD-009`, the canonical
  sole-`kX1Table` intake and its no-op classification, the current source-owned
  symbol map, and `HARDWARE_EVIDENCE.md`. None supplies different X1 bytes.
- **Implementation seam:** nine ordered raw-byte X1 points in a new accepted
  intake, the existing offline generator and active source-owned include,
  exact semantic/diff manifests, canonical `glyph_mk6` build, durable artifact
  binding, and candidate-specific physical protocol/result.
- **Activation:** a new canonical production-authorized intake supplies exact
  nine changed points, current baseline identity, sole-X1 ownership, and
  non-placeholder approval; immutable artifact custody and the exact hardware
  protocol must also be available.
- **Boundaries:** no second table, routing/modifier/game semantics, runtime-
  loaded config, persistence, device/protobuf write, flashing automation,
  Nunchuk claim, or inference from the no-op pilot. Any H3 candidate must build
  at an exact SHA and receive exact-artifact physical PASS before merge.
- **Validation:** exactly one changed table and directions 1–9 only; byte-range
  coordinates; the other 27 table digests unchanged; active diff limited to
  X1; canonical build; candidate/artifact SHA-256 binding; exact-snapshot
  physical PASS before merge.
- **Risk/readiness:** H3, `USER_DECISION_GATED`; confidence high on lifecycle
  and zero on future content.

## Current authorized work and dependency ordering

`GP-PROV-005` remains the sole canonical `READY` work order and is not
re-proposed here. Planner-estimated order for skeptical Curator review is:

1. repair/revalidate `GP-PROV-004`, because later provenance census work should
   not copy a false correspondence pattern;
2. repair `GP-CTL-001` and `GP-VAL-003`, optionally as one mechanically coupled
   docs/checker change while preserving both identities;
3. activate `GP-VAL-005` to make already passing advertised modes load-bearing;
4. evaluate the non-mutation design for `GP-VAL-006`;
5. evaluate `GP-PROV-006` after provenance repair and `GP-VAL-007` after its
   exact metadata definition is curated;
6. retain `GP-VAL-008`, `GP-ART-001`, and `GP-X1-001` behind their stated
   evidence or user-decision gates.

These are Planner estimates only. This packet does not alter the active queue,
declare work `READY` or `PREAUTHORIZED`, or override the Curator.

## Rejected, deferred, and duplicate alternatives

- Do not resurrect the official-configurator lane, capture requirement, or a
  renamed equivalent of retired `GP-CONFIG-002`.
- Do not create runtime-loaded config, WebSerial/device write, protobuf binary
  write, persistent runtime-config storage, flashing automation, or neutral-
  profile/vendor-export semantics.
- Do not repair the failed canonical-grid candidate without exact new source
  authority and the full H3 lifecycle; legacy hardware results do not transfer.
- Do not infer Nunchuk coverage or root cause; both remain unproved.
- Do not expand `GP-PROV-005` into postprocessor replacement, output
  equivalence, or custody; source/purpose lineage belongs to `GP-PROV-005`,
  immutable store/custody belongs to `GP-ART-001`.
- Defer exact postprocessor-target workflow hardening until source-backed CLI
  semantics support an output-equivalence-preserving change; ambient
  `ls *.uf2 | xargs` targeting is evidence for future H1 review, not current
  behavior-change authority.
- Defer binding an actual resolved build-input closure into artifact sidecars
  until trustworthy extraction semantics exist; do not imply pins,
  reproducibility, or accepted custody.
- Defer hardening `builder_scripts/arduino_pico.py` despite its global Git
  config mutation and unchecked Git reads until the exact version-string
  preservation/failure contract and build validation are curated as H1 work.
- Defer dependency/action pinning and reproducibility claims. A permitted live
  read-only probe resolved several abbreviated Git selectors, but one named
  external repository route was unavailable; partial lookup evidence cannot
  authorize input changes.
- Do not treat the remote X1 authority recovery/deletion branch as unfinished
  current work; the later canonical recovery and accepted no-op intake
  supersede that branch's state.

This packet is candidate supply only. It grants no implementation, queue,
firmware, table, workflow, build, external-storage, publication, device, or
hardware authority.
