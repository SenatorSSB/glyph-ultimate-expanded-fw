# Glyph Portfolio Candidate Packet — 2026-09-19 20:50 EEST

This is an immutable, non-authoritative Revision-2 Planner publication. It
records candidate supply for independent Work-Order Curator review only. It
does not authorize implementation, change the canonical queue, create a
`READY` or `PREAUTHORIZED` item, alter firmware behavior, or claim hardware
validation.

```yaml
packet_id: glyph-portfolio-20260919-2050
packet_state: FRESH
planning_branch: planning/portfolio-20260919-2050
base_configurator_sha: f1977c1104472d1e58733a14a11acf83fef3139b
base_live_verified: true
previous_planner_packet: glyph-portfolio-20260919-0320
previous_packet_commit: 1c15b71d2d1658c1fde13689f925c7372308dc1d
previous_packet_freshness: CONSUMED
candidate_count: 7
new_candidate_count: 6
materially_refined_candidate_count: 1
carried_forward_without_new_evidence_count: 0
current_ready_work_order_count: 0
curator_review_required: true
global_wait_proposed: false
material_events_since_publication: []
planner_authority: NON_AUTHORITATIVE_CANDIDATE_SUPPLY_ONLY
configurator_changed: false
```

## Audit basis and freshness

This audit is anchored at clean canonical
`f1977c1104472d1e58733a14a11acf83fef3139b`. An ordinary read-only fetch and
`ls-remote` initially verified local `configurator`, `origin/configurator`,
`FETCH_HEAD`, and live `refs/heads/configurator` at that SHA. Immediately before
branch creation, the restricted network path failed DNS resolution; the
permitted read-only network retry succeeded, again returned the exact live SHA,
and confirmed that `planning/portfolio-20260919-2050` did not exist remotely.

Packet `glyph-portfolio-20260919-0320` is `CONSUMED`. GP-PROV-009,
GP-VAL-016, GP-CONFIG-007, GP-VAL-017, and GP-PERSIST-002 were independently
authorized, implemented, reviewed, integrated, and published `DONE`. Its two
remaining reviewed dispositions, GP-VAL-018 and GP-PROV-010, were not retained
as Curator survivors. The canonical queue now records zero Ready, zero recorded
or mechanically activatable Preauthorized, zero invalidated Preauthorized,
zero hardware-pending, and effective authorized runway zero against target
four. GP-VAL-011 remains `REVIEW / OWNER_DEFERRED / NONEXECUTABLE`.

The shipped candidates materially consume the prior packet. The later direct
user governance correction GLYPH-UD-016 is also material to role routing: a
completed Curator must consume its obligation and cannot publish a
`CURATION_REQUIRED` self-loop. It does not authorize filler or execution of a
gated candidate. No active firmware table byte, runtime publication path,
hardware result, protocol command, or game-semantic authority changed.

## Broad audit scope and result

The broad audit covered current runtime/source architecture; active table
ownership and source sync; coordinate-native validation, dry-run, bridge, and
source-owned generator seams; source-authority intake; SetConfig/GetConfig and
persistence boundaries; current host/operator tooling; build-input identity,
artifact custody and correspondence; workflow validation/publication gates;
checker census, manifest and health; current hardware evidence; recent Git
history, packet consumption and curation; and current user direction.

Current focused evidence remained green: framework/navigation/agent-surface,
checker census (201 entries), validation health (40 manifest entries), source
sync (28 tables and semantic digest
`b0082f068e0e552d479ec8ed8bf5867737a75a19e5e60aede55bafb72b883874`),
generator modes, coordinate-native offline pipeline, persistence research,
artifact workflow, publication workflow, pre-build identity, artifact custody,
source-authority intake, compiled SetConfig transaction cases (9), and the
GP-CONFIG-007 host-stage schema. The full aggregate remains unavailable in
preflight on the known ignored
`.pio/libdeps/glyph_mk6/Adafruit_TinyUSB_XInput/` directory. That is the
owner-deferred GP-VAL-011 state and is neither reopened nor reclassified here.

The audit found independent counterexamples outside the gated H2 lane:

- three materially different valid coordinate-native profiles hash to the
  same emitted source-owned layout packet because `convert_profile()` returns
  a fixed fixture after validation;
- current artifact workflow checkers accept success-masked checkout,
  postprocessor, sidecar-write, and sidecar-verification commands;
- an ignored file under `src/` can produce a clean builder identity and pass
  artifact-custody checkout validation;
- successful SetConfig replaces `_config`, while activation masks, selected
  mode state, custom-mode derived masks, and display/LED bindings have separate
  cached or reselection boundaries that current transaction tests do not
  characterize;
- production source-authority intake accepts nonexistent approval, ownership,
  and source-reference strings as evidence;
- checker relevance classification does not recognize new CI/build/artifact/
  hardware-evidence safety checkers unless they also happen to use the current
  runtime-config vocabulary; and
- a completed local build exposes deterministic observational metadata through
  PlatformIO, `.piopm`, package manifests and Git package identities, but the
  current provenance record deliberately does not characterize that resolved
  state.

Candidate supply therefore exists outside missing modifier/layout data, and a
portfolio-global evidence wait is not supportable.

## Dominant bottleneck

The dominant engineering bottleneck is exact correspondence: several current
tools and gates validate that an object is well formed without proving that it
corresponds to the input, source bytes, command exit status, authority locator,
or live runtime state claimed. The dominant product bottleneck remains
separate: the next H2 source-owned modifier/layout realization still requires
owner/Senscope values, ownership, routing intent, and expected outputs.

## Horizon A — immediate correspondence and safety candidates

### GP-SRC-007 — Fail closed on non-corresponding coordinate-native conversion

- **Objective/value:** Prevent a validated coordinate-native profile from being
  presented as converted source-owned layout data when the output is actually a
  fixed unrelated fixture. This protects review and candidate preparation from
  false input/output provenance.
- **Current source evidence:**
  `tools/convert_coordinate_native_profile_to_source_owned_spec.py::convert_profile`
  calls `validate_bridge_profile()` and unconditionally returns
  `EXPECTED_LAYOUT_SPEC`. The current checker positively requires the minimal,
  9-way modifier, and bridge profiles—whose coordinates, table counts and
  routing differ—to emit the same output. Direct execution produced the same
  SHA-256 `069a956e9d0f1701bc1126f4c8b41f481fabd82dae80fba589dfaa0ee69f5ef7`
  for all three outputs. Candidate preparation calls this seam as profile
  conversion.
- **Implementation seam:** Make the bridge prove explicit profile-to-layout
  correspondence or reject the input. The smallest safe repair may accept only
  an exact explicitly mapped bridge subset and must reject generic profiles
  until source-owned table ownership and mapping are explicit.
- **Scope/exclusions:** Converter, bridge checker/fixtures, directly affected
  offline pipeline docs, and deterministic manifest/census/health consequences
  only. No tie/conflict-policy choice, new table values, inferred mapping,
  routing semantics, active source mutation, runtime loading, persistence,
  device write, or flashing.
- **Dependencies:** Preserve the 28-table source-owned authority and existing
  generator full/overlay/reject policy. A general data-derived converter is a
  later substantive dependency, not part of this fail-closed repair.
- **Validation:** Distinct coordinate/table/routing inputs must produce
  corresponding distinct explicitly authorized data or fail; unsupported
  profiles cannot emit the fixed layout; exact supported mapping, negative
  corpus, generator modes, candidate-generation checks, manifest/census/health,
  navigation and independent review pass.
- **Risk/gates/confidence/readiness:** H1 offline tooling/source-authority
  integrity; no build or hardware; high confidence; `CURATION_READY` for the
  fail-closed correction.

### GP-VAL-019 — Make artifact provenance commands failure-bearing

- **Objective/value:** Ensure checkout identity, postprocessing, sidecar write,
  and sidecar verification commands must themselves succeed before artifact
  publication; substring/order presence is not sufficient.
- **Current source evidence:** Both
  `check_glyph_artifact_postprocessor_workflow.py` and the shared publication
  workflow validator pass the current workflow. In-memory mutations appending
  `|| true` independently to `--verify-checkout`, the `glyph_nuker` command,
  `--write-sidecar`, and `--verify-sidecar` were accepted by both validators.
  GP-VAL-016 hardened only the aggregate validation command and does not cover
  these downstream artifact commands.
- **Implementation seam:** Enforce exact supported single-command lines, order,
  and step fields for the four artifact-provenance operations, using a bounded
  shared exact-command helper only if useful.
- **Scope/exclusions:** Artifact workflow checker, bounded workflow parser
  helper, focused fixture/adversarial cases, and deterministic manifest/census/
  health consequences. No workflow YAML or command changes, runner changes,
  artifact/postprocessor bytes, publication or custody policy, build inputs,
  firmware, device, hardware, or GP-VAL-011.
- **Dependencies:** The exact current `.github/workflows/build.yml` command
  lines and default shell contract remain unchanged.
- **Validation:** Reject `|| true`, `; true`, trailing `exit 0`, subshell,
  function, heredoc, line-continuation, comment disguise, conditional and
  duplicate variants for every protected command; accept the exact current
  workflow; run both workflow checkers, manifest/census/health, framework,
  navigation/surface, syntax and independent review.
- **Risk/gates/confidence/readiness:** H0 checker-only; no build or hardware;
  high confidence; `CURATION_READY`.

### GP-PROV-011 — Reject ignored files under source-critical roots

- **Objective/value:** Prevent repository source or build-control files hidden
  by ignore rules from receiving a clean firmware identity or passing artifact
  custody.
- **Current source evidence:** In an isolated repository, adding
  `.git/info/exclude` entry `src/ghost.cpp` and creating that ignored file left
  `git status --porcelain --untracked-files=all` empty, made the builder return
  a clean short SHA, and made `require_clean_candidate_checkout()` accept the
  checkout. GP-PROV-009 directly verifies tracked entries but deliberately
  preserved ignored-only semantics. Hardware correspondence already supplies a
  finite critical-path classification precedent.
- **Implementation seam:** Add a finite ignored-critical-path check to the
  shared worktree-integrity seam and consume it from builder identity and
  custody. Reject ignored paths only under reviewed source/build-control roots.
- **Scope/exclusions:** Builder/custody integrity helper and focused isolated
  checkers; finite roots such as `src`, `include`, `HAL`, `config`, `lib`,
  build declarations/hooks and directly reviewed scripts. Preserve allowed
  ignored caches and custody roots such as `.pio`, `.platformio-home`, and
  `local_backups`. No complete ignored-tree fingerprinting, nested-repository
  traversal, timeout/concurrency redesign, dependency-resolution claim,
  artifact policy, firmware behavior, device or hardware action.
- **Dependencies:** Preserve GP-BUILD-001, GP-PROV-009, GP-ART-001 and
  GP-VAL-015 semantics; do not reopen GP-VAL-011.
- **Validation:** `.gitignore`, `.git/info/exclude`, and isolated global-exclude
  files under every critical root reject; allowed cache/custody paths pass;
  existing tracked, staged, unstaged, untracked, hidden-tracked and symlink
  cases retain their outcomes; run pre-build/custody/provenance gates,
  canonical `pio run -e glyph_mk6`, and independent review.
- **Risk/gates/confidence/readiness:** H1 source-to-artifact integrity; build
  required because the build hook changes; no hardware if clean-source output
  and runtime behavior remain unchanged; high confidence; `CURATION_READY`.

## Horizon B — runtime and authority characterization

### GP-CONFIG-008 — Characterize successful SetConfig runtime rebinding

- **Objective/value:** Establish what a successful persisted SetConfig changes
  immediately in live behavior and what remains cached, reselection-dependent,
  or reboot-dependent before any corrective architecture is chosen.
- **Current source evidence:**
  `ConfiguratorBackend.cpp::HandleSetConfig()` completes success with
  `_config = candidate`. `mode_selection.cpp` separately caches activation
  masks, suppresses same-index reconfiguration through `current_mode_index`,
  mode implementations cache derived custom masks/pointers, and the display/
  LED path rebinds only when the current mode object pointer changes. Current
  persistence research explicitly does not claim successful-update runtime
  rebinding, while the compiled SetConfig checker covers only the handler's
  nine transaction outcomes.
- **Implementation seam:** Add exact-source host characterization and a bounded
  research contract separating in-place Config visibility from activation-mask,
  same-index mode, custom-mode, display/LED, backend, reselection and reboot
  boundaries.
- **Scope/exclusions:** Host/source characterization, docs/fixture/checker and
  manifest/census/health only. No firmware fix, automatic reboot, protocol
  success reinterpretation, backend reinitialization, persistence mechanism,
  game-semantic decision, device access, or hardware claim.
- **Dependencies:** Bind exact production bodies and preserve GP-CONFIG-005
  live-RAM rejection semantics and GP-PERSIST-002 raw-read characterization.
  Any desired runtime repair is separate H2/H3 work.
- **Validation:** Source-corresponding cases cover activation-binding changes,
  same-index mode changes, custom modifier/combo masks, referenced custom-mode
  indices, RGB/display bindings, reselection and reboot boundaries; source
  drift, manifest/census/health, framework/navigation and independent review.
- **Risk/gates/confidence/readiness:** H1 characterization only; no build or
  hardware; high gap confidence and medium harness-shape confidence;
  `CURATION_READY`.

### GP-SRC-008 — Resolve source-authority intake evidence locators

- **Objective/value:** Prevent arbitrary nonempty strings from being treated as
  approval, ownership, or replacement source evidence in a production-authorized
  generator intake.
- **Current source evidence:** `review_intake()` checks
  `approval_reference`, ownership `authorization_reference`, and replacement
  `source_reference` only for non-placeholder text; `emit_generator_input()`
  carries the unverified approval string into emitted metadata. An in-memory
  current-baseline X1 packet with nonexistent approval, ownership and source
  references returned no blockers and `production_emission_allowed=true`. The
  checker uses arbitrary positive strings and asserts the canonical X1 text
  without resolving the `GLYPH-UD-010` anchor.
- **Implementation seam:** Resolve the current canonical repo-local locator
  forms and their claimed anchor/path/symbol correspondence in the intake
  validator and checker. Unsupported locator forms fail closed rather than
  being inferred.
- **Scope/exclusions:** Intake validator/checker, exact current production
  intake corpus, locator contract docs, and deterministic manifest/census/
  health consequences. Do not infer approval meaning, create human authority,
  broaden ownership, alter coordinates, or reinterpret historical packets.
- **Dependencies:** Limit the first slice to current canonical
  `USER_DIRECTION.md#glyph-ud-*` anchors and reviewed repo source path/symbol
  locators. Any external evidence locator needs separate authority design.
- **Validation:** Missing/ambiguous/duplicate anchor, escaping or absent path,
  missing symbol, mismatched ownership/reference, unsupported form, stale
  baseline and fake source references fail; exact current X1 references pass;
  intake/generator/manifest/census/health and independent review pass.
- **Risk/gates/confidence/readiness:** H1 source-authority integrity; no build
  or hardware; high confidence; `CURATION_READY` for current canonical locator
  forms.

### GP-VAL-020 — Classify build, CI, artifact and hardware safety checkers

- **Objective/value:** Keep newly added safety checkers from silently falling
  outside both the current validation manifest and explicit exclusion set when
  they do not use runtime-config vocabulary.
- **Current source evidence:**
  `generate_glyph_checker_census.py::STRONG_RELEVANCE` recognizes current
  runtime/source-owned/profile concepts. A synthetic
  `check_glyph_ci_publication_safety.py` that referenced
  `.github/workflows/build.yml` received no relevance signal; after ordinary
  census regeneration the runner would not require manifest or exclusion
  classification. Existing CI/build/artifact/hardware-evidence checkers are
  already classified, so the gap concerns future classification, not current
  checker execution.
- **Implementation seam:** Add a closed, source-backed strong-signal vocabulary
  for CI workflows, build declarations/hooks, artifact custody/provenance, and
  hardware-evidence control-plane references, plus aggregate adversarial cases.
- **Scope/exclusions:** Static census generator, aggregate adversarial checker,
  deterministic census/health docs, and no other checker semantics. Do not
  execute every discovered checker, claim semantic/transitive completeness,
  change commands/workflows/build inputs/artifacts, or reopen GP-VAL-011.
- **Dependencies:** The closed signal inventory must classify all current hits
  without forcing unrelated general-purpose checkers into this manifest.
- **Validation:** One synthetic unclassified checker per signal fails;
  manifest/exclusion classification passes; unrelated census-only checker stays
  allowed; census, manifest, health, aggregate adversarial, framework and
  independent review pass.
- **Risk/gates/confidence/readiness:** H0 validation-control-plane hardening;
  no build or hardware; high gap confidence; `CURATION_READY`.

## Horizon C — resolved-build observation research

### GP-PROV-010 — Define completed-build resolved-input observation

- **Objective/value:** Record deterministic observational identities for the
  actual locally resolved toolchain, platform, framework and libraries used by
  a completed canonical build, without changing selectors or claiming
  reproducibility.
- **Current source evidence:** The existing provenance inventory and 44-record
  resolution packet explicitly do not inspect caches or complete resolution.
  On the audited completed build, `pio pkg list -e glyph_mk6` reported the
  resolved platform, framework, tools and libraries; `pio system info
  --json-output` reported PlatformIO Core 6.1.19, Python 3.14.4 and host type;
  package `.piopm`/manifest files expose versions and declared URIs; Git-backed
  platform/framework/library trees expose full HEAD and worktree status. The
  observation also showed why version strings alone are insufficient: some
  Git package trees contain untracked material while retaining the same
  reported package version.
- **Implementation seam:** Define a read-only schema/checker and capture tool
  for an already completed isolated build. Bind source SHA and exact build
  invocation; classify CLI values, `.piopm`/manifest values, Git HEAD/status,
  and bounded content identities as observational; reject absent, ambiguous,
  dirty, symlinked, duplicated, or selector-inconsistent records.
- **Scope/exclusions:** Docs/tool/fixture/checker research only. No build,
  network fetch, install/update, selector/pin/action/runner change, CI sidecar,
  artifact acceptance, reproducibility claim, firmware, device or hardware
  action. CI integration, if useful, is a later candidate.
- **Dependencies:** Preserve GP-PROV-003/004/006/008 declaration authority and
  GP-PROV-009 clean tracked-source semantics. Cache metadata remains observed
  state, never source authority by itself.
- **Validation:** Synthetic complete, missing, ambiguous, duplicate, dirty Git,
  mutable registry, selector mismatch, symlink and path-escape records;
  deterministic normalized output; provenance/manifest/census/health and
  independent review.
- **Risk/gates/confidence/readiness:** H0 read-only provenance research; no
  build or hardware in the first slice; medium-high confidence;
  `CURATION_READY` for bounded completed-build observation only. This materially
  refines the prior `RESEARCH_GATED` candidate with current interface evidence.

## User-direction and evidence effects

- GLYPH-UD-001/005 preserve the Glyph/Senscope split and prevent inferred
  layout or gameplay semantics. GP-SRC-007 fails closed instead of inventing a
  coordinate-to-table mapping.
- GLYPH-UD-013 keeps GP-VAL-011 owner-deferred and nonexecutable. GP-PROV-011
  uses finite critical-root ignored-file detection and does not expand the
  deferred complete aggregate-proof contract.
- GLYPH-UD-015 motivates exact source/build/artifact correspondence but does
  not authorize external storage, release, upload, flashing or device write.
- GLYPH-UD-016 routes completed curation with zero effective runway back to
  planning rather than a Curator self-loop; this packet creates a new, separate
  review obligation only through its immutable Planner publication.
- GP-VAL-018 remains `USER_DECISION_GATED` on owner/Senscope selection of the
  exact tie/conflict policy. No new direction arrived, so it is preserved as
  prior evidence and is not republished as fresh candidate supply.
- GP-CONFIG-005 and GP-X1-001 exact hardware PASS records remain unchanged.
  No candidate here creates or reinterprets hardware evidence. Nunchuk remains
  `NOT_TESTED`; root cause remains unproven.

## Rejected and held alternatives

| Alternative | Disposition | Evidence-based reason |
| --- | --- | --- |
| Reopen GP-VAL-011 | REJECT | GLYPH-UD-013 makes the aggregate isolation repair owner-deferred and nonexecutable. |
| Repeat any 0320 candidate | REJECT | GP-PROV-009, GP-VAL-016, GP-CONFIG-007, GP-VAL-017 and GP-PERSIST-002 are DONE with strict correspondence. |
| Re-adjudicate GP-VAL-018 without new direction | HOLD | The owner/Senscope tie/conflict choice is unchanged and already reviewed; implementation behavior is not semantic authority. |
| General coordinate-native to 28-table mapping | SUBSTANTIVE_DEPENDENCY_GATED | No explicit role/table/routing-to-source-symbol mapping or ownership exists; GP-SRC-007 only fails closed. |
| SetConfig runtime-rebinding firmware repair | SUBSTANTIVE_DEPENDENCY_GATED | GP-CONFIG-008 first characterizes exact boundaries; desired live reinitialization/reboot behavior and H2/H3 acceptance remain unresolved. |
| Change CMD_GET_CONFIG firmware behavior | SUBSTANTIVE_DEPENDENCY_GATED | GP-PERSIST-002 characterized current behavior; protocol/error-framing and desired failure outcomes remain unresolved. |
| Implement atomic persistence/recovery | REJECT | GP-PERSIST-001 records unresolved H3 recovery, fault-model and compatibility decisions. |
| Pin every dependency or claim reproducible builds | REJECT | GP-PROV-010 is observation only; selector/security/update policy and complete reproducibility authority remain unresolved. |
| Repair or expand current protobuf device-write tooling | HOLD | Device-write changes require separate explicit authority and are not needed for this portfolio's offline/source/validation candidates. |
| Recover old planning, curation, result or failed branches | REJECT | They are consumed, integrated, superseded, historical evidence, or non-authoritative; branch names do not create work. |
| Revive official configurator or external-remapper work | REJECT | GLYPH-UD-007 retires official compatibility and external-remapper evidence remains quarantined. |
| Propose portfolio-global evidence wait | REJECT | Seven bounded independent candidates exist outside the owner-gated H2 lane. |

## Global wait assessment

`global_wait_proposed: false`. The broad audit found independent source,
validation, provenance, authority and runtime-characterization work that does
not depend on owner/Senscope modifier data or new physical testing. The event
that would resume the H2 product lane remains owner/Senscope delivery of exact
layout values, owned table/routing scope and expected outputs, but that
candidate-local gate does not establish portfolio-global scarcity.

## Curator handoff

The Work-Order Curator must independently review this exact packet as:

- packet ID `glyph-portfolio-20260919-2050`;
- planning branch `planning/portfolio-20260919-2050`;
- base `f1977c1104472d1e58733a14a11acf83fef3139b`;
- exact immutable planning commit supplied by the published branch;
- initial inventory of exactly seven candidate headings;
- `curator_review_required: true`;
- `global_wait_proposed: false`.

No ranking or readiness estimate above is authorization. The Curator must
independently verify every candidate, choose dispositions, create the required
immutable receipt, and adopt any queue state through the separate canonical
curation sequence.
