# Glyph Portfolio Candidate Packet — 2026-09-21 22:32 EEST

This immutable Revision-2 packet is non-authoritative candidate supply for an
independent Work-Order Curator. It authorizes no implementation, queue change,
firmware behavior, device operation, integration retry, or hardware judgment.

```yaml
packet_id: glyph-portfolio-20260921-2232
packet_state: FRESH
planning_branch: planning/portfolio-20260921-2232
base_configurator_sha: 47813ecef8d98fc489a207b16e365c4b8e71f2c1
base_live_verified: true
previous_planner_packet: glyph-portfolio-20260921-1252
previous_packet_commit: 525296975bcd6791e8e3a57945b2c47e0fc16ef0
previous_packet_freshness: CONSUMED
candidate_count: 1
curator_review_required: true
global_wait_proposed: false
material_events_since_publication: GP-X1-002 obtained exact-snapshot hardware PASS; its current-canonical integration attempt preserved the tested firmware inputs but exposed missing GP-X1-002 exact paths in the finite GP-VAL-015 NON_BEHAVIORAL inventory.
planner_authority: NON_AUTHORITATIVE_CANDIDATE_SUPPLY_ONLY
configurator_changed: false
```

## Basis, freshness, and limits

Live `origin/configurator` was fetched and verified at
`47813ecef8d98fc489a207b16e365c4b8e71f2c1`; this planning commit must be its
direct child. Live refs also verified candidate
`f657715b26d26587a931074ce7dd12c698785290` and the diagnostic integration
attempt `5a47d0b23a8daceb60a26ed5ff547cb413d45589`. The candidate's direct parent is
`34a6c1bc9a9e13dcdf412c8b56a5c53011383503`, its tree is
`972e1a2ef0cdf41a9d3b6f6204ef1cc0c4dd3fa6`, and it is an ancestor of the
diagnostic integration attempt. That attempt remains blocked and is not an
implementation base or substitute candidate.

The immutable Revision-2 PASS record is
`git-json:66f1d25622a79e7ac4298b7558c6013410eabc07:docs/calibration/fixtures/gp_x1_002_hardware_evidence_2026-09-21.json`.
It binds the tested candidate to preserved UF2 SHA-256
`00dc75b65a080830131c3c260558d4daeff45bb528e43d196b8ad94fcd06f451`.
This packet neither rejudges that evidence nor expands its physical claims.

Inspected authority includes `AGENTS.md`, current state/roadmap/workflow,
runtime implementation boundary, framework README, authorization/runway,
subagent contracts, work-order template, current queue, GP-VAL-015 source and
contract, its real-Git adversarial tests, the complete GP-X1-002 candidate
delta, current PlatformIO source filters/build inputs, the immutable hardware
evidence, and the failed correspondence reproduction. No Senscope source or
game semantics are involved.

The failure reproduces as an unclassified-path rejection at
`docs/agent_framework/GP_X1_002_HARDWARE_PROTOCOL.md`. GP-VAL-015 deliberately
classifies both the complete tested-candidate delta and the later
candidate-to-target delta. The candidate changes fourteen paths: the active
`GeneratedRuntimeConfigBaseline.current.hpp` remains `CRITICAL`; four
validation fixtures/docs are already `NON_BEHAVIORAL`; nine exact host-only
paths are unclassified. The candidate-to-integration delta adds one further
unclassified exact hardware-evidence path. No blanket prefix or extension rule
is needed or allowed.

## GP-VAL-027 — extend GP-VAL-015 for exact GP-X1-002 host paths

- Readiness estimate: `CURATION_READY`. Risk H1. The substantive safety model
  already exists in completed GP-VAL-015; this is a finite, dependency-audited
  inventory extension plus tests and contract documentation.
- Objective: allow GP-VAL-015 correspondence to classify the exact
  GP-X1-002 host/governance paths needed for the tested candidate and immutable
  PASS publication while retaining exact critical firmware/build-input
  correspondence and fail-closed rejection of every unknown path.
- Why this matters: the current classifier cannot evaluate the physically
  accepted candidate because its finite inventory predates GP-X1-002. The
  integration attempt is blocked by classifier coverage, while the tested
  active table header and other critical firmware/build inputs otherwise retain
  exact candidate correspondence.
- Behavioral claim: add only the ten exact paths listed below to
  `NON_BEHAVIORAL_PATHS`, with critical classification still taking precedence.
  These paths remain subject to their normal governance, evidence, census,
  manifest, intake, source-sync, health, and candidate validators. Unknown or
  unsafe paths still fail closed. No firmware/runtime/build input, tested
  candidate, artifact, protocol content, hardware record content, or device
  behavior changes.
- Exact inventory additions:
  - `docs/agent_framework/GP_X1_002_HARDWARE_PROTOCOL.md`
  - `docs/agent_framework/SUBAGENT_CONTRACTS.md`
  - `docs/calibration/fixtures/gp_x1_002_hardware_evidence_2026-09-21.json`
  - `docs/runtime_config/intakes/x1_normal_restoration_overlay_hardware_candidate.intake.json`
  - `docs/runtime_config/source_authority_intake_workflow.md`
  - `tools/check_glyph_gp_x1_002_candidate.py`
  - `tools/check_glyph_runtime_config_source_sync.py`
  - `tools/check_glyph_runtime_config_validation_health.py`
  - `tools/check_glyph_source_owned_source_authority_intake.py`
  - `tools/source_owned_source_authority_intake.py`
- Dependency rationale: `platformio.ini` builds `src/`, `HAL/pico/src`, and
  `config/glyph` sources with declared include, library, build-hook, and
  dependency controls. None of the ten paths is selected by those filters,
  included by compiled firmware, or executed by the firmware build. The
  protocol, subagent contract, workflow prose, intake, and evidence record are
  host documentation/data. The five `tools/` paths are offline candidate,
  source-authority, source-sync, or validation programs. They may govern and
  validate source generation, but they are not inputs to the already-built
  tested artifact. Their correctness remains independently enforced by normal
  validators. The tested active header
  `src/modes/runtime_config/generated_source_owned/GeneratedRuntimeConfigBaseline.current.hpp`
  remains critical because it supplies compiled table bytes.
- Scope: update only `tools/glyph_hardware_correspondence.py`, its focused
  real-Git adversarial tests, `docs/agent_framework/HARDWARE_CORRESPONDENCE.md`,
  and deterministic direct consequences in checker census/manifest/health
  metadata. Queue/status mirrors may change only through independent curation
  and later completion publication. A narrowly necessary agent-surface test may
  be updated if it directly binds the exact inventory contract.
- Explicit excluded scope: no edit to firmware, active tables, HAL, config,
  platform/build/dependency inputs, GP-X1-002 candidate, protocol, authority
  intake, evidence content, UF2, artifact custody, physical PASS, runtime-loaded
  configuration, device write/flashing, persistence, Nunchuk, game semantics,
  Senscope binding, GP-VAL-011, or GP-CONFIG-010. Do not edit, amend, reuse, or
  publish the diagnostic integration branch under this order.
- Touched planes: docs/checkers only.
- Source authority: completed GP-VAL-015 finite exact-path model and dependency
  contract; exact GP-X1-002 candidate and candidate-to-integration deltas;
  current PlatformIO build filters and dependency declarations; immutable
  GP-X1-002 hardware PASS evidence; reproduced current-classifier failure.
- Dependencies/prerequisites: live canonical remains the exact planning base;
  GP-X1-002 remains `HARDWARE_VALIDATED` for the pinned candidate/artifact/PASS
  record; the ten paths retain the dependency properties above; independent
  Curator confirms a complete H1 order before implementation.
- Substantive authorization rationale: the owner directed completion of the
  accepted X1 restoration lifecycle, and GP-VAL-015 already requires reviewed
  finite inventory growth when later host paths arise. The observed failure and
  dependency graph resolve the bounded classifier decision without a firmware,
  product, or hardware judgment.
- Automated validation:
  1. Add real-Git tests showing an exact GP-X1-002 candidate-style delta and
     later exact evidence path classify as `NON_BEHAVIORAL`, while its active
     generated table stays `CRITICAL` and must remain byte/mode exact.
  2. Test each of the ten exact inventory entries and reject adjacent/lookalike
     names, prefix/extension tricks, case variants, traversal/ambiguous paths,
     executable metadata, symlinks, gitlinks, and unknown additions.
  3. Retain and run the full GP-VAL-015 adversarial suite, including later
     critical drift, candidate-independent critical drift, dirty critical
     inputs, unsupported modes, direct-parent and ancestry checks.
  4. Reproduce `verify_correspondence(..., integrated=True)` for exact candidate
     `f657715b26d26587a931074ce7dd12c698785290`, tested base
     `34a6c1bc9a9e13dcdf412c8b56a5c53011383503`, and diagnostic integration
     `5a47d0b23a8daceb60a26ed5ff547cb413d45589`; require correspondence PASS only
     after all ordinary affected validators pass. The diagnostic branch stays
     noncanonical and nonpublishable.
  5. Run framework, sequence, navigation/context, agent surface, source-sync,
     source-authority intake, candidate, census, manifest/health, Python syntax,
     `git diff --check`, exact changed-path, and clean-state gates. Run a fresh
     independent implementation review before publication.
- Canonical build: `NOT_REQUIRED` for this H1 host classifier repair. Any
  firmware/build-input delta stops. A later GP-X1-002 integration supervisor
  must separately rerun its required build and validation gates without
  treating that rebuild as the physically accepted artifact.
- Expected artifact: `NOT_APPLICABLE`.
- Manual acceptance: `NOT_REQUIRED`.
- Rollback/recovery: keep the classifier repair unmerged if any inventory entry
  lacks the stated dependency proof, an unknown path needs exemption, critical
  precedence weakens, or any focused/ordinary validator or independent review
  fails. Preserve the tested artifact and PASS evidence unchanged. Return
  expanded scope to Curator.
- Status/documentation updates: independent Curator may adopt GP-VAL-027 as a
  complete H1 `READY` work order and synchronize runway/status mirrors. Later
  implementation and strict DONE publication remain separate. GP-X1-002 stays
  `HARDWARE_VALIDATED` until a fresh post-repair Implementation Supervisor
  starts from then-current canonical and completes exact-candidate integration.
- Stop conditions: any runtime/build input or evidence content would change;
  classification requires a directory/pattern exemption; normal validators
  would be skipped; correspondence cannot reject critical drift; GP-CONFIG-010
  would change; or the pinned candidate/artifact/PASS identity differs.

## Rejected alternatives

Rejected: blanket trust for `docs/`, `tools/`, JSON, Markdown, checker names, or
GP-X1 prefixes; dynamically trusting paths from a changed manifest; treating all
candidate paths as harmless; removing candidate-path classification; moving the
active generated header out of the critical set; special-casing a branch or SHA;
editing the candidate/protocol/evidence to fit the existing inventory; bypassing
GP-VAL-015; or declaring the diagnostic integration valid without the repaired
model and ordinary validators.

## Publication and next action

Publish only this packet on `planning/portfolio-20260921-2232` as a direct child
of the verified canonical base. A fresh independent Work-Order Curator must
verify the immutable packet, live refs, exact deltas, ten dependency
classifications, validation plan, exclusions, and GP-X1-002 identity. If
accepted, the Curator should publish the required immutable receipt and later
canonical adoption of one complete H1 `READY` GP-VAL-027 order. This planning
branch must never be merged as implementation.
