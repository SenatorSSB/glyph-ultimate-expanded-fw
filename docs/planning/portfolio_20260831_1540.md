# Glyph Portfolio Candidate Packet — 2026-08-31 15:40 EEST

```yaml
packet_id: glyph-portfolio-20260831-1540
packet_state: FRESH
planning_branch: planning/portfolio-20260831-1540
base_configurator_sha: d94eb6d629f9e8e73e893971a3f47c4485cf17ee
base_live_verified: true
previous_planner_packet: glyph-portfolio-20260827-1210
previous_packet_branch: planning/portfolio-20260827-1210
previous_packet_commit: ae1d15b9a7941934b26d4371b0ea0e10691629cb
previous_packet_base: 8c04262c66613d46b933b1b739c01c575cb0c580
previous_packet_freshness: MATERIALLY_CONSUMED
candidate_count: 10
repair_reactivation_count: 4
new_candidate_count: 3
carried_forward_candidate_count: 3
current_ready_work_order_count: 0
curator_review_required: true
global_wait_proposed: false
planner_authority: NON_AUTHORITATIVE_CANDIDATE_SUPPLY_ONLY
configurator_changed: false
```

## Result, freshness, and portfolio state

This is a material broad-audit refresh of live `configurator`, not a timer-only
or SHA-drift packet. The ordinary read-only fetch live-verified
`refs/heads/configurator` at
`d94eb6d629f9e8e73e893971a3f47c4485cf17ee`; this branch starts from that exact
commit.

The previous packet is materially consumed. Its seven actionable repair and
hardening candidates are now `DONE`, including its final actionable survivor,
`GP-VAL-006`. The canonical queue now reports zero `READY`, zero recorded or
mechanically activatable `PREAUTHORIZED`, zero invalidated Preauthorization,
zero hardware-pending work, and effective authorized runway zero against target
four. Its primary state is `PLANNING_REQUIRED`, and it explicitly requests a
fresh broad Planner audit.

Three prior non-executable survivors remain gated, but they do not establish
portfolio-global scarcity. This audit found seven independent H0/H1
correspondence, validation-safety, and build-provenance candidates outside
those gates.

The dominant bottleneck is **fail-open correspondence and trusted execution**:
current control-plane prose, workflow census, active-source classification,
health metadata, manifest command declarations, aggregate execution, and
documented local build entrypoints can disagree with the source or machine
state while all current checks still pass.

`GLOBAL_EVIDENCE_WAIT_SUPPORTED` is not proposed. Fresh independent work exists
without game-semantic, firmware-behavior, hardware, artifact-store, or user
product decisions.

## Broad-audit evidence

The audit read the current agent/context/state/roadmap/workflow/boundary and
framework authority documents, the complete active queue, current user
direction, the latest packet and its Curator dispositions, current and
historical hardware evidence, runtime source and active publication surfaces,
generated/source consistency, source-authority intake, checker census,
validation manifest and health record, aggregate runner, fixtures, tracked CI
workflows, PlatformIO configuration, local build wrappers, postprocessor
provenance and lineage, repository history, worktrees, and live remote refs.

Observed current facts on the exact base:

- the full runtime-config aggregate passes with checker census `194`, manifest
  entries `32`, and current load-bearing checks `28`;
- `tools/check_glyph_agent_framework_docs.py`, checker census, validation
  health, aggregate adversarial tests, candidate-generation safety, framework,
  navigation, and agent-surface checks pass;
- the queue's guarded machine summaries correctly report zero runway and
  `PLANNING_REQUIRED`, but three current documents retain contradictory
  unguarded Ready/runway prose;
- Git tracks three workflow files, while the current publication-route census
  discovers only the two top-level `.github/workflows` files;
- the active generated baseline manifest row still calls the included
  compile-time table-content header an inert baseline;
- the health fixture reports an exclusion count of 37 while retaining an
  unchecked empty duplicate `strong_signal_exclusions` list;
- the aggregate accepts underconstrained command vectors and trusts
  `mutation_risk` while executing every current checker in canonical `ROOT`
  without a timeout or repository-immutability proof;
- the canonical local fallback build wrappers are prescribed by `AGENTS.md`
  and `docs/WORKFLOW.md` but are outside the declared build-input provenance
  inventory;
- no product source, workflow, build input, user direction, source-authority
  intake, hardware record, or active table byte changed since the prior packet
  base; changes were control-plane, checker, and host-tool completions;
- the canonical-grid candidate remains `HARDWARE_FAIL`; the accepted Y2 and
  Alternative-B results remain legacy evidence for their exact prior
  mechanisms only; Nunchuk remains `NOT_TESTED` and root cause remains
  unproven;
- runtime-loaded config, persistence, WebSerial/device write, protobuf binary
  write, backend config write, and flashing automation remain unimplemented
  and unauthorized.

No product file, active firmware source, workflow, queue, hardware record, or
canonical document was changed by the Planner. This branch adds this packet
only.

## Horizon A — exact current-truth correspondence

### GP-CTL-001 repair/revalidation — Bind all current runway prose

- **Identity:** reopen `GP-CTL-001`; do not mint a successor for incomplete
  acceptance of the same current machine/prose runway-parity objective.
- **Objective/value:** make every designated current operating-state statement
  agree with the canonical queue's Ready IDs, counts, effective/target runway,
  primary liveness, and current validation count.
- **Current source evidence:** guarded summaries in `docs/AGENT_CONTEXT.md`,
  `docs/CURRENT_STATE.md`, and `docs/ROADMAP.md` correctly report no Ready item,
  runway zero, and `PLANNING_REQUIRED`. Unguarded prose in those same files
  still calls `GP-VAL-006` the sole Ready item, reports runway one and
  `RUNWAY_LOW`, says the current validation count is 27 before its
  implementation, or denies that `PLANNING_REQUIRED` is current. The framework
  checker nevertheless passes.
- **Implementation seam:** current status prose and the existing
  agent-framework checker/adversarial fixtures only. Either remove redundant
  current claims or place every retained claim under one exact machine-derived
  correspondence contract.
- **Scope/exclusions:** no queue item, priority, authorization, target,
  liveness derivation, user direction, product source, runtime behavior, or
  hardware state change.
- **Dependencies/gates:** queue machine state remains zero-runway at
  implementation start; any legitimate concurrent queue publication requires
  rebinding, not hard-coded preservation of this snapshot.
- **Validation:** deliberate stale Ready ID, count, liveness, priority, and
  current validation-count mutations in each current surface fail; historical
  evidence remains outside current-truth parsing; framework, sequence,
  aggregate, navigation, and agent-surface checks pass.
- **Risk/readiness:** H0, `CURATION_READY`; confidence high; no hardware,
  external-evidence, or user gate.

### GP-VAL-003 repair/revalidation — Complete tracked workflow-route census

- **Identity:** reopen `GP-VAL-003`; its accepted objective promised a complete
  tracked CI build/publication-route census and current health correspondence.
- **Objective/value:** discover and classify every tracked top-level or nested
  GitHub workflow that can build, publish, release, or invoke another build
  workflow, without promoting an unresolved route into a gated current route.
- **Current source evidence:** Git tracks `.github/workflows/build.yml`,
  `.github/workflows/build-device-config.yml`, and
  `config/glyph/.github/workflows/build.yml`. The current discovery helper uses
  `git ls-files .github/workflows`, so the two-record fixture omits the nested
  caller. That caller runs on push/pull request and invokes
  `GregTurbo/HayBox-Glyph/.github/workflows/build-device-config.yml@configurator`
  with a PAT. Validation health still says the census records “both workflow
  files,” and all current checks pass.
- **Implementation seam:** static tracked-workflow discovery, existing
  workflow census fixture/checker, health prose, exact manifest dependencies,
  and deterministic census consequences.
- **Scope/exclusions:** no workflow YAML mutation; no caller/owner/PAT/secret,
  permission, release, branch-protection, or live-invocation inference; no
  build, postprocessor, upload, device, runtime, or product change. The nested
  caller remains `UNRESOLVED_EXTERNAL` unless separate authoritative evidence
  proves more.
- **Dependencies/gates:** tracked workflow set and accepted top-level
  `CURRENT_GATED` versus unresolved classifications remain source-correspondent
  at implementation start.
- **Validation:** omitted or added nested workflow, moving reusable-workflow
  ref/call drift, event drift, and unclassified build/publication routes fail;
  the current build workflow's validation dominance remains exact; unresolved
  external routes remain explicitly ungated.
- **Risk/readiness:** H0, `CURATION_READY`; confidence high; no hardware or user
  gate.

### GP-SRC-004 repair/revalidation — Finish active-baseline classification

- **Identity:** reopen `GP-SRC-004`; the accepted objective was to remove
  machine-enforced inert claims from the active generated baseline.
- **Objective/value:** make the validation manifest and every current
  classification surface distinguish active compile-time table content from
  unchanged source-owned active-view publication.
- **Current source evidence:**
  `src/modes/UltimateIdentityRuntimeTables.hpp` includes
  `GeneratedRuntimeConfigBaseline.current.hpp`; its focused checker and fixture
  now correctly classify active table content. The current manifest row
  `generated_baseline_artifact` still has reason `inert baseline equivalence`.
- **Implementation seam:** exact manifest reason/classification wording plus a
  bounded checker assertion and deterministic health/census consequences.
- **Scope/exclusions:** no table point, symbol, order, semantic digest,
  generator behavior, active `RuntimeConfigView` publication, routing, build
  input, candidate, firmware artifact, or controller change.
- **Dependencies/gates:** active include chain and source-owned publication
  path remain unchanged.
- **Validation:** inert wording on the active baseline row fails; inert example
  artifacts retain their exact classification; all 28 table values/order and
  active publication source remain exact; source-sync, baseline, manifest,
  health, and aggregate checks pass.
- **Risk/readiness:** H0, `CURATION_READY`; confidence high; no hardware or user
  gate.

### GP-VAL-009 — Make validation-health schema exact

- **Objective/value:** remove or bind every duplicate health-fixture field so
  the machine record cannot carry contradictory exclusion state while its
  derived count remains correct.
- **Current source evidence:**
  `runtime_config_validation_health.json` reports
  `explicit_exclusion_count: 37` but also carries
  `strong_signal_exclusions: []`. The health checker derives the count from the
  manifest, never validates the duplicate list, and does not require an exact
  top-level health schema.
- **Implementation seam:** health fixture, health checker and adversarial
  fixtures, health documentation, and deterministic census consequence. The
  preferred bounded design is a schema bump that removes the obsolete duplicate
  field; if retained, it must equal a deterministic reviewed projection of the
  manifest.
- **Scope/exclusions:** no manifest entry/exclusion reclassification,
  applicability decision, checker behavior, workflow, product, runtime, or
  hardware change.
- **Dependencies/gates:** Curator binds one exact representation—removal or
  deterministic equality—before implementation; no implementer discretion
  remains after curation.
- **Validation:** unknown, missing, stale, duplicate, wrong-type, wrong-count,
  and wrong-content health fields fail; current 194/32/28/37 state passes;
  health prose, manifest, census, and aggregate remain correspondent.
- **Risk/readiness:** H0, `CURATION_READY`; confidence high; no hardware or user
  gate.

## Horizon B — aggregate and build-boundary hardening

### GP-VAL-010 — Bind manifest commands to tracked checkers

- **Objective/value:** prevent the validation manifest from naming a checker
  path while executing a different interpreter, wrapper, argument set, or
  untracked target.
- **Current source evidence:** the runner requires only a nonempty string list,
  `entry.path == entry.command[1]`, and filesystem existence before directly
  executing the command. It does not require `python3`, exact correspondence
  with `required_arguments`, or a stage-0 tracked regular non-symlink checker.
- **Implementation seam:** manifest runner validation and isolated aggregate
  adversarial fixtures only. Require an exact interpreter/path/argument
  contract and the existing normalized tracked-regular path policy.
- **Scope/exclusions:** no checker applicability, branch-policy,
  source-dependency, product semantic, workflow, build, runtime, or hardware
  change.
- **Dependencies/gates:** current commands and required arguments are
  re-derived mechanically; any noncanonical-argument lane remains excluded
  rather than silently activated.
- **Validation:** alternate executable, shell wrapper, extra or missing
  argument, malformed `required_arguments`, command/path mismatch, untracked,
  executable-mode confusion, directory, symlink, and escaping target fail;
  exact current commands pass.
- **Risk/readiness:** H0, `CURATION_READY`; confidence high; no hardware or user
  gate.

### GP-VAL-011 — Isolate and bound aggregate checker execution

- **Objective/value:** make the aggregate prove that current checker execution
  cannot mutate or hang the canonical repository instead of trusting
  `mutation_risk` metadata.
- **Current source evidence:** the runner executes each current checker with
  `cwd=ROOT`, inherited environment, and no timeout. It performs no before/after
  HEAD, index, tracked-byte, status, untracked, or ignored-output comparison.
  Existing adversarial coverage rejects a manifest row declared unsafe but does
  not catch a passing mutator or hanging checker. GP-VAL-006 isolates only its
  candidate-preparation checker and does not establish aggregate-wide
  containment.
- **Implementation seam:** after GP-VAL-010, execute current checks in one or
  more exact standalone disposable Git snapshots with controlled Python
  environment, bounded time, preserved branch/base context, and canonical
  repository immutability proof. Report the exact offending checker and
  failure class.
- **Scope/exclusions:** no current checker reclassification or semantic edit;
  no network, dependency installation, build, workflow execution, artifact,
  device, firmware/runtime, or canonical write authority.
- **Dependencies/gates:** `GP-VAL-010` command correspondence is canonical;
  Curator binds the exact branch/base/dirty-tree snapshot design and timeout
  budget. If current checkers cannot be made meaningful in an isolated copy
  without changing their semantics, return to curation.
- **Validation:** synthetic tracked, staged, untracked, ignored, index, and HEAD
  mutation stays confined and fails; a hanging checker times out; pre-existing
  dirty state is represented exactly or fails closed; branch/base scope checks
  remain meaningful; current 28 checks pass and leave canonical identity/status
  unchanged.
- **Risk/readiness:** H1, `PREAUTHORIZABLE` after GP-VAL-010 and exact snapshot
  architecture are bound; confidence medium-high; no hardware or user gate.

### GP-PROV-003 repair/revalidation — Census canonical local build entrypoints

- **Identity:** reopen `GP-PROV-003`; its accepted objective promised one
  canonical build-input provenance boundary and explicitly included local build
  scripts, so the omitted documented wrappers are incomplete acceptance of the
  same inventory rather than a new candidate.
- **Objective/value:** extend declared build provenance to the documented local
  build entrypoints whose bytes and ambient selection can affect how the
  canonical or fallback build is invoked.
- **Current source evidence:** `AGENTS.md` and `docs/WORKFLOW.md` prescribe
  `pio run -e glyph_mk6` and allow `./scripts/build-glyph-mk6-quiet.sh` as the
  fallback. The fallback calls `scripts/pio-local.sh`, which selects
  `.platformio-home`, `.venv/bin/python`, `python`, or `python3` from ambient
  state. The GP-PROV-003 inventory discovers declaration files, the PlatformIO
  extra script, and `glyph_nuker`, but does not bind either local wrapper.
- **Implementation seam:** extend the declared-only provenance docs, fixture,
  and checker with exact wrapper modes/hashes, command chain, documented
  canonical/fallback classification, and explicit unresolved ambient
  interpreter/PWD/TMPDIR facts; add only deterministic manifest/census/health
  consequences.
- **Scope/exclusions:** no wrapper, PlatformIO, dependency, environment,
  workflow, build, source, artifact, or upload change; no claim of dependency
  closure, interpreter identity, reproducibility, artifact acceptance, or
  hardware evidence.
- **Dependencies/gates:** wrapper paths remain documented current entrypoints;
  the inventory stays static and must not execute shell or build tooling.
- **Validation:** wrapper addition/removal/rename/mode/hash, call-chain, env
  selection, canonical/fallback classification, and unresolved-fact drift fail;
  unrelated scripts are not promoted automatically; provenance, census, health,
  and aggregate checks pass without executing a build.
- **Risk/readiness:** H0, `CURATION_READY`; confidence high; no hardware or user
  gate.

## Horizon C — evidence and user-decision-gated survivors

### GP-VAL-008 — Research a current runtime-behavior regression lane

- **Objective/value:** identify the minimum current-source and directly
  accepted-evidence-backed runtime-behavior regression subset without reviving
  stale May-28 literal anchors.
- **Current source evidence:** source-sync and offline semantic bridge checks
  are current, while the historical behavior evaluator remains explicitly
  excluded. No new domain authority or hardware result appeared.
- **Implementation seam:** read-only source/evidence mapping and a proposed
  fixture/checker design; research may validly conclude that no safe subset is
  yet available.
- **Scope/exclusions:** no invented Ultimate semantics, expected coordinate,
  routing or table edit, Nunchuk claim, firmware change, or substitution of
  source inspection for hardware evidence.
- **Dependencies/gates:** every expected case needs current source/tests or
  explicit owner/domain evidence.
- **Validation:** exact per-case provenance, stale-anchor rejection, historical
  isolation, and zero product-source delta.
- **Risk/readiness:** H1 research, `EVIDENCE_GATED`; confidence medium on the
  gap and low on executable breadth.

### GP-ART-001 — Establish approved immutable firmware-artifact custody

- **Objective/value:** define and prove one durable content-addressed custody
  path so future H2/H3 evidence can bind an exact candidate SHA to exact
  retrievable bytes.
- **Current source evidence:** the hardware contract requires an immutable
  candidate/artifact-addressed locator; current provenance explicitly records
  no established store or immutable locator.
- **Implementation seam:** owner-selected store/custody/retention contract,
  synthetic write/readback/tamper/expiry verification, then separately
  authorized real-store integration.
- **Scope/exclusions:** no implicit store choice, credentials, upload, release,
  build, device update, artifact acceptance, or hardware PASS.
- **Dependencies/gates:** project owner names the exact store, custody,
  retention, immutability, access, expiry, and readback model; external
  permissions must be verifiable.
- **Validation:** content-addressed write/readback/re-hash, wrong content,
  mutable locator, deletion/expiry, access denial, and missing custody cases.
- **Risk/readiness:** H1/external state, `USER_DECISION_GATED`; confidence high
  on the gap and unknown on the store choice.

### GP-X1-001 — Realize a separately authorized X1 behavior candidate

- **Objective/value:** after exact production-authorized replacement values
  exist, realize exactly one X1-table delta through the approved source-owned
  path and exact-snapshot hardware lifecycle.
- **Current source evidence:** `GLYPH-UD-008` and `GLYPH-UD-009` authorize only
  the baseline-equivalent sole-`kX1Table` no-op intake. No different X1 bytes,
  second-table ownership, or gameplay meaning exists.
- **Implementation seam:** new exact nine-point accepted intake, existing
  offline generator, active source-owned table include, semantic/diff manifest,
  canonical build, durable artifact identity, and candidate-specific hardware
  protocol/result.
- **Scope/exclusions:** no second table, routing/modifier/game semantics,
  runtime-loaded config, persistence, device/protobuf write, flashing
  automation, Nunchuk claim, or inference from the no-op pilot.
- **Dependencies/gates:** new explicit production authority supplies nine
  changed X1 points and sole-X1 ownership; `GP-ART-001` custody and an exact
  hardware protocol are available; H2/H3 candidate receives exact-artifact
  physical PASS before merge.
- **Validation:** exactly one changed table and directions 1–9, byte-range
  coordinates, other 27 table digests unchanged, canonical build, exact
  candidate/artifact binding, and physical PASS before merge.
- **Risk/readiness:** H3, `USER_DECISION_GATED`; confidence high on lifecycle
  and zero on future content.

## Planner-estimated curation order

These are non-authoritative estimates only:

1. reopen `GP-CTL-001`, `GP-VAL-003`, and `GP-SRC-004` to repair direct
   contradictions in already-completed objectives;
2. curate `GP-VAL-009` and `GP-VAL-010` as small exact H0 schema and command
   correspondence gates;
3. reopen `GP-PROV-003` as independent H0 declared-input provenance repair;
4. preauthorize `GP-VAL-011` only after `GP-VAL-010` and its exact isolated
   snapshot/timeout contract are accepted;
5. retain `GP-VAL-008`, `GP-ART-001`, and `GP-X1-001` behind their stated
   evidence or user-decision gates.

Planner estimates do not alter the queue, mark work `READY` or
`PREAUTHORIZED`, or override the Curator.

## User-direction and evidence effects

- `GLYPH-UD-007` still retires official-configurator interoperability and
  capture as product/progression dependencies.
- `GLYPH-UD-008` and `GLYPH-UD-009` still authorize only the exact
  baseline-equivalent sole-X1 no-op intake; no different table content or
  active behavior follows.
- No new user direction, source-authority intake, hardware result, immutable
  artifact-custody decision, or external workflow-ownership evidence appeared.
- Legacy Y2 and Alternative-B reports remain operational evidence for their
  accepted prior mechanisms only and cannot satisfy a new Revision-2
  candidate/artifact gate.
- The canonical-grid result remains `HARDWARE_FAIL`; no repair candidate is
  proposed without new source authority.
- Nunchuk remains `NOT_TESTED`; root cause remains unproven.

## Rejected, deferred, and duplicate alternatives

- Do not duplicate any completed 2026-08-27 packet candidate. Reopen only the
  four exact identities whose accepted correspondence objectives are directly
  incomplete on current source.
- Do not resurrect the official-configurator capture or compatibility lane, or
  rename invalidated `GP-CONFIG-002`.
- Do not classify the nested workflow as current-gated, infer its caller,
  ownership, PAT, permissions, or release authority, or edit it without
  separate source and user authority.
- Do not expand all 194 discovered checkers into current load-bearing scope;
  historical, hardware-evidence, superseded, unsafe, and out-of-lane
  distinctions remain intentional.
- Defer `builder_scripts/arduino_pico.py` remediation of global Git config and
  unchecked Git reads until its exact clean/dirty/error firmware-version
  contract and build proof are curated. Reopened `GP-PROV-003` inventories
  this boundary but does not change it.
- Defer postprocessor target rewriting. `glyph_nuker` purpose, CLI semantics,
  byte transformation, source lineage, reproducibility, safety, and artifact
  acceptance remain `UNKNOWN`; the current invocation cannot be changed under
  an equivalence claim absent source evidence.
- Defer action/dependency pinning, resolved build closure, runner-image
  identity, and reproducibility claims; current observations remain bounded and
  mostly unresolved.
- Do not treat a unique quiet-build log path as artifact custody or portfolio
  recovery; it is local convenience and does not establish immutable firmware
  bytes.
- Do not repair or rebuild the failed canonical-grid candidate without exact
  new source authority and the full H3 lifecycle.
- Reject runtime-loaded config, persistent storage, WebSerial/device write,
  protobuf binary write, backend config write, flashing automation,
  `candidate.view`, `active_storage.view`, generated active
  `RuntimeConfigView` replacement, RAM-backed active publication, Nunchuk, root
  cause, and game-semantic proposals under current authority.

This packet is candidate supply only. It grants no implementation, queue,
firmware, table, workflow, build, external-storage, publication, device, or
hardware authority.
