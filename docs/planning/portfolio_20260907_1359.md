# Glyph Portfolio Candidate Packet — 2026-09-07 13:59 EEST

This fresh material packet contains non-authoritative candidate supply only.
Independent Curator review is required before its dispositions can become
canonical. No global wait is proposed. It grants no implementation or product
authority.

```yaml
packet_id: glyph-portfolio-20260907-1359
packet_state: FRESH
planning_branch: planning/portfolio-20260907-1359
base_configurator_sha: a23658d1d2b2e90952de4c62a343de52c386041a
base_live_verified: true
previous_planner_packet: glyph-portfolio-20260907-1256
previous_packet_commit: fc2a0eac5963298de9ff1e2c5b3f86a27d96f056
previous_packet_base: 4ef7e43f6f4f8b888ad7f10dc2794d9aec48a7e6
previous_packet_freshness: STALE_WAIT_REVOKED
candidate_count: 2
new_candidate_count: 0
carried_forward_candidate_count: 2
current_ready_work_order_count: 0
curator_review_required: true
global_wait_proposed: false
planner_authority: NON_AUTHORITATIVE_CANDIDATE_SUPPLY_ONLY
configurator_changed: false
```

## Audit result and freshness

This broad refresh follows a material failure that revoked the earlier 1256
packet and its accepted wait. The ordinary minimal live GitHub verification
failed restricted DNS; the permitted read-only retry independently verified
live configurator at `a23658d1d2b2e90952de4c62a343de52c386041a`. The clean
supervisor checkout matched that SHA, and this isolated planning branch has
that exact direct parent. The broad source/evidence findings below were
rechecked against its canonical product source and complete delta since
`4ef7e43f6f4f8b888ad7f10dc2794d9aec48a7e6`. That delta is control-plane,
its directly coupled governance checker and census only: product/runtime,
build inputs, source generators and hardware evidence have not changed.
Fresh source reads covered current SetConfig rejection branches, persistence
research and boot handling, X1 artifact reconciliation, neutral-profile
boundaries, queue/work-order state, scheduled/manual routing, and the
immutable Planner/Curator receipt implementation. The existing broader audit
of the unchanged remaining surfaces is retained explicitly below.

GP-PERSIST-001 remains canonically DONE: reviewed feature
`2b2a48e14e9d621b13038d7a8f29e57713ef462a` entered canonical through
`3a9d41a9927983f1ba5d1e4ff6cf74af57ac634f`, with separate completion at
`4ef7e43f6f4f8b888ad7f10dc2794d9aec48a7e6`. Its evidence study is not
requeued. GP-VAL-011's earlier reviewed implementation remains historical
ancestry, but current work-order completion is explicitly revoked.

The clean main checkout exposed an ignored nested-Git-directory fingerprint
bug that temporary-clone validation had not exercised. Independently rebound
repair candidate `a0373bde823856c4835bb5aed429d0b402eadd48` corrected physical
traversal and passed 41 adversarial groups, but its full actual-main aggregate
failed the fixed 300-second budget during checker_context (entry 10), with
final canonical proof UNAVAILABLE. It is failed, unmerged local evidence,
not current implementation authority. The previously accepted 1256 wait is
historical and cannot hide this unresolved technical repair.

Source inspection found more than 55,000 ignored framework files. Read-only
metadata inspection counted 4,772,972,757 logical bytes under eight ignored
directory roots, including 4,443,348,617 bytes in the framework-arduinopico
root. These are local observations, not repository size requirements or a
new performance benchmark. The canonical independent adjudication records
initial complete fingerprint 205.872 seconds and 54.814 of a bounded 60.002 seconds in 26,104 required
opens. A small threaded diagnostic indicates possible latency overlap only;
it proves neither full-tree feasibility nor safe cancellation/proof aggregation.
Complete byte/membership proof and the existing 300-second operational
deadline remain mandatory. Skipping ignored content, changing checker semantics, weakening independence or raising time budgets
is not a proposed repair.

Two existing owner-gated candidates survive; zero new candidates are invented.
The existing GP-VAL-011 repair remains separately unresolved and is not renamed
or duplicated as candidate supply. Canonical queue status REVIEW is explicitly
NONEXECUTABLE/REPAIR_REQUIRED, not pending merge approval. The substantive
stop and narrow next-authority question are durably recorded in
`docs/agent_framework/GP_VAL_011_RECOVERY_ADJUDICATION.md`; temporary diagnostic
files are not necessary to reconstruct them. Zero executable runway does not
support global scarcity/evidence wait while this technical repair is unresolved.

## Broad audit and why each surface supplies no new immediate implementation

| Surface | Current source/evidence inspected | Finding and disposition |
| --- | --- | --- |
| Source-owned realization and active publication | `src/modes/Ultimate.cpp`; `src/modes/UltimateIdentityRuntimeTables.hpp`; active `GeneratedRuntimeConfigBaseline.current.hpp`; `tools/check_glyph_source_owned_table_symbol_map.py`; `IMPLEMENTATION_BOUNDARY.md` | The existing source-owned active view remains selected. GP-SRC-006 already enforces the exact active initializer/resolver structure. No new active-path design is authorized or needed by source evidence. |
| Generator, input integrity, authority intake, offline writes | `generate_source_owned_runtime_config.py`; `source_owned_generator_modes.py`; `source_owned_source_authority_intake.py`; `manage_source_owned_source_authority_intake.py`; candidate-prep/install tools; source-authority workflow | Strict duplicate/non-finite JSON rejection, explicit ownership, 28-table baseline identity, overlay/preserve, no-op discrimination, deterministic preview, protected output, and isolated writes already cover the concrete current failure classes. A new corpus/index/rewriting utility without an unmet use case would duplicate completed GP-SRC-001/002/003/005 work. |
| X1 lifecycle and other modifier migration | `USER_DIRECTION.md` GLYPH-UD-008..012; X1 intake; `current_x1_regression_subset.json` and checker; X1 protocol/result/fixture | Exact sole/non-mode X1 offset-41 candidate is accepted, integrated and now baseline-equivalent. It owns neither mode+X1 nor any other table or future values. Additional modifier/table work needs new exact nine-point data and ownership/routing scope; baseline source is comparison authority only. No H2/H3 candidate is proposed from guessed values. |
| Build, reproducibility, provenance | `builder_scripts/arduino_pico.py`; local wrapper chain; provenance inventory and historical observations; `glyph_nuker_source_lineage.md`; tracked workflows | GP-BUILD-001 resolves the proven Git fail-open/global-config issue; GP-PROV-008 covers local entrypoints while keeping historic observations immutable. Movable selectors, runner/installed toolchain identity and binary purpose remain explicit limits. GP-PROV-005 already performed bounded named authoritative lineage research. No new source/owner evidence justifies repeating it or silently pinning dependencies. Build reproduction would require new build/artifact scope, not invent an H0 task. |
| Runtime-config aggregate and applicability | runner; aggregate adversarial checker; `glyph_checker_context.py`; manifest/census/health; GP-VAL-011 contract repair | GP-VAL-011 is unresolved existing repair, not DONE and not a new candidate. Original environment/ref/ownership corrections remain reviewed history; actual-main ignored-directory traversal exposed a further deterministic-budget failure. Curator has stopped implementation pending a new explicit proof/ownership/cancellation design. Current/historical/excluded applicability stays unchanged; no checker removal or deadline relaxation is proposed. |
| Controller backend failure safety and current Config write | `HAL/pico/src/comms/ConfiguratorBackend.cpp`; its header; `InputMode.cpp`/header; `mode_selection.cpp`; current transport source-authority note | A real live-state rejection asymmetry remains; GP-CONFIG-005 retains the required explicit owner-authority gate. A source-only precursor is the branch/reference mapping in this audit plus PERSIST research; do not create a duplicate research packet to delay the product choice. |
| Artifact custody | `HARDWARE_EVIDENCE.md`; `WORKFLOW.md`; sidecar provenance; X1 result artifact-reconciliation section and fixture | Exact X1 bytes were recovered to local content-addressed backup after testing under a candidate-specific anomaly. This neither establishes prospective approved custody nor authorizes upload. GP-ART-001 remains a real prerequisite before next ordinary Revision-2 handoff. |
| Current-Config persistence and recovery | `HAL/pico/src/core/Persistence.cpp`; `config/glyph/common/src/config.cpp`; SetConfig; `current_config_persistence_recovery_research.md`/fixture/checker and pinned Arduino/littlefs research | Completed research records checked/unchecked error paths, wrapper truncate/sync semantics, boot decode/default/autoformat ambiguity, wear/capacity unknowns and options without selecting a recovery mechanism. A future recoverable-save implementation needs an owner-approved failure invariant, compatibility/boot policy and H3 work order. Research is not requeued. |
| Custom backend/configurator versus retired official app | GLYPH-UD-007; current transport source-authority note; backend SetConfig source; historical official corpus | Existing custom backend handling is current source; official app interoperability/capture stays retired. No official capture, vendor export, WebSerial or new write lane is proposed. |
| Future neutral-profile seams | `docs/project/G3_NEUTRAL_PROFILE_INTEGRATION_BOUNDARY_DESIGN.md`; `docs/project/G8E_SENSCOPE_HANDOFF_BOUNDARY_NOTES.md`; converter and offline profile checker; implementation boundary | Offline strict profile/layout bridge and packaging are already checked. Senscope owns neutral intent and game datasets/equivalence. New generic capability/schema/effective-output support would need source and separate authority; no app work or semantic inference belongs here. |
| Hardware and failed history | latest Y2 and Alternative-B legacy evidence; generated-grid failure `e643017c...`; Revision-2 X1 record | Legacy evidence does not authorize new artifacts; failed grid/candidate/RAM-publication paths remain excluded. Nunchuk is NOT_TESTED and root cause remains unproven. No untested candidate waits for hardware; the remaining repair is H1 technical work, not physical-evidence scarcity. |
| Orchestration/control plane | queue; authorization/runway; supervisor/subagent contracts; scheduled/manual prompts; Runner boundary; framework checker packet correspondence | Existing scheduled/manual prompts route through canonical instructions; no custom daemon or schedule change is justified. Completed governance work now validates actual candidate count/boolean and immutable Curator receipt correspondence; the former hardcoded 13-count issue is historical. Fresh packet adoption needs ordinary separate Curator receipt/adoption, with no wait accepted and no repair hidden by a zero-runway label. |

The parent supervisor's independent source/persistence and GP-VAL-011
reviewers supply parallel read-heavy specialist coverage; this Planner did not
spawn a duplicate investigator while all useful concurrency slots were
occupied. Planner findings remain non-authoritative and require a separate
skeptical Curator review.

## Surviving candidates

### GP-CONFIG-005 — Preserve prior live Config when existing SetConfig rejects

- Estimate: `USER_DECISION_GATED`; H2, potentially H3 if durable recovery is
  brought into scope. Existing custom Glyph/HayBox backend only.
- Exact current evidence: `HandleSetConfig()` resets `_config` before
  `pb_decode`; decode failure attempts `LoadConfig(_config)` and ignores its
  result; later count/type/reference validation failure returns without that
  restore; save failure also returns error without restoring. The backend
  holds `Config &_config`; mode objects can retain pointers into Config.
  Changed rejected data remaining observable is a source inference, not a
  physical observation or gameplay claim.
- Objective after authority: make rejected current-Config updates preserve the
  previously live Config while successful updates retain explicitly reviewed
  publication behavior. Do not conflate that with durable rollback or a new
  runtime-config mechanism.
- Small owner question: **Authorize a bounded repair of this existing custom
  backend's SetConfig rejection behavior, requiring prior live Config to remain
  intact on rejected decode, validation or save, with persistence recovery
  separately gated; or leave that path unchanged for now?**
- Options/consequences: authorize the invariant and permit Curator to bind
  staging/restoration, reference rebinding and exact failure/success checks
  before one H2 candidate; or defer, retaining the documented current risk.
  Disabling the route is another firmware/product decision, not an automatic
  fallback. Source evidence supports the value of preserving prior live state;
  it does not choose rollout priority or implementation architecture for the
  owner.
- Safe precursor: source rejection/reference mapping has been completed here
  and in GP-PERSIST-001; it requires no device, rejected-input injection or
  implementation. No further mandatory duplicate H0 packet is needed before
  the owner can answer.
- Dependencies: explicit existing-backend-write approval; complete Curator
  architecture/scope/validation contract; approved exact artifact custody
  before hardware handoff; exact candidate/artifact protocol and physical PASS
  before merge. No PREAUTHORIZED activation is mechanical yet.
- Allowed future seam: current backend handler and separately authorized
  negative/failure corpus only, after exact work order. Exclude protobuf/schema,
  official-app/vendor compatibility, runtime tables, WebSerial, persistence
  redesign, flashing, gameplay, and unrelated mode behavior.
- Validation: every decode/reference/save failure, failed restore path if used,
  reference validity and success publication, canonical build, independent
  review and Revision-2 hardware acceptance. Host tests cannot claim controller
  acceptance.
- Invalidation: source handler/reference topology change; new owner direction;
  different desired failure invariant; new storage semantics; missing candidate
  custody or exact test evidence. Re-curate rather than infer authority.
- Overlap: distinct from completed H1 PERSIST research; durable file recovery
  remains its own future H3 decision.

### GP-ART-001 — Approve prospective exact firmware-artifact custody

- Estimate: `USER_DECISION_GATED`; H1 custody design/external state, with no
  firmware or physical test implied.
- Exact evidence: hardware contract rejects mutable `.pio` as durable custody
  and requires unique candidate-SHA/artifact-SHA locator plus immediate
  pre-update retrieval/re-hash. X1 result explicitly classifies its post-test
  local backup as a specially allowed reconciliation anomaly. Existing CI
  sidecars record identity with `immutable_locator: null`; CI upload metadata
  alone proves neither immutability nor retention.
- Remaining problem: make future candidates retrievable as the exact tested
  bytes through hardware handoff, result processing, integration and rollback;
  prevent rebuild substitution, mutation, deletion/expiry and inaccessible
  artifacts. X1 is complete and is not reopened.
- Small owner question: **Which prospective custody model is approved for new
  Revision-2 candidates: explicitly approve an owner-held content-addressed
  local archive with a stated durable backup/retention responsibility, or name
  an approved external release/object/artifact store and its retention/access
  owner?** The owner must specify retention (at least through candidate
  lifecycle and intended rollback use), who preserves access, and whether the
  local durability risk is acceptable. No store or credentials are chosen here.
- Options/consequences: an explicitly approved durable local archive avoids
  external publication but needs a real preservation/access policy and policy
  alignment; an external approved store can add independent custody, but needs
  named location, permission, immutability/retention/readback and owner. Merely
  leaving bytes in `.pio`, using expiring CI artifacts, or rebuilding later is
  insufficient. Source evidence does not prefer one store for the owner.
- Does current local policy suffice? **Not prospectively under current
  canonical authority.** Exact X1 preservation succeeded, but candidate-specific
  reconciliation is not standing approval. Separate explicit local-custody
  policy could resolve the gate; this packet does not grant it.
- Timing: resolve before the next behavior-changing modifier candidate's
  ordinary hardware handoff, including any prospective GP-CONFIG-005 H2/H3
  artifact. H0/H1 research does not require firmware-artifact custody.
- Allowed future seam: owner-approved custody contract plus bounded synthetic
  locator/readback/hash/tamper/missing/expiry/access-denial validation; real
  store integration only when separately authorized. Exclude credentials,
  upload/release, real artifacts/builds, device writes and hardware PASS from
  the present candidate.
- Invalidation: store retention/permission/immutability changes; missing exact
  bytes; candidate source/artifact change; new owner custody direction. Rehash
  actual retrieved bytes rather than accept a locator string.
- Overlap: complements GP-PROV-001/002/007 sidecar correspondence; it does not
  redo those checks or retroactively relabel X1 evidence.

## Other real future decisions, without manufacturing implementation candidates

The newly completed persistence packet binds Arduino-Pico
`32e74d024e5e3ee5e7ec9593f5a4101641c61897` and littlefs
`6a53d76e90af33f0656333c1db09bd337fa75d23`, with 48 ordered current
operations and 15 immutable upstream blob records. Source shows unchecked
header write/seek/close handling, wrapper truncate-open sync and default
autoformat, plus a boot path that may save reset/partially decoded Config
after load failure. Those consequences remain labeled inference or unknown
where appropriate; source does not prove physical power-loss outcomes.
Upstream fault injection operates at program/erase boundaries and does not
prove torn operations, brownouts or controller behavior. No new source-grounded
H1 study is required merely to ask the owner for the intended recovery goal.

Fresh persistence research records a separate future product choice: what
must survive a failed save/reboot (old-or-new valid Config, specifically a
prior-good Config, or approved default recovery), including decode failure,
mount/autoformat failure and compatibility. Selecting temp-and-rename,
temp-plus-backup or two-slot/generation before that invariant is authorized
would select an H3 mechanism without authority. The evidence study itself
needs no user decision to complete. No speculative GP-PERSIST successor is
counted as executable supply.

Future modifiers need the owner/Senscope to supply exact table ownership,
ordered nine-point raw data and routing scope. No existing X1/Y2 PASS answers
that for another table. These are future direction/evidence opportunities,
not untested implementation candidates or current hardware-pending work.

Rejected duplicates: another official-configurator capture/export lane;
repeating successful source/write guardrails or X1 regression; another
inventory solely to grow the count; repeated binary-lineage search with no
new named source; automatic dependency pinning/build reproduction; a custom
orchestration daemon; active RuntimeConfigView/RAM/profile publication;
controller/game semantics inferred from source grid names. None is proposed.

## No global wait; exact next authority boundary

`global_wait_proposed: false`. Effective authorized runway is zero: no READY,
recorded PREAUTHORIZED, mechanically activatable PREAUTHORIZED or
hardware-pending work is identified. This is not an accepted global wait and
is not a declaration that only product-owner decisions remain.

Existing GP-VAL-011 is a non-executable REPAIR_REQUIRED item. The failed local
candidate may be preserved for evidence but must not merge. A new explicit
bounded proof architecture and ownership/cancellation contract must be
independently authorized before implementation resumes; that must preserve
complete ignored state, byte/membership proof, independent snapshot, fixed
120/300-second budgets, process cleanup, unchanged checker/ref/environment
semantics and fail-closed unavailable proof. This Planner neither chooses
that architecture nor converts its unresolved work into READY supply.
The narrow owner question is whether to authorize a separate, explicitly
designed and independently reviewed concurrent fingerprint repair effort
preserving all these invariants, or defer and leave it non-executable. The
owner need not choose worker count or mechanism. Authorization permits
subsequent exact contract work; it does not prove performance feasibility.

The two carried owner gates are independently answerable while technical
repair remains stopped. An answer on one lane authorizes fresh curation of
that lane only and does not repair GP-VAL-011. Choosing nothing does not cause
this technical blockage: its separately required authority remains unresolved.
Future exact modifier data or a current-Config durability goal can likewise
trigger bounded fresh curation without reopening retired or forbidden lanes.
Timer passage alone is no reason to manufacture candidates.

Independent Curator should adopt fresh provenance and the two truthful owner
gates while retaining the existing non-executable repair and no global wait.
The next ordinary Runner must read the canonical technical stop and route to
Curator/owner authority, not execute a failed branch or infer implementation
permission from a zero-runway packet. This packet supports neither an
all-safe-work-completed claim nor NORMAL_WORKFLOW_RESUMED.

## Publication and non-claims

Planner publishes only the single non-authoritative packet on its own direct
child of the final live canonical base. Curator independently verifies source,
counted candidates, exclusions, explicit non-wait scope and exact object
correspondence;
records dispositions and approval/rejection on a separate canonical control
plane branch; and performs necessary directly coupled governance checks with
fresh independent review. No candidate is Ready because it appears here.

Firmware/runtime source changed by this Planner: NO. Build: NO. Firmware
artifact: NO. Device: NO. Hardware test: NO. New gameplay or controller semantic
claim: NO. Nunchuk remains NOT_TESTED; root cause remains unproven; all forbidden
runtime publication/loading/storage/browser/protobuf/flashing boundaries remain.
