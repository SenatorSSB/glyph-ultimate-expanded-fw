# Glyph Portfolio Candidate Packet — 2026-09-21 12:52 EEST

This immutable Revision-2 packet is non-authoritative candidate supply for an
independent Work-Order Curator. It authorizes no implementation, queue change,
firmware behavior, device operation, or hardware acceptance.

```yaml
packet_id: glyph-portfolio-20260921-1252
packet_state: FRESH
planning_branch: planning/portfolio-20260921-1252
base_configurator_sha: 71dc9979a78ae2174a232884e1692f833ea80de3
base_live_verified: true
previous_planner_packet: glyph-portfolio-20260921-1243
previous_packet_commit: 5578f51c1e5d9ffef7b80ebb1e401a9ee36d1574
previous_packet_freshness: REJECTED_INVALID_FRONTMATTER
candidate_count: 1
curator_review_required: true
global_wait_proposed: false
material_events_since_publication: GP-CONFIG-010 exact-snapshot hardware acceptance remains separately INCONCLUSIVE; the owner confirmed the exact earlier nine-point kX1Table as the desired restoration target; packet glyph-portfolio-20260921-1243 was rejected before adoption because its frontmatter was not flat.
planner_authority: NON_AUTHORITATIVE_CANDIDATE_SUPPLY_ONLY
configurator_changed: false
```

## Basis, freshness, and limits

Live `configurator` was verified through `git ls-remote` at
`71dc9979a78ae2174a232884e1692f833ea80de3`; the planning commit must be its
direct child. The source checkout was clean at that SHA. The current queue has
zero READY work, zero effective authorized runway, and one separate
hardware-pending item. The last canonically adopted packet,
`glyph-portfolio-20260921-0052`, is consumed. This new owner decision is a
material event and supplies one narrowly bounded candidate for independent
curation. Global wait is not proposed.

The immediately preceding packet,
`glyph-portfolio-20260921-1243` at
`5578f51c1e5d9ffef7b80ebb1e401a9ee36d1574`, was rejected before adoption:
its `material_events_since_publication` frontmatter used an indented YAML list,
while the immutable correspondence contract requires every nonblank
frontmatter line to be a unique anchored flat `key: value` line. This corrected
packet is a fresh direct child of live canonical. It does not modify or rely on
the rejected packet for authority.

Inspected authority includes `AGENTS.md`, current agent state, current state,
roadmap, workflow, runtime implementation boundary, framework README,
authorization/runway, work-order template, hardware evidence contract, current
queue, current user-direction register, exact current source, both X1 authority
intakes, GP-X1-001 protocol/evidence, and Git history around the X1 table.
Senscope was not used as source authority, and this packet creates no Senscope
binding or modifier identity.

The project owner supplied the exact restoration target on 2026-09-21 after
reviewing both the current test grid and the earlier source table. The owner
confirmed these ordered direction rows, 1 through 9:

| Direction | Meaning | Raw X | Raw Y | Miniscreen X | Miniscreen Y |
| ---: | --- | ---: | ---: | ---: | ---: |
| 1 | down-left | 93 | 51 | -35 | -77 |
| 2 | down | 128 | 51 | 0 | -77 |
| 3 | down-right | 163 | 51 | +35 | -77 |
| 4 | left | 93 | 128 | -35 | 0 |
| 5 | neutral | 128 | 128 | 0 | 0 |
| 6 | right | 163 | 128 | +35 | 0 |
| 7 | up-left | 93 | 205 | -35 | +77 |
| 8 | up | 128 | 205 | 0 | +77 |
| 9 | up-right | 163 | 205 | +35 | +77 |

The miniscreen columns apply only `display = raw - 128`; they make no gameplay,
angle, or radius claim. Git source at parent
`045bca0d1450c261c3c60ccf5ef86f7302bd3dbc` and the accepted historical
`x1_baseline_equivalent_overlay_v1.intake.json` contain the same ordered raw
points. Commit `74ae24364b84520d4e0e39240beb9867653cc7b9` changed only `kX1Table`
semantics from those values to the current offset-41 grid for GP-X1-001.

## GP-X1-002 — restore owner-confirmed prior X1 table

- Readiness estimate: CURATION_READY. Risk H2. Confidence high for the exact
  table delta and existing realization mechanism; physical acceptance remains
  mandatory.
- Objective: restore the existing sole/non-mode `kX1Table` to the exact nine
  owner-confirmed values above while preserving every unrelated table, routing
  rule, mode behavior, publication mechanism, and GP-CONFIG-010 identity.
- Why this matters: GP-X1-001 intentionally installed a test-oriented offset-41
  table. The owner has now selected the prior source-owned table as the desired
  normal X1 behavior.
- Behavioral claim: with the existing table selection path selecting
  `RuntimeTableId::X1`, the nine normalized direction rows read the exact raw
  points shown above. No claim is made for mode+X1/`kMX1Table`, another
  modifier, a physical button name, gameplay effect, angle, radius, Nunchuk,
  persistence, or runtime-loaded configuration.
- Selected architecture: use the accepted source-owned production-authority
  intake and generator pipeline in `overlay_preserve` mode, owning only
  `kX1Table`. Create a new restoration-specific authority record rather than
  rewriting either historical X1 intake. Preserve the existing active
  `RuntimeConfigView` publication mechanism. The current source confirms that
  non-mode X1 selects `RuntimeTableId::X1`; it does not authorize a routing
  change.
- Scope: durable owner-direction record for the exact nine rows; one new
  restoration-specific production-authorized intake; directly required
  source-authority locator/checker and generated source-owned consequences;
  exact `kX1Table` replacement; candidate-focused source/diff/table checks;
  hardware protocol; canonical build, exact UF2 custody, and source-free
  hardware-pending control-plane publication. After exact PASS, update current
  regression/evidence correspondence only through the normal evidence and
  integration lifecycle while preserving GP-X1-001 as historical evidence.
- Explicit excluded scope: no GP-CONFIG-010 source, candidate, artifact,
  protocol, evidence, or queue mutation beyond preserving its separate current
  status; no change to the other 27 tables, `kMX1Table`, routing, precedence,
  bindings, active publication, mode behavior, profiles, persistence, USB,
  WebSerial/device/protobuf/backend write, flashing automation, neutral Profile
  schema, Senscope data or binding, gameplay semantics, Nunchuk claim, or root
  cause claim.
- Touched planes: source-owned configuration, generated tables/artifacts,
  firmware runtime table bytes, build tooling/checker consequences, and
  docs/evidence control plane. Any routing or active-publication delta stops.
- Source authority: direct project-owner confirmation on 2026-09-21 of the
  exact ordered raw rows; matching historical source at
  `045bca0d1450c261c3c60ccf5ef86f7302bd3dbc`; matching accepted historical
  intake `docs/runtime_config/intakes/x1_baseline_equivalent_overlay_v1.intake.json`;
  current source table at
  `src/modes/runtime_config/generated_source_owned/GeneratedRuntimeConfigBaseline.current.hpp`;
  current selection structure in `src/modes/Ultimate.cpp` and table identity in
  `src/modes/UltimateRuntimeConfigInterpreter.hpp`. Historical agreement
  supports provenance; the owner's new confirmation supplies the current
  product decision.
- Dependencies/prerequisites: independent Curator must durably record the new
  owner direction and complete a READY H2 work order. Live canonical, current
  source-owned generator/intake contracts, active publication path, and exact
  unrelated table/routing bytes must remain materially unchanged. GP-CONFIG-010
  remains isolated and need not complete first.
- Substantive authorization rationale: the owner selected every behaviorally
  relevant coordinate, the current code already supplies the bounded X1 table
  path, and the accepted overlay mechanism supports exact single-table
  ownership. No semantic value, routing choice, or backend capability needs to
  be invented. Hardware risk is handled by exact-snapshot build and physical
  acceptance before merge.
- Validation: require exact ordered values and miniscreen conversion; prove
  only `kX1Table` semantic content changed and all other 27 table bytes are
  preserved; prove routing, mode selection, precedence, active publication,
  compiled profile data, persistence, USB and GP-CONFIG-010 paths are unchanged;
  validate new authority identity and reject old/current/mixed/tampered rows,
  expanded ownership, stale baseline, and unsupported locator forms. Run the
  source-authority intake, generator-mode, generated-source contract, current
  runtime-config aggregate, agent framework, sequence, navigation, agent
  surface, and exact diff checks. Build with `pio run -e glyph_mk6`, using the
  documented quiet fallback only if necessary. Obtain a fresh independent
  source/diff/firmware-safety review.
- Candidate/artifact lifecycle: commit the complete source candidate before
  build; record exact base and candidate SHAs; inspect the map/RAM delta; hash,
  preserve, read-back hash, and re-hash the UF2 under
  `local_backups/hardware-artifacts/<candidate>/<artifact>/firmware.uf2`.
  Publish only source-free hardware-pending metadata to canonical. Stop before
  merge.
- Required physical protocol: print the complete nine-row raw/miniscreen table
  immediately before testing. On the owner's existing normal Ultimate setup,
  activate only the unchanged sole/non-mode X1 path and verify all nine rows,
  neutral/release behavior, normal unrelated representative inputs,
  miniscreen/menu sanity, continuous connection, ordinary disconnect,
  power-cycle/reconnect, automatic expected profile selection, and a
  representative post-reconnect X1 row. The activation sequence must be
  source-resolved or owner-confirmed before the test; do not invent a physical
  button mapping. Nunchuk remains NOT_TESTED unless explicitly exercised.
- Acceptance: PASS requires every mandatory exact row and regression check to
  pass for the exact candidate/artifact identity. Preserve structured
  Revision-2 evidence, including controller model as owner supplied, anomalies,
  rollback observations, and explicit nonclaims. A rebuilt or integrated UF2
  is not the physically accepted artifact.
- Integration: after independent evidence processing publishes exact PASS, a
  fresh Implementation Supervisor must verify GP-VAL-015 correspondence against
  then-current canonical, rerun invalidated gates, and integrate only if the
  tested critical source/build inputs remain exact. Preserve the original
  tested artifact identity and never describe the integration rebuild as
  physically tested.
- Rollback/recovery: on FAIL, PARTIAL, INCONCLUSIVE, custody mismatch, or source
  drift, preserve the result and stop. Do not patch or rebuild under the same
  evidence identity. If restoration is requested, use the currently accepted
  rollback artifact selected through project custody records.
- Invalidation/stops: any coordinate change; ownership beyond `kX1Table`;
  routing/binding/precedence/mode/publication delta; inferred physical route;
  loss of exact generator/source correspondence; coupling to GP-CONFIG-010 or
  Senscope; unsupported behavior claim; or inability to keep historical
  GP-X1-001 evidence immutable returns to Curator.

## Rejected and deferred alternatives

Rejected: editing the table directly without a fresh authority intake;
rewriting the historical baseline-equivalent or offset-41 intakes; treating
the earlier table as accepted merely because it appears in Git; changing
`kMX1Table` or any route to make values appear consistent; reusing GP-X1-001
hardware evidence; folding the change into GP-CONFIG-010; or selecting a
Senscope modifier identity.

Deferred: a first real Senscope-to-Glyph binding remains a separate owner
decision and realization/source-authority record. The restoration can proceed
controller-locally without that binding because the owner has supplied exact
Glyph table authority.

## Publication and next action

Only this packet is published on its planning branch as a direct child of the
verified canonical base. Its immutable inventory contains one unique GP-ID
candidate heading. A fresh independent Curator must verify the exact packet,
live canonical source, owner direction, historical/current X1 correspondence,
scope, validation and physical gate; then publish an immutable receipt and
later canonical adoption before any Implementation Supervisor changes firmware.
This planning branch must never be merged as firmware implementation.
