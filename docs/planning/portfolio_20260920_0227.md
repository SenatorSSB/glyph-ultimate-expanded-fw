# Glyph Portfolio Candidate Packet — 2026-09-20 02:27 EEST

This is an immutable, non-authoritative Revision-2 Planner publication. It
records candidate supply for independent Work-Order Curator review only. It
does not authorize implementation, change the canonical queue, create a
`READY` or `PREAUTHORIZED` item, alter firmware behavior, or claim hardware
validation.

```yaml
packet_id: glyph-portfolio-20260920-0227
packet_state: FRESH
planning_branch: planning/portfolio-20260920-0227
base_configurator_sha: c4a4ddfb82b2ec7b650202be427ef5797f5092fe
base_live_verified: true
previous_planner_packet: glyph-portfolio-20260919-2050
previous_packet_commit: 4e38028b97ed07e894368564b9f9cdce87ba9c55
previous_packet_freshness: CONSUMED
candidate_count: 6
new_candidate_count: 6
materially_refined_candidate_count: 0
carried_forward_without_new_evidence_count: 0
current_ready_work_order_count: 0
curator_review_required: true
global_wait_proposed: false
material_events_since_publication: []
planner_authority: NON_AUTHORITATIVE_CANDIDATE_SUPPLY_ONLY
configurator_changed: false
```

## Audit basis and freshness

This audit is anchored at clean canonical
`c4a4ddfb82b2ec7b650202be427ef5797f5092fe`. The initial ordinary
`fetch`/`ls-remote` attempt immediately before branch creation failed with
sandbox DNS resolution. The required read-only network-enabled retry then
verified local `configurator`, `origin/configurator`, `HEAD`, and live
`refs/heads/configurator` at that exact SHA and confirmed that no
`planning/portfolio-20260920-*` branch existed remotely. All other registered
worktrees were prunable missing-gitdir records; no legitimate concurrent
canonical writer was found.

Packet `glyph-portfolio-20260919-2050` is materially `CONSUMED`, not merely old
or SHA-drifted. Its six authorized candidates are now `DONE`: GP-SRC-007,
GP-VAL-019, GP-PROV-011, GP-CONFIG-008, GP-SRC-008, and GP-VAL-020. The queue
records zero Ready, zero recorded or mechanically activatable Preauthorized,
zero invalidated Preauthorized, zero hardware-pending, and effective authorized
runway zero against target four. Its curation obligation is resolved and no
survivor remains. GP-PROV-010 is still research-gated in canonical prose,
GP-VAL-018 is still user-decision-gated, and GP-VAL-011 remains
`REVIEW / OWNER_DEFERRED / NONEXECUTABLE` under GLYPH-UD-013. None is silently
republished as fresh supply here.

## Broad audit scope and result

The broad refresh covered queue and branch history; runtime/source publication;
source-owned generation, extraction, adapter, and authority-intake seams;
configurator transaction and display paths; persistence research and fault
boundaries; build identity and dependency/provenance records; CI validation,
build, postprocessing, sidecar, and publication ordering; artifact custody and
hardware correspondence; checker census, manifest, health, fixtures, and
source-dependency policy; current hardware evidence; user direction; and
forbidden runtime/device-write paths.

Current framework, navigation, checker-census, and validation-health checks
pass with 202 census entries and 41 manifest entries. The full aggregate still
stops in the already owner-deferred GP-VAL-011 preflight on ignored nested
`.pio/libdeps/glyph_mk6/Adafruit_TinyUSB_XInput/`; this audit does not reopen or
reclassify it. No firmware build or hardware action was performed because this
Planner cycle is read-heavy and non-authoritative.

Fresh adversarial probes found independent gaps outside the owner-gated H2
layout lane:

- a noncanonical `fixture-intake` / `fixture-profile` packet with arbitrary
  `fixture-approval` authority and ownership of `kDefaultTable` receives no
  blocker, reports `production_emission_allowed: true`, and emits a production
  changeset;
- masked or unreachable canonical CI build, artifact-copy, and trusted-base
  commands are accepted by the current workflow checkers;
- source extraction is unchanged when the active generated adapter mapping is
  mutated from `SOURCE_OWNED_GENERATED_TABLE(kX1Table, 2)` to index `3`, so the
  raw generated rows can remain valid while compiled table-symbol
  correspondence drifts;
- the current menu path contains source-backed null, empty-page, and
  index-equals-count states that are not characterized by an exact-production
  host harness; and
- the persistence ledger identifies concrete unchecked SaveConfig fault
  windows, but no executable exact-production fault harness records their
  outcomes.

The dominant engineering bottleneck remains exact correspondence. Several
current gates validate a declared record, command, or raw generated array
without proving that it is the exact authority, executable operation, compiled
adapter mapping, or runtime state claimed. The separate product bottleneck is
unchanged: a new modifier/layout realization still needs owner/Senscope values,
owned table/routing scope, and expected outputs.

## Horizon A — immediate authority, build, and source-correspondence candidates

### GP-SRC-009 — Fail closed on noncanonical production authority

- **Objective/value:** Prevent unreviewed intake identities and arbitrary
  nonempty locator strings from authorizing a production source-owned
  changeset after GP-SRC-008 purported to close the accepted corpus to the
  exact X1/GLYPH-UD-010 mapping.
- **Current source evidence:**
  `tools/source_owned_source_authority_intake.py::_closed_canonical_x1_mapping`
  returns without a blocker for a noncanonical identity that does not reuse the
  canonical locator. The generic approval checks then accept nonempty strings.
  The current checker deliberately constructs `fixture-intake`,
  `fixture-profile`, `fixture-approval`, and
  `user-source-authority-reference`; direct review reports no blockers and
  `production_emission_allowed: true`, and emission owns `kDefaultTable`.
  GP-SRC-008's canonical work order says only the closed current-corpus mapping
  is accepted and unsupported locators fail closed.
- **Implementation seam:** Close production emission to the one currently
  resolved canonical X1 corpus record. Preserve draft/review and synthetic
  test construction only through explicitly non-production helpers or
  operations. A future production authority entry requires separate reviewed
  registry/resolver authority.
- **Scope/exclusions:** Intake validator, manager/checker, fixtures, locator
  documentation, and deterministic manifest/census/health consequences only.
  No new authority, generic anchor grammar, new table ownership or values,
  source install, active firmware, build, device, or hardware action.
- **Dependencies:** Preserve the current 28-table baseline, exact
  GLYPH-UD-010/kX1Table/nine-point correspondence, stale-baseline rejection,
  overlay/preserve semantics, and downstream production gates.
- **Validation:** For `requested_operation=production_changeset`, noncanonical
  identities, arbitrary locators, multi-table overlays, and full replacements
  fail production emission with stable authority blockers through direct API
  and CLI paths. The exact canonical X1 record with current-baseline
  substitution remains accepted as a `NO_OP`. `source_equivalence_proof`,
  draft/review helpers, and synthetic tests retain their current non-production
  use but must never set `production_emission_allowed`. Run intake, generator, manifest,
  census, health, framework, navigation, surface, syntax, and independent
  review gates.
- **Risk/gates/confidence/readiness:** H1 source-authority integrity; no build
  or hardware; high confidence; `CURATION_READY`.

### GP-VAL-021 — Seal the CI build-to-upload command chain

- **Objective/value:** Require the actual firmware build, output-directory,
  artifact copy, postprocessing, sidecar write/verification, and final upload
  boundary to be exact, unique, unconditional, failure-bearing, and ordered so
  verified bytes cannot be changed before publication.
- **Current source evidence:** The artifact workflow checker accepts
  `pio run -e "$PIO_ENV" || true`, artifact `cp ... || true`, and a build inside
  `if false; then ...; fi` because build discovery is substring-based and
  GP-VAL-019's protected set begins at checkout/postprocessing. Independent
  probes also show that both a new step and an extra command in the sidecar
  step can append to `$ARTIFACT_PATH` after `--verify-sidecar` while the
  artifact checker still passes.
- **Implementation seam:** Extend the bounded workflow parser/checker contract
  to the exact current `pio`, output-directory, copy, and sidecar commands;
  require the reviewed sidecar step's exact command set and make verified
  sidecar completion immediately dominate the one upload step with no
  intervening executable mutation surface.
- **Scope/exclusions:** Workflow parser/checkers, focused in-memory adversarial
  cases, documentation, and deterministic manifest/census/health consequences.
  The current workflow commands and YAML remain unchanged. No dependency pin,
  runner, build input, postprocessor behavior, artifact bytes/store/custody,
  firmware, device, hardware, or GP-VAL-011 work.
- **Dependencies:** Preserve GP-VAL-016 aggregate-command integrity,
  GP-VAL-019's four protected commands, current default GitHub shell contract,
  and the observed-only sidecar classification.
- **Validation:** Reject masking, trailing success, conditional, function,
  subshell, heredoc, duplicate, renamed, wrong-field, wrong-directory,
  post-verification mutation, intervening step, and alternate-upload cases for
  every protected operation. Accept the exact current workflow. Run both
  workflow checkers, parser tests, provenance, manifest/census/health,
  framework, navigation, surface, syntax, and independent review.
- **Risk/gates/confidence/readiness:** H0 static CI/checker hardening; no build
  or hardware; high confidence; `CURATION_READY`.

### GP-VAL-022 — Validate every generated adapter mapping

- **Objective/value:** Prove that every active table symbol and every point/axis
  expansion in `UltimateIdentityRuntimeTables.hpp` maps to the intended row and
  coordinate of the generated 28-table baseline instead of validating only the
  raw array contents.
- **Current source evidence:**
  `extract_glyph_identity_runtime_tables.py` parses the raw generated array and
  normalizes it by embedded row comments, but does not parse the 28
  `SOURCE_OWNED_GENERATED_TABLE(symbol,index)` invocations or the nine
  `SOURCE_OWNED_GENERATED_TABLE_POINT(index,point)` x/y expansions. The current
  symbol-map checker anchors only `kDefaultTable, 0`. Replacing the live
  adapter line for `kX1Table` from index `2` to `3` leaves extracted tables
  byte-for-byte unchanged.
- **Implementation seam:** Add one load-bearing exact parser or bounded host
  compile probe for all symbol-to-index and point-to-axis mappings, then make
  the extractor/source-sync and generated-baseline gates consume that proved
  correspondence without duplicating table authority.
- **Scope/exclusions:** Extractor and/or one focused static/host checker,
  adapter/source-sync fixtures and documentation, plus deterministic
  manifest/census/health consequences. No table value, symbol order, active
  mapping, generated array, runtime publication, build selector, firmware
  behavior, device, or hardware change.
- **Dependencies:** Preserve the current 28-symbol order and semantic digest,
  source-owned active publication, and exact X1/Y2 hardware evidence. The gate
  validates current C++ correspondence; it does not invent table semantics.
- **Validation:** Reject swapped, duplicate, missing, out-of-range, and skipped
  table indices; wrong point indices; x/y inversion; wrong raw array/namespace;
  extra/duplicate macro invocations; and malformed expansion shape. Accept the
  exact current adapter and retain existing source-sync/generator results.
  Run focused checker/extractor, current X1, source sync, generated baseline,
  manifest/census/health, framework, navigation, surface, syntax, and fresh
  independent review.
- **Risk/gates/confidence/readiness:** H1 active-source correspondence checker
  only; no active bytes change and no build or hardware; high confidence;
  `CURATION_READY`.

### GP-VAL-023 — Make trusted comparison-base setup failure-bearing

- **Objective/value:** Ensure CI validation runs against the intended fetched
  immutable comparison base rather than accepting a masked, skipped, or
  unverified base-setup sequence.
- **Current source evidence:** The validation-publication checker requires
  substrings for `git fetch --no-tags origin`, `test -n
  "$GLYPH_CHECKER_BASE"`, and `git rev-parse --verify
  "$GLYPH_CHECKER_BASE"`. It accepts `|| true` variants and an unreachable
  conditional fetch/verification while the aggregate step itself remains
  exact.
- **Implementation seam:** Extend the reviewed workflow-step contract to
  require the exact unique trusted-base fetch, nonempty-base assertion, and
  immutable `rev-parse --verify` operations in fixed order and as
  unconditional failure-bearing commands.
- **Scope/exclusions:** Shared bounded parser, validation-publication checker,
  focused adversarial fixture/cases, and deterministic manifest/census/health
  consequences only. No workflow YAML, remote selection, event/permission,
  runner, aggregate, network behavior, Git credential, build, artifact,
  firmware, device, hardware, or GP-VAL-011 change.
- **Dependencies:** Preserve existing pull-request base versus
  `origin/configurator` expression semantics and GP-VAL-016's exact aggregate
  command gate.
- **Validation:** Reject masking, trailing success, conditional, duplicate,
  reordered, wrong-ref, wrong-field, shell override, function, subshell,
  heredoc, continuation, and comment-disguise cases for each base operation;
  accept the exact current workflow; run publication/artifact workflow,
  manifest/census/health, framework, navigation, surface, syntax, and
  independent review.
- **Risk/gates/confidence/readiness:** H0 static CI trust-boundary hardening; no
  build or hardware; high confidence; `CURATION_READY`.

## Horizon B — bounded runtime and persistence characterization

### GP-CONFIG-009 — Characterize config-menu invalid states

- **Objective/value:** Establish deterministic source-backed behavior for
  null-current-mode, zero-item-page, and index-equals-count menu states before
  selecting a firmware repair or claiming physical controller impact.
- **Current source evidence:** `ConfigMenu::HandleControls` subtracts one from
  `items_count` without an empty-page guard and uses `>` rather than `>=` before
  indexing the selected item. `DefaultConfigMenu` dereferences
  `_backends[0]->CurrentGameMode()` while `CommunicationBackend` initializes
  its mode to null. SetConfig accepts `default_mode_config == 0`; backend boot
  selects a mode only when the field is greater than zero; the Glyph config
  menu is nevertheless constructed in setup.
- **Implementation seam:** Build an exact-production host characterization for
  the relevant menu bodies and minimal source-backed backend/config state,
  using sanitizers or equivalent deterministic bounds/null assertions.
- **Scope/exclusions:** Host harness, fixtures, characterization docs/checker,
  exact-source drift binding, and deterministic manifest/census/health
  consequences only. No firmware/UI behavior fix, default selection,
  SetConfig validation change, persistence/protocol change, gameplay decision,
  device access, or hardware claim.
- **Dependencies:** Preserve GP-CONFIG-005 transaction behavior and
  GP-CONFIG-008 rebinding observations. Treat physical display/controller
  consequences as unknown. Any repair needs a separately curated expected
  empty/no-mode UI contract.
- **Validation:** Characterize null current mode, zero backend/mode options,
  index equal to count, up/down/enter/back paths, and setup construction with
  exact production bodies. Fail on source drift. Run existing SetConfig and
  rebinding checks, host sanitizer/assertion cases, manifest/census/health,
  framework, navigation, surface, syntax, and independent review.
- **Risk/gates/confidence/readiness:** H1 host characterization; no firmware
  build or hardware; high source-gap confidence and medium harness confidence;
  `CURATION_READY` for characterization only. A repair is H2/H3 and separately
  evidence-gated.

## Horizon C — evidence-gated persistence research

### GP-PERSIST-003 — Execute the SaveConfig fault-window ledger

- **Objective/value:** Turn the existing source-level SaveConfig operation
  ledger into deterministic exact-production host observations so later
  recovery design is based on reproduced outcomes rather than inference.
- **Current source evidence:** `HAL/pico/src/core/Persistence.cpp::SaveConfig`
  checks open and encode success but does not check the placeholder-header
  write count, body/header seeks, final header rewrite count, or close/sync
  status. The canonical persistence research packet already lists these
  windows but does not execute them against production bodies.
- **Implementation seam:** Create a bounded host fault-injection harness around
  the exact SaveConfig/LoadConfig/CheckSavedConfig bodies and the current
  LittleFS file-open/read/write/seek, CRC, and protobuf seam, recording return
  value and resulting file shape at each named cut point. Constructor-level
  mount behavior is outside this slice.
- **Scope/exclusions:** Research harness, fixtures, docs/checker, exact-source
  binding, and deterministic manifest/census/health consequences. No firmware
  fix, atomicity/recovery guarantee, file-format migration, boot/default/
  autoformat policy, wear claim, cross-core guarantee, device access, or
  hardware result.
- **Dependencies:** Curator must confirm that the existing closed operation
  ledger is sufficient authority for the proposed host fault model. Any
  corrective persistence architecture remains separately user/architecture
  gated and H3.
- **Validation:** Inject open failure, short placeholder/final header writes,
  encode failure, seek failures, and short/error reads; record deterministic
  return and file-state observations; record close/sync status only as ignored
  or unobservable where the exact wrapper exposes no injectable result; bind
  exact production sources; preserve explicit non-claims. Run current
  persistence and SetConfig characterization, manifest/census/health,
  framework, navigation, surface, syntax, and independent review.
- **Risk/gates/confidence/readiness:** H1 host research only; no firmware build
  or hardware; high source-gap confidence, medium harness/authority confidence;
  `RESEARCH`.

## User-direction and evidence effects

- GLYPH-UD-001/005 preserve the Glyph/Senscope split. No candidate invents
  modifier, layout, routing, or game semantics.
- GP-SRC-009 enforces the exact already-recorded GLYPH-UD-010/kX1Table mapping;
  it creates no new human authority and approves no new table or value.
- GLYPH-UD-013 keeps GP-VAL-011 owner-deferred and nonexecutable. None of the CI
  candidates changes aggregate isolation, ignored-state proof, budgets, or
  runner architecture.
- GLYPH-UD-015 preserves exact source/build/artifact correspondence but does
  not authorize release, external storage, upload changes, flashing, or device
  write.
- GLYPH-UD-016 means the consumed prior packet correctly returned to planning.
  This new material packet creates a fresh Curator-review obligation only when
  published; it does not authorize its own candidates.
- GP-CONFIG-005 and GP-X1-001 exact hardware PASS records remain unchanged.
  No candidate reinterprets hardware evidence. Nunchuk remains `NOT_TESTED` and
  root cause remains unproven.

## Rejected and held alternatives

| Alternative | Disposition | Evidence-based reason |
| --- | --- | --- |
| Reopen GP-VAL-011 | REJECT | GLYPH-UD-013 makes the aggregate isolation repair owner-deferred and nonexecutable. |
| Republish GP-PROV-010 unchanged | HOLD | The prior Curator already found completed-build binding, package enumeration, generated-metadata policy, and bounded content closure unresolved; this audit found no source-backed architecture that resolves them. |
| Re-adjudicate GP-VAL-018 | HOLD | The owner/Senscope tie/conflict choice is unchanged and no new direction arrived. |
| Immediate SetConfig live-rebind repair | SUBSTANTIVE_DEPENDENCY_GATED | Reboot versus in-place reinitialization, cached masks, backend/display/RGB rebinding, and cross-core publication remain unresolved. |
| Treat a failed SetConfig success acknowledgment as a simple retry fix | SUBSTANTIVE_DEPENDENCY_GATED | Persistence/live publication may already have succeeded; retry/idempotency and protocol outcomes require a decision. |
| Implement atomic persistence/recovery | REJECT | GP-PERSIST-003 is observation only; old-or-new guarantees, boot/default/autoformat, compatibility, diagnostics, concurrency, wear, and recovery UX remain unresolved H3 decisions. |
| Promote action/dependency pinning or reproducible builds | REJECT | Current provenance is observational; selector/security/update policy and complete content closure remain unresolved. |
| Repair inert installer symlink/atomic-write behavior now | HOLD | The potential gap overlaps completed GP-SRC-001/005 scope and needs exact history/authority reconciliation before being fresh supply. |
| Harden GP-CONFIG-008 fixture prose ahead of active adapter correspondence | HOLD | Unchecked fixture fields exist, but GP-VAL-022 protects the active compiled mapping and is the larger current correctness bottleneck. |
| Recover old planning, curation, result, or failed branches | REJECT | They are integrated, consumed, superseded, historical evidence, or explicitly nonexecutable; branch names do not authorize work. |
| Activate candidate/active-storage/runtime-loaded/device-write paths | REJECT | These paths remain forbidden and active-storage has separate hardware-failure evidence. |
| Add modifier/table values or infer gameplay semantics | REJECT | Owner/Senscope source authority is absent. |
| Propose portfolio-global evidence wait | REJECT | Five independent `CURATION_READY` candidates exist outside the owner-gated H2 and persistence-repair lanes. |

## Global wait assessment

`global_wait_proposed: false`. The fresh broad audit found independent
source-authority, CI trust, active-source correspondence, and host runtime
characterization work that does not depend on owner/Senscope modifier data or
new physical testing. Owner/Senscope delivery of exact layout values, owned
table/routing scope, and expected outputs remains the event that can resume the
next H2 product lane, but that candidate-local gate does not establish
portfolio-global scarcity.

## Curator handoff

The Work-Order Curator must independently review this exact packet as:

- packet ID `glyph-portfolio-20260920-0227`;
- planning branch `planning/portfolio-20260920-0227`;
- base `c4a4ddfb82b2ec7b650202be427ef5797f5092fe`;
- exact immutable planning commit supplied by the published branch;
- initial inventory of exactly six candidate headings;
- `curator_review_required: true`;
- `global_wait_proposed: false`.

No score, ranking, or readiness estimate above is authorization. The Curator
must independently verify every candidate, choose dispositions, create the
required immutable receipt, and adopt any queue state through the separate
canonical curation sequence.
