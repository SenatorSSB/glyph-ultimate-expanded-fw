# Glyph Portfolio Candidate Packet — 2026-08-23 14:50 EEST

```yaml
packet_id: glyph-portfolio-20260823-1450
packet_state: FRESH
planning_branch: planning/portfolio-20260823-1450
base_configurator_sha: 7688ee287491ff05898038045f5c1918be09f675
base_live_verified: true
previous_planner_packet: glyph-portfolio-20260823-0152
previous_packet_branch: planning/portfolio-20260823-0152
previous_packet_commit: 6a21c4f442f3de6fe2da42094dbdc32f68c95d2a
previous_packet_freshness: PARTIALLY_CONSUMED_STILL_USEFUL
candidate_count: 9
new_candidate_count: 3
carried_forward_candidate_count: 6
curator_review_required: true
global_wait_proposed: false
material_events_since_publication: []
planner_authority: NON_AUTHORITATIVE_CANDIDATE_SUPPLY_ONLY
configurator_changed: false
```

## Result and freshness

This is a material refresh, not a duplicate publication caused by time or SHA
drift. The preceding packet remains useful, but three of its candidates have
shipped and one shipped guardrail changed an assumption that the current v2
generator lane was fail-closed:

- `GP-SRC-001`, `GP-CONFIG-001`, and `GP-CONFIG-003` are now `DONE` on
  `configurator`.
- `GP-PROV-001`, `GP-VAL-001`, and `GP-SRC-002` are canonically `READY`.
- Effective authorized runway is three against the recorded target of four,
  with canonical liveness `RUNWAY_LOW`.
- The broad refresh showed that the completed `GP-SRC-001` implementation did
  not cover the reusable v2 prepare/install path promised by its authorized
  every-write-path scope. That path
  accepts the active table-content header as an install target and accepts a
  payload modified after preparation while retaining stale declared digests.
- Two independent control-plane gaps were also reproduced: queue prose can
  contradict machine-derived runway while the framework checker passes, and
  an H2/H3 queue item can reference a nonexistent hardware-evidence record
  without failing validation.

This is an incomplete-guardrail repair finding, not evidence that the shipped
GP-SRC-001 changes themselves altered runtime behavior. The old packet's
surviving validation, provenance, preview, source-authority,
and official-capture candidates remain useful. This packet adds three current
correctness/safety candidates, records their effect on the surviving preview
candidate, and carries the still-relevant successors and external gates
forward for one coherent Curator review. The total is three new candidates
plus six carry-forwards: three canonically Ready items and three prior
dependency/user/evidence-gated survivors. Carrying them forward is freshness
accounting, not new authorization or duplicate supply; `GP-SRC-002` is carried
with a material prerequisite warning.

No new hardware result, production-owned table delta, runtime-loaded
capability, device-write capability, Nunchuk result, or proven low-level root
cause was found.

## Live base and broad-audit evidence

Immediately before branch creation, `git fetch --prune origin` completed and
local `configurator`, `origin/configurator`, and the worktree `HEAD` all
resolved to `7688ee287491ff05898038045f5c1918be09f675`. The worktree was clean.
The planning branch was created directly from that SHA.

The refresh inspected:

- every required current governance, queue, user-direction, workflow,
  implementation-boundary, authorization/runway, and hardware-evidence
  document;
- the previous Planner packet, both Curator reviews, the three completed
  candidate lineages, their current queue dispositions, and recent live Git
  history;
- the active `Ultimate.cpp` include/publication path, the generated baseline
  table-content header, the interpreter, dormant candidate/active-storage
  scaffolds, and the current source-sync contracts;
- generator v2 modes, source-authority intake, preparation/install paths,
  layout-spec and coordinate-native offline bridges, fixtures, negative
  corpora, and candidate-generation guardrails;
- checker census, curated manifest, validation health and aggregate,
  framework/queue checks, docs navigation, historical checker classification,
  and branch-context enforcement;
- the current official-configurator five-check lane, manual-capture lane,
  official corpus/manifest/target/diff evidence, and quarantined external-
  remapper material;
- current and historical hardware evidence for the latest Y2 source-owned
  baseline, the source-aligned Alternative B alias, and the failed canonical-
  grid candidate;
- both GitHub workflows, PlatformIO/build tooling, `glyph_nuker` file identity,
  artifact publication/provenance, recovery and exact-snapshot handoff
  requirements, and forbidden persistence/device-write/flashing boundaries.

Observed verification on the live base:

- `python3 tools/run_glyph_runtime_config_validation.py --json`: PASS, 20
  current load-bearing checks.
- `python3 tools/check_glyph_checker_census.py`: FAIL; the committed census has
  185 entries while static discovery finds 186. The new official-configurator
  checker is absent, and eight tracked checker records have byte drift.
- `python3 tools/check_glyph_source_owned_candidate_generation.py`: FAIL; on
  `configurator`, the branch guard masks the expected active-table-path error.
- `python3 tools/check_glyph_agent_framework_docs.py`: PASS despite the
  machine/prose runway contradictions and nonexistent accepted synthetic
  hardware-evidence record.
- `python3 tools/check_glyph_docs_navigation.py`: PASS.
- `python3 tools/check_glyph_docs_agent_surface.py`: PASS.
- `python3 tools/check_glyph_checker_context.py`: PASS.
- `python3 tools/check_glyph_official_configurator_validation.py`: PASS, five
  current offline checks; no universal compatibility claim.
- `python3 tools/check_glyph_official_configurator_manual_capture_result.py`:
  PASS with zero completed captures.
- `python3 tools/check_glyph_reconstructed_source_authority_evidence.py`: PASS;
  production delta remains `NONE_AUTHORIZED`.
- `python3 tools/check_glyph_identity_runtime_table_source_sync.py`: PASS, 28
  tables; this is not a new hardware result.

Two non-mutating in-memory reproductions called `install_prepared(...,
dry_run=True)`. One accepted the exact active path
`src/modes/runtime_config/generated_source_owned/GeneratedRuntimeConfigBaseline.current.hpp`.
The other accepted an artifact whose first coordinate was modified after
preparation while the recorded artifact and prepared semantic digests remained
unchanged. A third accepted an arbitrary modified manifest-row action without
recomputing the manifest or prepared digest. No target was written.

No compiled firmware, workflow, `glyph_nuker`, or active-table bytes changed
between the preceding packet base and this base. The current firmware source
lineage remains the hardware-accepted source-owned baseline; the build tooling
was inspected, but a new firmware build was not needed for this docs-only
Planner packet.

## Largest current bottleneck

The largest bottleneck is **fail-open correspondence between declared safety
evidence and the paths/bytes/status it is supposed to protect**.

The v2 prepared-packet installer can target active compile-time source and can
accept payload bytes that no longer match their declared digests. Separately,
the runtime aggregate can pass over a stale checker inventory, canonical prose
can disagree with machine queue state, and a hardware-validated queue fixture
can point to no evidence file at all. These are not controller-behavior or game-
semantic gaps; they are trust-boundary gaps in source mutation, validation,
and exact-snapshot acceptance. They should be closed before broadening
publication or preview tooling.

## Horizon A — close current fail-open trust boundaries

### GP-SRC-003 — Complete prepared-packet integrity and output guardrails

- **Objective/value:** repair the incomplete GP-SRC-001 write-path coverage by
  making the reusable v2 prepare/install path enforce an exact prepared-packet
  schema, recompute and cross-verify every semantic claim, and refuse active or
  protected repository targets, so an offline-looking action cannot overwrite
  active source or install a tampered packet.
- **Current source evidence:** `validate_manifest()` compares declared fields
  but does not recompute table, artifact, row, manifest, or prepared-packet
  digests or validate the complete row schema/cross-correspondence.
  `install_prepared()` accepts any absolute path except three case-
  sensitive name fragments. `prepare --output` writes directly without the
  source-authority intake path policy. Non-mutating reproductions accepted the
  active baseline header, a post-preparation coordinate mutation with stale
  digests, and an arbitrary manifest-row action. Current docs claim the
  generator never edits `src/**` and verifies
  manifest digests. The focused checker omits the prepared digest and does not
  cover protected paths, aliases, case variants, symlinks, or recomputation.
- **Implementation seam:** one strict prepared-packet validator reused by
  prepare/install and later preview rendering; exact schema/key validation;
  rederive baseline, canonical table order, table digests, artifact digest,
  manifest rows/counts/classification/digest, and prepared digest; central
  resolved-path policy for preparation and install outputs; branch-independent
  negative coverage.
- **Scope/exclusions:** host tooling, docs, fixtures, and checkers only. No
  active-source write, table-byte change, source ownership decision, candidate
  branch, build, hardware artifact, RuntimeConfigView publication, runtime
  loading, persistence, device/protobuf write, flashing, or game semantics.
- **Dependencies:** `GP-SRC-001` is Done. This should precede or be explicitly
  reconciled with `GP-SRC-002` so the preview renderer does not build a second,
  narrower integrity implementation.
- **Validation:** tampered packet/artifact/manifest/row/digest and unknown-field
  corpus; active-header/protected-tree/case/alias/symlink target rejection;
  safe temporary output and atomic write coverage; candidate-generation safety,
  generator-mode, authority-intake, source-sync, full aggregate, and docs
  checks; repository and 28-table semantic digests unchanged.
- **Risk estimate:** H1 host-side source-mutation and integrity safety; no
  hardware gate when active bytes remain unchanged.
- **Hardware/evidence/user gates:** independent Curator authorization; stop on
  any compiled-source or table-byte delta. No user/domain decision is needed.
- **Confidence:** high.
- **Decision-aid score:** 98/100.
- **Non-authoritative readiness estimate:** `CURATION_READY`.

### GP-HW-001 — Hardware evidence-record correspondence gate

- **Objective/value:** ensure H2/H3 queue acceptance is backed by a resolvable,
  machine-validated evidence reference for the exact candidate, artifact,
  locator, protocol, result, and gaps rather than a nonempty string.
- **Current source evidence:** `validate_work_order()` checks H2/H3 SHA and
  locator shapes, but for PASS/FAIL/PARTIAL/INCONCLUSIVE it only requires a
  nonempty `hardware_evidence_record`. Its accepted `HARDWARE_VALIDATED`
  synthetic fixture uses `docs/evidence/test.md`, which does not exist. No
  current checker cross-validates a record's candidate SHA, artifact SHA-256,
  preserved locator, protocol version, result, or evidence gaps against the
  queue item.
- **Implementation seam:** first record accepted evidence-reference forms
  without assuming every record must be a worktree-local repository file.
  Candidate design inputs are a current-tree repo path or an immutable full
  evidence-commit-plus-path whose Git object/content is verified; any external
  form must be explicitly supported, immutable, and verifiable rather than an
  arbitrary string. Then add a Revision-2 structured result record/schema and
  read-only queue cross-reference validator for PASS, FAIL, PARTIAL, and
  INCONCLUSIVE.
- **Scope/exclusions:** no hardware execution or claim, artifact retrieval or
  execution, upload/store selection, legacy-evidence upgrade, device update,
  flashing, firmware/runtime source, or weakening of manual acceptance.
- **Dependencies:** the correspondence requirement is source-backed by
  `HARDWARE_EVIDENCE.md`, but the canonical evidence-reference representation
  is not explicit. Curator/architecture must resolve accepted reference forms.
  GP-HW-001 is independent of the already-Ready GP-PROV-001: the human result
  record and artifact-provenance sidecar are distinct even though they must
  agree on candidate/artifact/locator values.
- **Validation:** unresolved, missing, escaping, mutable branch-only, missing
  commit/object/path, or malformed references; malformed/duplicate record
  fields; candidate/artifact/locator/
  protocol/result/gap mismatch; PASS with gaps; partial/inconclusive without
  gaps; FAIL mismatch; complete repo-relative and any explicitly authorized
  external-reference synthetic cases. Framework, docs, and liveness tests
  remain green.
- **Risk estimate:** H0 checker/control-plane change; no physical test is
  required. The acceptance invariant is safety-critical even though the patch
  changes no product or firmware behavior.
- **Hardware/evidence/user gates:** accepted evidence-reference forms are a
  substantive dependency; then independent Curator authorization and review.
  It must not reinterpret historical pre-Revision-2 evidence or fabricate a
  current result.
- **Confidence:** high.
- **Decision-aid score:** 95/100.
- **Non-authoritative readiness estimate:**
  `SUBSTANTIVE_DEPENDENCY_GATED` on the reference-form decision.

### GP-CTL-001 — Machine/prose runway parity

- **Objective/value:** make canonical current-state and queue interpretation
  agree mechanically with queue JSON so operators do not schedule from stale
  runway prose.
- **Current source evidence:** machine state and `CURRENT_STATE.md` report
  three Ready items and runway 3/4; `AGENT_CONTEXT.md` says effective runway
  four, and `ACTIVE_AGENT_QUEUE.md` twice says four Ready items. The framework
  checker passes because it validates the machine block but not these mirrors.
- **Implementation seam:** derive or validate Ready/DONE/runway/liveness prose
  from the queue state in the Curator-owned framework checker, or remove
  manually duplicated numeric claims while preserving a readable current
  summary.
- **Scope/exclusions:** current governance docs and their narrow control-plane
  checker only. No queue authorization change, candidate promotion, product or
  runtime checker, firmware, source authority, hardware, or user-direction
  edit.
- **Dependencies:** none; preserve the canonical machine state as three Ready,
  zero Preauthorization, zero hardware pending, runway 3/4, `RUNWAY_LOW`.
- **Validation:** deliberate Ready/DONE/count/liveness drift in every mirrored
  surface fails; exact machine-derived state passes; framework, sequence,
  docs-navigation, and docs-agent-surface checks pass.
- **Risk estimate:** H0.
- **Hardware/evidence/user gates:** Curator review; no hardware or user/domain
  gate.
- **Confidence:** high.
- **Decision-aid score:** 92/100.
- **Non-authoritative readiness estimate:** `CURATION_READY`.

## Horizon B — preserve current runway and sequence successors

### GP-VAL-001 — Checker-census freshness as a load-bearing gate

- **Objective/value:** prevent the current aggregate from passing over stale
  checker discovery metadata.
- **Current source evidence:** the standalone census fails at 185 committed
  versus 186 discovered while the 20-check aggregate passes. The shipped
  official-configurator checker and eight byte-changed checkers are absent or
  stale in the census.
- **Implementation seam:** the existing canonical work order: deterministic
  regeneration, load-bearing census freshness, curated applicability retained,
  and adversarial drift coverage.
- **Scope/exclusions:** no automatic checker applicability, checker execution
  during census generation, historical promotion, CI wiring, product/runtime
  source, or hardware claim.
- **Dependencies:** `GP-SRC-001` is Done; current canonical status is `READY`.
- **Validation:** the canonical work-order validation list, including deliberate
  checker-set/byte drift that fails the aggregate.
- **Risk estimate:** H0.
- **Hardware/evidence/user gates:** canonical queue authority already exists;
  no hardware gate. This packet does not reauthorize it.
- **Confidence:** high.
- **Decision-aid score:** 94/100.
- **Non-authoritative readiness estimate:** `CURATION_READY` carry-forward
  estimate only; canonical `READY` authority is unchanged.

### GP-PROV-001 — Artifact postprocessor provenance research

- **Objective/value:** record what CI and the tracked postprocessor identity
  prove and add an inert full-SHA/final-hash sidecar verifier without claiming
  an immutable store exists.
- **Current source evidence:** `.github/workflows/build.yml` and `glyph_nuker`
  are unchanged. CI still names output with a short SHA, postprocesses it, and
  uploads no full candidate SHA or final artifact SHA-256 sidecar. The tracked
  binary hash remains
  `8c488005c1ae7676518a0f8e048ff7d2fb51b71b743fdb785aeed3d8cf9f56ae`;
  its purpose/effect remains unknown from repository authority.
- **Implementation seam:** the existing canonical static research and
  synthetic sidecar/verifier work order.
- **Scope/exclusions:** never execute `glyph_nuker` or a real firmware
  artifact; no workflow integration, upload, release, store selection,
  device write, flashing, or purpose/effect inference.
- **Dependencies:** none. GP-PROV-001 and GP-HW-001 share exact candidate/
  artifact/locator values but own distinct record types; neither should wait
  for or reshape the other. Current canonical status is `READY`.
- **Validation:** exact static identities, synthetic verifier corpus, docs
  navigation, and zero workflow/binary/artifact mutation.
- **Risk estimate:** H0 research.
- **Hardware/evidence/user gates:** canonical queue authority already exists;
  no hardware gate. Purpose/effect stays `UNKNOWN` without new authority.
- **Confidence:** high on the gap and bounded work.
- **Decision-aid score:** 90/100.
- **Non-authoritative readiness estimate:** `CURATION_READY` carry-forward
  estimate only; canonical `READY` authority is unchanged.

### GP-SRC-002 — Authorized v2-to-C++ review preview

- **Objective/value:** render a validated, authority-preserving prepared v2
  packet into inactive deterministic C++ review text.
- **Current source evidence:** the prepared artifact/manifest still lacks a
  C++ consumer and the old layout-spec renderer remains a separate contract.
  The new GP-SRC-003 evidence shows that the shared prepared-packet and target
  safety assumptions need reconciliation first.
- **Implementation seam:** the existing canonical renderer work order, reusing
  the strict validator and resolved-path policy from GP-SRC-003 rather than
  creating a parallel integrity implementation.
- **Scope/exclusions:** stdout or explicitly safe temporary review output only;
  no active/compiled source write, candidate, build, production authority,
  runtime loading, device write, persistence, or flashing.
- **Dependencies:** canonical queue status remains `READY`, which Planner does
  not change. Its recorded prerequisite says the generic write guardrails are
  current; the GP-SRC-003 reproductions make that prerequisite false. Curator
  must re-review and resequence or reissue GP-SRC-002 before execution because
  the work order expressly requires strict packet revalidation and path safety.
  Planner recommends no execution until that canonical reconciliation occurs
  and GP-SRC-003 has supplied the shared guardrail or Curator has authorized an
  equally complete repair.
- **Validation:** the canonical work-order corpus plus proof it uses the shared
  strict validator and cannot bypass the corrected install policy.
- **Risk estimate:** H1 inactive generator/tooling safety.
- **Hardware/evidence/user gates:** Curator decides whether to retain,
  resequence, narrow, or reauthorize the existing work order. Any source write
  stops.
- **Confidence:** high on continued value; medium on current sequencing.
- **Decision-aid score:** 82/100.
- **Non-authoritative readiness estimate:** `HOLD` pending Curator freshness
  review, without altering its canonical `READY` status.

### GP-VAL-002 — CI validation parity before artifact publication

- **Objective/value:** run the accepted fail-closed current validation before
  CI publishes firmware artifacts, with pull-request coverage and least-
  required permissions.
- **Current source evidence:** `.github/workflows/build.yml` remains push-only
  and builds/postprocesses/uploads without the census or current aggregate.
- **Implementation seam:** focused workflow validation before build/publication
  using the post-GP-VAL-001 commands; retain explicit current-vs-historical
  checker classification.
- **Scope/exclusions:** no branch-protection assumption, checker weakening,
  postprocessor remediation, release automation, runtime behavior, or device
  write.
- **Dependencies:** `GP-VAL-001` must be complete and green; fresh Curator
  judgment is required afterward. GP-PROV-001 is a separate provenance lane,
  not a dependency of validation parity.
- **Validation:** workflow syntax and permissions review; local command parity;
  negative publication gate proving stale/failed validation prevents upload.
- **Risk estimate:** H1 CI/publication safety; no controller hardware gate.
- **Hardware/evidence/user gates:** fresh Curator review after dependencies;
  no automatic activation is currently recorded.
- **Confidence:** high on the gap; medium on final workflow shape.
- **Decision-aid score:** 84/100.
- **Non-authoritative readiness estimate:** `SUBSTANTIVE_DEPENDENCY_GATED`.

## Horizon C — external authority and evidence supply

### GP-AUTH-001 — Source-authorized production table delta intake

- **Objective/value:** provide the first explicit production-owned table set
  and exact replacement content for the completed source-owned lane.
- **Current source evidence:** the reconstructed authority checker reports
  `NONE_AUTHORIZED`; the submitted empty baseline-equivalence intake is a
  no-op and cannot supply production value. Coordinate equality does not prove
  ownership or mapping.
- **Implementation seam:** user/Senscope-supplied version-1 intake with exact
  owned symbols, nine-point replacements, per-table authority references,
  production intent, approval evidence, acknowledgements, and current baseline
  identity.
- **Scope/exclusions:** Planner and Curator must not invent ownership, mapping,
  coordinates, game semantics, or approval. No candidate or hardware claim is
  created by intake alone.
- **Dependencies:** none for a human/Senscope authority intake. GP-SRC-003 and
  the reviewed preview path are downstream generation/preview/candidate
  prerequisites, not blockers to supplying authority evidence now.
- **Validation:** current intake validator/negative corpus, v2 manifest, exact
  table review; any later active delta needs build, immutable exact artifact,
  and H2/H3 physical PASS before merge.
- **Risk estimate:** H3 for eventual active behavior; intake itself is
  non-runtime evidence.
- **Hardware/evidence/user gates:** explicit user/Senscope source-authority and
  exact content.
- **Confidence:** high on the missing gate; unknown on future content.
- **Decision-aid score:** 72/100.
- **Non-authoritative readiness estimate:** `USER_DECISION_GATED`.

### GP-CONFIG-002 — Exact official-configurator manual capture

- **Objective/value:** replace bounded unknowns about app version and exact
  import/export acceptance with a new hashed operator record.
- **Current source evidence:** the current five-check offline lane passes, the
  manual-capture scaffold passes with zero completed captures, and
  `GP-CONFIG-003` removed only the host-metadata false failure. Exact live app
  version and route evidence remain absent.
- **Implementation seam:** operator-performed capture using the existing
  procedure, hashes, structural diff, and result checker.
- **Scope/exclusions:** no universal compatibility, product exporter, automated
  app interaction, semantic inference, firmware change, or device-write
  implementation.
- **Dependencies:** internal `GP-CONFIG-003` dependency is satisfied; an
  external operator with official configurator access is still required.
- **Validation:** manual-capture result checker plus corpus hashes and
  structural diff.
- **Risk estimate:** H1 evidence operation; no firmware hardware claim.
- **Hardware/evidence/user gates:** exact external operator capture.
- **Confidence:** medium-high.
- **Decision-aid score:** 60/100.
- **Non-authoritative readiness estimate:** `EVIDENCE_GATED`.

## User-direction and evidence effects

- `GLYPH-UD-001` keeps this portfolio inside Glyph/HayBox firmware/backend
  realization. No candidate decides Senscope-owned game semantics, datasets,
  solver behavior, or neutral profile intent.
- `GLYPH-UD-002` fixes live `configurator` and the canonical build boundary.
  All comparisons use the freshly fetched live SHA.
- `GLYPH-UD-003` makes GP-HW-001 and GP-PROV-001 safety-critical: a build,
  path string, or sidecar is not controller acceptance, and no hardware PASS
  may be fabricated.
- `GLYPH-UD-004` supports closing bounded H0/H1 trust boundaries before
  speculative runtime expansion.
- `GLYPH-UD-005` permits a future complete source-grounded H2/H3 work order to
  reach exact candidate publication autonomously, but it neither supplies the
  missing GP-AUTH-001 content nor waives physical PASS before merge.
- Latest Y2 hardware evidence supports only the accepted current source-owned
  baseline. Alternative B evidence supports the source-aligned alias shape
  while preserving the current publication path. Canonical-grid evidence is a
  failure and supplies neither replacement authority nor a low-level root
  cause. Missing legacy artifact identity cannot satisfy a new Revision-2
  gate. Nunchuk remains `NOT_TESTED`.

## Rejected or held alternatives

- **Treat GP-SRC-001 completion as proof that every v2 write path is safe —
  REJECT.** The live non-mutating reproductions directly contradict that
  assumption; GP-SRC-003 is a new correctness candidate, not a duplicate docs
  cleanup.
- **Fold all of GP-SRC-003 invisibly into GP-SRC-002 — REJECT.** The existing
  installer must be safe independently of whether a new renderer ships.
  Curator may coordinate implementation, but the safety invariant needs its
  own explicit scope and validation.
- **Make a one-time prose count correction without a parity invariant —
  REJECT.** It would fix today's 3/4 wording but leave the same recurrence path
  for the next queue transition.
- **Require only `Path.exists()` in the current worktree for hardware evidence
  — REJECT.** Evidence may be published on a separate evidence/control-plane
  commit; an immutable commit-plus-path must be resolvable without pretending
  every valid record is in the current tree.
- **Accept any nonempty external hardware-evidence string — REJECT.** An
  external form must be explicitly supported, immutable, and verifiable or it
  preserves the current fail-open condition.
- **Write an active header to demonstrate the installer issue — REJECT.** Dry-
  run and in-memory tampering already reproduce it without mutation.
- **Remove dormant candidate/active-storage structs from `Ultimate.cpp` now —
  HOLD.** They are compile-present but not called by the active path, and
  historical checkers reference them. First establish a source/build footprint
  and validation migration; any compiled-source delta needs separate risk and
  build judgment and may require hardware.
- **Repair every failing historical roadmap, readiness, parser, or
  prehardware checker — REJECT.** Current discoverability and applicability
  must be established first; green historical residue is not current evidence.
- **Repair or remove `.github/workflows/build-device-config.yml` — HOLD.** Its
  caller/ownership contract and `meta.yaml` source may be external; no current
  source-backed use decision was found.
- **Run `glyph_nuker` to infer its purpose or byte effect — REJECT.** File
  identity and workflow text do not authorize execution or a behavior claim.
- **Select or upload to an immutable artifact store now — REJECT.**
  `GP-PROV-001` expressly stops at observed-only research and a nullable
  unresolved locator; architecture/retention/credential policy remains a
  separate decision.
- **Add another generator mode, more coordinate-native fixtures, or another
  source-authority intake schema — REJECT as duplicate.** Current source-sync,
  modes, intake, converter, positive/negative corpus, bundle, and offline
  export checks pass outside the newly isolated prepared-packet gap.
- **Repair the canonical-grid candidate — REJECT.** It is a recorded
  `HARDWARE_FAIL`, lacks replacement authority, and must not merge.
- **Implement runtime-loaded config, persistence, WebSerial/device write,
  protobuf write, release upload, or flashing — REJECT under the current
  boundary.** Separate explicit approval, authority, architecture, and safety
  evidence are absent.
- **Claim Nunchuk coverage or a low-level root cause — REJECT.** Direct
  evidence is absent.

## Global evidence wait assessment

`GLOBAL_EVIDENCE_WAIT_SUPPORTED` is **not proposed**
(`global_wait_proposed: false`). The fresh broad audit looked outside the
user/evidence-gated candidates and found three new independent H0/H1
correctness/safety candidates, alongside three current authorized H0/H1 work
orders and a dependency-gated CI successor. External gates on production
table authority, official-app capture, and immutable artifact storage do not
establish portfolio-global scarcity.

## Curator handoff

This packet authorizes and implements nothing. Curator must independently
reproduce the GP-SRC-003 path/digest failures, verify the GP-HW-001 record gap
and GP-CTL-001 prose drift, and decide whether the canonical `GP-SRC-002`
work order remains fresh or must be resequenced/reissued. Planner does not edit
the queue or clear `curator_review_required`.

Suggested review order: GP-SRC-003, GP-HW-001, GP-CTL-001, then the freshness
of GP-SRC-002 and the dependency shape of GP-VAL-002. Existing canonical Ready
authority for GP-PROV-001 and GP-VAL-001 remains owned by the queue, not by this
packet.
