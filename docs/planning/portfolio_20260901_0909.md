# Glyph Portfolio Candidate Packet — 2026-09-01 09:09 EEST

```yaml
packet_id: glyph-portfolio-20260901-0909
packet_state: FRESH
planning_branch: planning/portfolio-20260901-0909
base_configurator_sha: 1977ef0d6ec1a65d02947a0b7dae2675c2e8228c
base_live_verified: true
previous_planner_packet: glyph-portfolio-20260831-1540
previous_packet_branch: planning/portfolio-20260831-1540
previous_packet_commit: 896a06c092bfa2f99339c944fceffda957e4478d
previous_packet_base: d94eb6d629f9e8e73e893971a3f47c4485cf17ee
previous_packet_freshness: MATERIALLY_CONSUMED
candidate_count: 13
new_candidate_count: 6
repair_reactivation_count: 2
carried_forward_candidate_count: 5
current_ready_work_order_count: 1
current_ready_work_order_ids: [GP-VAL-010]
material_events_since_publication: []
curator_review_required: true
global_wait_proposed: false
planner_authority: NON_AUTHORITATIVE_CANDIDATE_SUPPLY_ONLY
configurator_changed: false
```

## Result, freshness, and portfolio state

This is a material broad-audit refresh of live `configurator`, not a timer-only
or SHA-drift packet. The ordinary restricted live lookup first hit sandbox DNS;
the required network-enabled read-only retry succeeded, and the subsequent
fetch and live verification matched local `configurator`, `origin/configurator`,
and `FETCH_HEAD` at
`1977ef0d6ec1a65d02947a0b7dae2675c2e8228c`. This branch starts from that
exact clean commit. Prunable worktree records point to missing temporary
directories and are not current writers.

The previous packet is materially consumed. `GP-SRC-004`, `GP-VAL-003`,
`GP-VAL-009`, and `GP-CTL-001` shipped; follow-up curation then authorized
`GP-VAL-010` as the packet's final executable survivor. The canonical queue now
reports one Ready H0 item, zero recorded or mechanically activatable
Preauthorization, zero invalidated Preauthorization, zero hardware-pending
work, and effective authorized runway one against target four. Its primary
state is `RUNWAY_LOW`, and it explicitly requests a fresh broad Planner audit.

The dominant bottleneck remains **fail-open trust correspondence**, now most
clearly at executable workflow-step interpretation, structural active-source
publication enforcement, aggregate selection/execution, Planner/Curator
provenance, and local build identity. Current checks can pass when required
commands exist only as YAML comments, when an explicitly requested category
executes zero checks, when an active-publication function gains an alternate
return, or when duplicate JSON sidecar keys conceal contradictory identity.

`GLOBAL_EVIDENCE_WAIT_SUPPORTED` is not proposed. Effective runway is nonzero,
two new independent H0 repair candidates and one new independent H0 source-
contract candidate exist, and several additional research/dependency/user-gated
lanes remain. Candidate-local evidence and product gates do not establish
portfolio-global scarcity.

## Broad-audit evidence

The audit read the current agent/context/state/roadmap/workflow/boundary and
framework authority documents, the complete canonical queue, current user
direction, the previous packet and Curator dispositions, current hardware
evidence, source-authority intake, active runtime publication and table-source
surfaces, existing configurator/persistence paths, checker census, validation
manifest and health record, aggregate runner and adversarial fixtures, tracked
CI workflows, build-input provenance, local build wrappers, PlatformIO pre-build
script, repository history, branches, worktrees, and live remote refs.

Observed current facts on the exact base:

- the full runtime-config aggregate passes with checker census `194`, manifest
  entries `32`, current load-bearing checks `28`, and exclusions `37`;
- the full aggregate took about 60–68 seconds across repeated observations;
  `candidate_generation` alone took about 35–37 seconds;
- agent-framework, sequence, census, validation-health, aggregate-adversarial,
  docs-navigation, and agent-surface checks pass;
- `GP-VAL-010` has no local, tracking, or live feature ref and remains the sole
  untouched Ready item;
- `python3 tools/run_glyph_runtime_config_validation.py --category
  historical_evidence --json` returns `PASS` with `results: []`;
- a synthetic workflow mutation that replaces the executable current aggregate
  command with a YAML comment plus `true` is accepted by
  `check_glyph_runtime_config_validation_publication_workflow.py`;
- the same comment-only mutation is accepted for the artifact checkout-identity
  command by `check_glyph_artifact_postprocessor_workflow.py`;
- `check_glyph_artifact_postprocessor_provenance.py` accepts a real-sidecar JSON
  document with a bogus first and correct last duplicate `candidate_git_sha`;
- the active-publication symbol-map checker accepts a synthetic
  `GetActiveRuntimeConfigState()` block with a conditional alternate return so
  long as the approved pointer fragment remains and the two named forbidden
  member strings do not appear;
- the current source-owned table bodies still enter through
  `UltimateIdentityRuntimeTables.hpp`, and active publication remains
  `&kSourceOwnedCurrentBaselineRuntimeConfig` through
  `GetActiveRuntimeConfigState()` and `ResolveActiveRuntimeConfig()`;
- the documented fallback build chain
  `scripts/build-glyph-mk6-quiet.sh -> scripts/pio-local.sh` remains outside the
  declared build-entrypoint provenance record, while
  `builder_scripts/arduino_pico.py` performs unchecked Git reads and an
  unchecked process-global Git configuration write during pre-build;
- `ConfiguratorBackend::HandleSetConfig()` decodes directly into live `_config`,
  restores only after decode failure, and returns without restoration after
  later validation or save failure. A rejected update leaving changed live
  values visible is an inference from source, not proven device behavior;
- `Persistence::SaveConfig()` truncates and rewrites `config.bin` in place. The
  repository already documents that path as non-atomic; a correct device-
  specific recovery design is not established;
- changes since the previous packet base are control-plane/docs/checker changes
  only. No product source, workflow, build input, user direction, source-
  authority intake, hardware result, or active table byte changed;
- Y2 and Alternative-B PASS remain pre-Revision-2 legacy evidence for their
  accepted mechanisms only; the canonical-grid candidate remains
  `HARDWARE_FAIL`; Nunchuk remains `NOT_TESTED`; root cause remains unproven;
- runtime-loaded config, runtime-config storage, WebSerial/device write,
  protobuf binary write, backend runtime-config write, and flashing automation
  remain unimplemented and unauthorized.

## Horizon A — independent current-truth repairs

### GP-SRC-006 — Structurally bind active source-owned publication

- **Objective/value:** make the load-bearing source-owned checker enforce the
  exact approved initializer and resolver structure instead of accepting an
  allowed string fragment inside a materially different publication function.
- **Current source evidence:**
  `tools/check_glyph_source_owned_table_symbol_map.py` requires the baseline
  pointer and resolver return fragments, then rejects only `candidate.view` and
  `active_storage.view` in the two function bodies. A synthetic conditional
  alternate return is accepted. Current source at `src/modes/Ultimate.cpp`
  uses one static state with the baseline pointer,
  `RuntimeConfigSource::SourceOwnedBaseline`,
  `RuntimeConfigActivationStatus::SourceOwnedSelected`, and one return.
- **Implementation seam:** the existing symbol-map checker and isolated
  temporary-source adversarial fixtures only; parse and require the exact
  current initializer members and single-return resolver shape.
- **Scope/exclusions:** no firmware/source edit, table byte, routing, schema,
  build input, active-view replacement, candidate/storage publication,
  runtime-loaded config, parser, device, or hardware change.
- **Dependencies:** Curator binds the exact currently accepted function shape;
  any real source topology change invalidates this checker-only candidate.
- **Validation:** alternate/conditional pointers, extra returns, mismatched
  source/status enums, parser/load-state reads, wrapper publication, RAM-backed
  views, and indirect resolver paths fail; exact current source passes with the
  full aggregate and source-sync suite.
- **Risk/gates/confidence/readiness:** H0; no hardware, external-evidence, or
  user gate; high confidence; `CURATION_READY`.

### GP-VAL-002 repair/revalidation — Prove executable validation-before-publication

- **Objective/value:** complete the accepted CI-publication gate by proving the
  aggregate command exists as an executable step and actually dominates every
  `CURRENT_GATED` local build/postprocess/upload route in
  `.github/workflows/build.yml`, not merely as raw YAML text. Preserve the
  canonical `UNRESOLVED_EXTERNAL` status of nested `config/glyph` workflow
  routes rather than claiming local dominance over them.
- **Identity/repair rationale:** the accepted GP-VAL-002 objective already
  claimed validation-before-publication. The executable-step defect is an
  incomplete acceptance of that same objective, so a new ID would falsely
  record the original objective as complete.
- **Current source evidence:**
  `tools/check_glyph_runtime_config_validation_publication_workflow.py` uses
  substring and regex scans of raw job text. Replacing the validation `run:`
  command with a comment containing the exact command plus `true` is accepted.
- **Implementation seam:** add a bounded fail-closed parser for the repository's
  current job/step/`run`/`uses` subset, use it in this checker, and add isolated
  comment, block-scalar, folded-scalar, duplicate-step, job-order, and
  alternate-route adversarial cases. The parser must reject unsupported shapes
  rather than infer execution.
- **Scope/exclusions:** no workflow YAML change, trigger/permission/branch-base
  decision, checker applicability, build, upload, artifact, product/runtime,
  device, or hardware change.
- **Dependencies:** current workflow shape and `GP-VAL-010` command identity
  remain source-correspondent; shared-parser scope must be reviewable without a
  new external YAML dependency or workflow execution.
- **Validation:** commented or inert commands, wrong job/step, duplicate or
  shadowed `run`, permissive failure handling, missing `needs`, and unguarded
  `CURRENT_GATED` local publication routes fail; exact current workflow passes;
  `UNRESOLVED_EXTERNAL` nested routes remain explicitly unresolved; static
  tests execute no workflow, build, postprocessor, upload, or device action.
- **Risk/gates/confidence/readiness:** H0; no hardware or user gate; high
  confidence; `CURATION_READY`.

### GP-PROV-002 repair/revalidation — Bind executable sidecar route and unique JSON identity

- **Objective/value:** complete the observed-only artifact-sidecar contract by
  proving its identity/generation/verification steps are executable workflow
  steps and its real JSON sidecar has unique keys.
- **Identity/repair rationale:** the accepted GP-PROV-002 objective already
  claimed exact sidecar generation and verification on the observed local
  route. These executable-step and duplicate-key defects are incomplete
  acceptance of that same objective, so a new ID would falsely record the
  original objective as complete.
- **Current source evidence:**
  `tools/check_glyph_artifact_postprocessor_workflow.py` orders raw substrings,
  so a comment-only checkout-identity command plus `true` is accepted.
  `tools/check_glyph_artifact_postprocessor_provenance.py::load_object()` uses
  ordinary `json.loads`; a duplicate bogus-then-correct `candidate_git_sha`
  is accepted by real-sidecar verification.
- **Implementation seam:** reuse the reviewed executable-step parser from the
  GP-VAL-002 repair, add duplicate-key-rejecting JSON load to the provenance
  tool, and extend synthetic workflow/sidecar adversarial fixtures.
- **Scope/exclusions:** no workflow YAML, postprocessor bytes/invocation,
  artifact generation/upload/store, immutable-locator, reproducibility,
  acceptance, firmware/runtime, device, or hardware change or claim.
- **Dependencies:** GP-VAL-002's exact shared executable-step parser is
  canonically integrated; the current sidecar schema and observed-only fields
  remain unchanged.
- **Validation:** comment-only, inert, reordered, wrong-job, and shadowed
  workflow steps fail; duplicate top-level and nested keys fail before identity
  comparison; exact current workflow and deterministic sidecar pass.
- **Risk/gates/confidence/readiness:** H0; no hardware or user gate; high
  confidence; `PREAUTHORIZABLE` after GP-VAL-002.

### GP-VAL-012 — Reject zero-execution category success

- **Objective/value:** prevent an explicit category request from returning
  aggregate `PASS` when no current checker was selected or executed.
- **Current source evidence:** the manifest category vocabulary includes
  `historical_evidence`, but selection always filters to `applicability ==
  current`; `all([])` then yields `PASS`. The exact live command with
  `--category historical_evidence --json` returns zero results and success.
- **Implementation seam:** aggregate runner selection validation and isolated
  adversarial fixtures. Bind categories to current-executable membership and
  require every explicitly requested category to select at least one current
  entry; if any requested category selects zero current entries, fail the whole
  request. Retain unfiltered current aggregate behavior.
- **Scope/exclusions:** no promotion or execution of historical/unsafe checks,
  manifest applicability or category reclassification, checker semantic edit,
  workflow, build, runtime, or hardware change.
- **Dependencies:** GP-VAL-010's exact command-correspondence repair is
  canonically integrated; runner/manifest category shape is otherwise stable.
- **Validation:** historical-only, excluded-only, unknown, duplicate, and mixed
  requests containing any zero-result category fail accurately (for example,
  `baseline` plus `historical_evidence` fails because the latter is empty);
  every requested current nonempty category executes its exact entries;
  unfiltered aggregate still runs all 28 current checks.
- **Risk/gates/confidence/readiness:** H0; no hardware or user gate; high
  confidence; `PREAUTHORIZABLE` after GP-VAL-010.

## Horizon B — trust architecture and build-boundary hardening

### GP-PROV-003 repair/revalidation — Census documented local build entrypoints

- **Objective/value:** finish the accepted declared build-input provenance
  boundary by recording the documented canonical/fallback local wrapper chain
  without executing it or claiming resolved interpreter identity.
- **Current source evidence:** `AGENTS.md` and `docs/WORKFLOW.md` prescribe
  `pio run -e glyph_mk6` and allow `scripts/build-glyph-mk6-quiet.sh`; the
  fallback invokes `scripts/pio-local.sh`, which selects local/ambient Python.
  The current inventory covers eight declarations but omits both wrappers.
- **Implementation seam:** schema-v2 provenance representation for local
  entrypoints, exact tracked modes/hashes and call chain, documentation, and
  deterministic manifest/census/health consequences. Preserve the existing
  selector inventory and the immutable historical GP-PROV-004 observation
  base/blob correspondence.
- **Scope/exclusions:** no wrapper/build/environment/selector/workflow change;
  no execution, dependency closure, interpreter identity, reproducibility,
  artifact acceptance, device, or hardware claim.
- **Dependencies:** Curator binds schema v2 and exact GP-PROV-004 current-versus-
  historical preservation/revalidation strategy before authorization.
- **Validation:** missing/changed wrapper path, mode, hash, command chain,
  canonical/fallback role, or unresolved ambient selection fails; old
  observations remain exact historical evidence; no shell/build tool executes.
- **Risk/gates/confidence/readiness:** H0; substantive representation dependency;
  high gap confidence; `SUBSTANTIVE_DEPENDENCY_GATED`.

### GP-VAL-011 — Isolate and time-bound aggregate execution

- **Objective/value:** make current aggregate execution incapable of mutating
  or hanging the canonical worktree instead of trusting declarative
  `mutation_risk` metadata.
- **Current source evidence:** every current checker executes with `cwd=ROOT`,
  inherited environment, and no timeout; no before/after HEAD, index, tracked,
  untracked, ignored, or dirty-state proof exists. One current checker consumed
  about 35–37 seconds in this audit.
- **Implementation seam:** exact disposable Git snapshot execution,
  branch/base/dirty-state representation, controlled environment, per-check and
  aggregate time budgets, canonical-worktree immutability proof, and isolated
  adversarial fixtures.
- **Scope/exclusions:** no checker reclassification or semantic edit; no
  network, dependency installation, build, workflow execution, artifact,
  device, firmware/runtime, or canonical write authority.
- **Dependencies:** GP-VAL-010 is integrated; Curator binds snapshot source,
  Git object availability, dirty-state policy, branch/base semantics,
  environment allowlist, and timeout budgets.
- **Validation:** tracked/staged/untracked/ignored/index/HEAD mutations remain
  confined and fail; a hanging checker times out with exact identity; current
  branch/base checks remain meaningful; all current checks pass and leave the
  canonical worktree byte- and Git-identical.
- **Risk/gates/confidence/readiness:** H1; substantive architecture dependency;
  high gap and medium design confidence; `SUBSTANTIVE_DEPENDENCY_GATED`.

### GP-CTL-003 — Bind Planner/Curator provenance and surviving supply

- **Objective/value:** make canonical liveness depend on verifiable packet and
  Curator correspondence rather than shape-valid prose, counts, and refs.
- **Current source evidence:** the framework checker validates coarse packet
  state, branch prefix, SHA shape, booleans, event strings, and a nonnegative
  `candidate_count`; it does not validate the recorded Curator provenance
  object, packet ID/commit/base correspondence, curation ref, or structured
  surviving candidate IDs. `candidate_count` influences liveness.
- **Implementation seam:** queue schema, agent-framework checker, exact packet
  metadata/survivor representation, and isolated synthetic Git-object cases.
- **Scope/exclusions:** no candidate promotion, curation judgment, liveness
  policy change, queue item authorization, Git fetch during ordinary offline
  validation, product/runtime, workflow, or hardware change.
- **Dependencies:** Curator binds whether non-ancestral planning/curation Git
  objects must be available offline and the exact representation of surviving
  candidate IDs/dispositions before implementation.
- **Validation:** wrong/missing packet commit, base, packet ID, curation ref,
  review date, candidate count, and survivor ID/disposition fail under either
  selected policy. Under an object-required policy, unavailable required Git
  objects fail; under a snapshot-embedded policy, exact embedded metadata must
  correspond without claiming unavailable objects were inspected. Exact
  current recorded provenance passes. Current queue facts are manually
  verified, not claimed wrong.
- **Risk/gates/confidence/readiness:** H0; substantive schema/object-availability
  dependency; high gap confidence; `SUBSTANTIVE_DEPENDENCY_GATED`.

### GP-BUILD-001 — Fail closed and avoid global Git mutation in pre-build identity

- **Objective/value:** preserve current firmware-version stamping while making
  Git identity/dirtiness failure explicit and keeping pre-build Git settings
  process-local.
- **Current source evidence:** `builder_scripts/arduino_pico.py` runs unchecked
  `git config --global core.longpaths true`, unchecked `git rev-parse --short
  HEAD`, and unchecked `git status --porcelain`; failed Git reads can produce an
  empty or misleading version while the build continues.
- **Implementation seam:** pre-build script plus isolated clean/dirty/missing-
  Git tests and deterministic provenance/manifest/census consequences. Preserve
  the existing short-SHA and `-DIRTY` visible format on successful reads; use
  explicit repository cwd, checked commands, and command-local configuration.
- **Scope/exclusions:** no selector/pin/dependency/workflow change, build result
  interpretation, firmware behavior or table change, artifact acceptance,
  upload, device, or hardware claim.
- **Dependencies:** Curator binds the exact clean/dirty/error version contract
  and whether build failure is required when Git identity cannot be proved.
- **Validation:** clean, staged, unstaged, untracked, missing repo, failed Git,
  long-path option, cwd, and no-global-config-mutation cases; canonical build
  required for the exact implementation snapshot because the pre-build input
  changes.
- **Risk/gates/confidence/readiness:** H1; build proof required, no hardware if
  compiled behavior beyond existing version metadata remains unchanged;
  high gap/medium contract confidence; `SUBSTANTIVE_DEPENDENCY_GATED`.

## Horizon C — evidence, backend, persistence, and owner-gated work

### GP-VAL-008 — Research a current runtime-behavior regression lane

- **Objective/value:** identify the minimum current-source and directly
  accepted-evidence-backed behavior regression subset without reviving stale
  May-28 literal anchors.
- **Current source evidence:** source-sync and offline semantic bridge checks
  pass, while the historical behavior evaluator remains excluded. No new
  domain authority or hardware result appeared.
- **Implementation seam:** read-only source/evidence mapping and proposed
  fixture/checker design; research may validly conclude no safe subset exists.
- **Scope/exclusions:** no invented Ultimate semantics, expected coordinate,
  routing/table edit, Nunchuk claim, firmware change, or source-for-hardware
  substitution.
- **Dependencies/validation:** every expected case needs current source/tests or
  explicit owner/domain evidence; enforce exact per-case provenance,
  stale-anchor rejection, historical isolation, and zero product-source delta.
- **Risk/gates/confidence/readiness:** H1 research; evidence gate; medium gap and
  low executable-breadth confidence; `EVIDENCE_GATED`.

### GP-CONFIG-005 — Preserve live Config on rejected updates

- **Objective/value:** after explicit approval, make existing `SetConfig`
  rejection transactional with respect to live in-memory `Config`.
- **Current source evidence:** `HandleSetConfig()` resets and decodes directly
  into `_config`; decode failure reloads persisted state, but later bounds/type/
  index validation failures and save failure return without restoration.
  Backend/mode code retains references or pointers to the shared configuration.
  Changed rejected data remaining live is inferred, not hardware-proven.
- **Implementation seam:** staged decode/validate/save/publish or exact rollback
  design, negative validation corpus, build, artifact, and physical regression.
- **Scope/exclusions:** no schema/vendor compatibility decision, new device-
  write capability, runtime-table config, WebSerial expansion, persistence
  redesign, flashing, or game semantics.
- **Dependencies:** explicit user approval for existing backend config-write
  behavior work; Curator resolves staging versus restoration, reload-failure,
  rebinding, success/publication, and validation architecture; GP-ART-001 is
  required before H2/H3 hardware handoff.
- **Validation:** malformed decode, every validation branch, injected save/
  restore failure, old live-state preservation, exact success behavior,
  canonical build, immutable artifact identity, and physical regression.
- **Risk/gates/confidence/readiness:** H2, potentially H3 if durable behavior is
  included; user, architecture, artifact, and hardware gates; high asymmetry and
  medium remedy confidence; `USER_DECISION_GATED`.

### GP-PERSIST-001 — Research atomic current-Config persistence

- **Objective/value:** research and propose a source-backed recoverable-save
  contract for the existing `config.bin` path before any power-loss behavior
  change or implementation candidate is authorized.
- **Current source evidence:** `Persistence::SaveConfig()` opens `config.bin`
  with `w+`, writes a placeholder header, encodes the body, and rewrites the
  header; repository architecture notes already classify this as non-atomic.
- **Implementation seam:** read-only investigation of LittleFS guarantees,
  current protobuf persistence, storage/wear constraints, feasible
  fault-injection methods, and prior-good/default recovery choices; produce a
  decision packet and a separately bounded future implementation candidate.
- **Scope/exclusions:** no runtime-table persistence, dual-slot runtime profile,
  vendor format, WebSerial expansion, flashing automation, or compatibility
  claim.
- **Dependencies/validation:** cite authoritative filesystem rename/write
  evidence and identify capacity/wear and recovery decisions; validate research
  provenance, current-source correspondence, and zero firmware/build/artifact/
  device delta. Interrupted-write, CRC, prior-good, repeated-save, build, exact
  artifact, and physical reboot validation belong only to a future, separately
  authorized H3 implementation candidate.
- **Risk/gates/confidence/readiness:** H1 research only; architecture and user
  decisions gate any later H3 implementation; high current non-atomic
  confidence and low-to-medium solution confidence; `RESEARCH`.

### GP-ART-001 — Establish approved immutable firmware-artifact custody

- **Objective/value:** define and prove one durable content-addressed custody
  path for future exact candidate/artifact hardware evidence.
- **Current source evidence:** the hardware contract requires an immutable
  candidate/artifact-addressed locator; current provenance records no
  established store or immutable locator.
- **Implementation seam:** owner-selected store/custody/retention contract,
  synthetic write/readback/tamper/expiry verification, then separately
  authorized real-store integration.
- **Scope/exclusions:** no implicit store choice, credentials, upload, release,
  build, device update, artifact acceptance, or hardware PASS.
- **Dependencies/validation:** owner names store, custody, retention,
  immutability, access, expiry, and readback model; verify content-addressed
  write/readback/re-hash and wrong/mutable/deleted/expired/access-denied cases.
- **Risk/gates/confidence/readiness:** H1/external state; user decision and
  permission gates; high gap/unknown store confidence; `USER_DECISION_GATED`.

### GP-X1-001 — Realize a separately authorized X1 behavior candidate

- **Objective/value:** after exact new production authority exists, realize
  exactly one X1-table delta through the approved source-owned path and full
  exact-snapshot hardware lifecycle.
- **Current source evidence:** `GLYPH-UD-008/009` authorize only the
  baseline-equivalent sole-`kX1Table` no-op intake. No different X1 bytes,
  second-table authority, or gameplay meaning exists.
- **Implementation seam:** new accepted nine-point intake, existing offline
  generator, active source-owned table include, semantic manifest, canonical
  build, artifact custody, and candidate-specific hardware protocol/result.
- **Scope/exclusions:** no second table, modifier/routing/game semantics,
  runtime-loaded config, persistence, device/protobuf write, flashing
  automation, Nunchuk claim, or inference from the no-op pilot.
- **Dependencies/validation:** new explicit nine-point sole-X1 authority,
  GP-ART-001 custody, exact protocol, exactly one changed table, other 27 table
  digests unchanged, canonical build, exact candidate/artifact identity, and
  physical PASS before merge.
- **Risk/gates/confidence/readiness:** H3; user, artifact, protocol, and hardware
  gates; high lifecycle and zero future-content confidence;
  `USER_DECISION_GATED`.

## Planner-estimated curation order

These are non-authoritative estimates only:

1. retain `GP-VAL-010` as the sole current Ready item; do not duplicate or
   reinterpret it;
2. curate `GP-SRC-006` and the `GP-VAL-002` repair as independent H0 work;
3. narrowly Preauthorize the `GP-PROV-002` repair after the shared executable-
   workflow parser lands, and `GP-VAL-012` after `GP-VAL-010` lands;
4. bind the schema/snapshot/object/error contracts for `GP-PROV-003`,
   `GP-VAL-011`, `GP-CTL-003`, and `GP-BUILD-001` before authorization;
5. retain `GP-VAL-008`, `GP-CONFIG-005`, `GP-PERSIST-001`, `GP-ART-001`, and
   `GP-X1-001` behind their exact evidence, user, architecture, artifact, and
   hardware gates.

Planner estimates do not alter the queue, create Ready/Preauthorized work, or
override Curator judgment.

## User-direction and evidence effects

- `GLYPH-UD-007` still retires official-configurator interoperability and
  capture as product/progression dependencies. `GP-CONFIG-005` concerns an
  existing backend failure-safety seam only and remains user-gated; it does not
  revive official-app compatibility.
- `GLYPH-UD-008/009` still authorize only the exact baseline-equivalent
  sole-X1 no-op intake; no changed table content follows.
- No new user direction, source-authority intake, hardware result, immutable
  artifact-custody decision, or external workflow-ownership evidence appeared.
- Legacy Y2 and Alternative-B results cannot satisfy a new Revision-2
  candidate/artifact gate. The canonical-grid result remains `HARDWARE_FAIL`.
- Nunchuk remains `NOT_TESTED`; root cause remains unproven.

## Rejected, deferred, and duplicate alternatives

- Do not duplicate `GP-VAL-010` or any completed 2026-08-31 packet candidate.
- Do not promote all 194 discovered checkers into current load-bearing scope or
  execute historical/unsafe checks merely to avoid empty category selection.
- Do not treat stale prunable worktree metadata as unfinished implementation or
  create canonical cleanup work from local housekeeping.
- Do not revive official-configurator capture/compatibility or rename
  invalidated `GP-CONFIG-002`.
- Do not activate or modernize the dormant 27-table parser without a separately
  approved runtime model and source authority.
- Do not treat existing protobuf `Config` persistence as authority for
  runtime-table persistence, or bundle live-memory rejection safety with atomic
  durable-save research.
- Defer action/dependency pinning, runner-image identity, complete dependency
  closure, and reproducibility claims; current observations remain bounded.
- Defer `glyph_nuker` remediation or replacement. Its source lineage, purpose,
  byte transformation, build recipe, reproducibility, safety, and artifact
  acceptance remain `UNKNOWN`.
- Do not treat a temporary quiet-build log as immutable artifact custody.
- Do not repair or rebuild the failed canonical-grid candidate without exact
  new source authority and the complete H3 lifecycle.
- Reject runtime-loaded config, runtime-config persistence, new
  WebSerial/device write, protobuf binary write, backend runtime-config write,
  flashing automation, `candidate.view`, `active_storage.view`, generated
  active `RuntimeConfigView` replacement, RAM-backed active publication,
  Nunchuk, root-cause, and game-semantic proposals under current authority.

This packet is candidate supply only. It grants no implementation, queue,
firmware, table, workflow, build, storage, publication, device, persistence,
or hardware authority.
