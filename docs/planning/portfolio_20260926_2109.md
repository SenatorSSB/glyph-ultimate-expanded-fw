# Glyph Portfolio Candidate Packet — 2026-09-26 21:09 EEST

This immutable Planner packet is non-authoritative candidate supply for an independent Curator. It authorizes no implementation, firmware build, controller action, hardware result, queue transition, or merge. Canonical queue status is unchanged.

```yaml
packet_id: glyph-portfolio-20260926-2109
packet_state: FRESH
planning_branch: planning/portfolio-20260926-2109
base_configurator_sha: 975b902c6b2facf27108cd5ac7153dd9efb7638a
base_configurator_tree: 2fda9b2c3a3c663b856dab217f8cb75257d04c98
base_live_verified: true
previous_planner_packet: glyph-portfolio-20260923-1435
previous_packet_commit: 3c445363ba15a403597efb7c288e3489db8a5fe0
previous_packet_freshness: CONSUMED
candidate_count: 3
curator_review_required: true
global_wait_proposed: false
planner_authority: NON_AUTHORITATIVE_CANDIDATE_SUPPLY_ONLY
configurator_changed: false
```

## Live basis and material events

The ordinary live `git ls-remote --heads origin configurator` returned the expected SHA. `git fetch origin` obtained live heads and tags; an all-heads read confirmed the same canonical ref. The canonical tree is above. The queue in `docs/project/ACTIVE_AGENT_QUEUE.md` reports immediate READY 0, recorded PREAUTHORIZED 0, mechanically activatable PREAUTHORIZED 0, hardware-pending 0, effective authorized runway 0, target 4, with `PLANNING_REQUIRED` and `PLANNER_REFRESH_REQUIRED`. Its previous Planner packet is CONSUMED, has no survivors, and no pending curation obligation. Target 4 is guidance, not a quota.

Since the previous packet, Curator chose Strategy B for GP-CONFIG-010. Exact combined candidate `1c0ff22646729d26d45eacb4b8322c5baea7de48` and UF2 SHA-256 `4312f6a64fd1009e231aab2015e3ce67f862abd88a8ea31cd545db0e91e27476` passed the bounded physical protocol. Canonical integrated the tested candidate through `50a6357eddf0d9c83d31233666c451806fb1f424`, added narrow correspondence inventory through `7ca129e218b292c0aa64b38577848dd8b63a4c66`, and recorded DONE at this packet's base. GP-X1-002 remains DONE. Active X1 remains `(93,51), (128,51), (163,51), (93,128), (128,128), (163,128), (93,205), (128,205), (163,205)`. Neither completion is proposed again.

The owner now explicitly resumes **planning and curation consideration** of GP-CONFIG-012/013/014. This new direction supersedes the parking effect of GLYPH-UD-017 (`docs/agent_framework/USER_DIRECTION.md`, `docs/project/DEFERRED_CONFIG_WORK_2026-09-21.md`) for these three items. It does not change their canonical REVIEW/NONEXECUTABLE state or authorize probes, implementation, builds, hardware or merge. The warning's source and affected identities remain unknown. Curator should record owner resumption in authoritative direction/control-plane state and independently replace any stale order it accepts. GP-VAL-011 remains separately OWNER_DEFERRED/NONEXECUTABLE under GLYPH-UD-013.

The historical 012/013/014 authorization base was `4351b951916abbfbcaffec6f4512571a17d15ca5`. Present `include src HAL config platformio.ini builder_scripts` differs there in `src/core/mode_selection.cpp` (GP-CONFIG-010) and `src/modes/runtime_config/generated_source_owned/GeneratedRuntimeConfigBaseline.current.hpp` (GP-X1-002). Thus old whole-plane byte-equality activation conditions fail even though each local condition below persists. Fresh replacement contracts are needed; no mechanical reactivation. Old GP-CONFIG-010 exclusions forbidding integration are stale after DONE and must not be copied.

The production selector remains `GregTurbo/HayBox-proto#db4e2f6` in `config/glyph/env.ini`. GP-VAL-026's tracked schema closure records full HayBox-proto commit `db4e2f68b5c4ddd407e7c11050a920c4b4ec54c8`, Nanopb commit `cad3c18ef15a663e30e3e43e3a752b66378adec1` / tag `0.4.9.1`, and observed package `nanopb/Nanopb@0.4.91`. Current SHA-256 values for `config.proto`, `config.options`, generated `config.pb.h`, and `pb.h` respectively match `2844d8fc8c78c9fbed00a6954a13d9826f4634cac152f8a9a707666f47bb893b`, `6a53dc93a79027669a3990c3a785e386e02e063c1f3438744aac49c3ad074805`, `532f7ac324a57895caf82950ee36c6900d883a42188e5d6bbc2d3507318538f3`, and `a2ecdca9fdaeef5f4972ed983540c0d6fb0a5c402a2e0b0349d7e1bc5e188d29`. The generated header contains `AnalogModifier modifiers[20]`; `config.options` has `CustomModeConfig.modifiers max_count:20`. This is observed fixture/selector correspondence, not transitive reproducible-build proof. Source, selector or schema drift returns to Curator.

## GP-CONFIG-012 — invalid button characterization

**Proposed Curator disposition: READY; Planner estimate CURATION_READY; risk H1.** Source: `HAL/pico/include/util/state_util.hpp:26-32` still shifts `1ULL << (buttons[j] - 1)` without zero/range validation, unlike `set_button` and `get_button` at lines 10-23. Generated schema has `BTN_UNSPECIFIED=0`. Prior host UBSan found a negative shift; physical reachability and symptoms are unknown. Current direct production callers are `CustomControllerMode::SetConfig` modifier and combo loops (`src/modes/CustomControllerMode.cpp:16-25`), `setup_mode_activation_bindings` (`src/core/mode_selection.cpp:203-214`), and `backend_config_from_buttons` (`src/core/config_utils.cpp:8-28`). GP-CONFIG-010 only guards the mode-mask array count, not button values. The AVR helper is a separate target.

**Candidate order:** Add exact-production host decoder/helper harness, checker, fixtures, provenance and report under `tools/` and `docs/`. Extend GP-VAL-026's tracked closure with exact `config.pb.c`, `pb_decode.c/h`, `pb_common.c/h` only after pinning upstream/build-resolved bytes, licenses, hashes and validation-manifest dependencies. The recorded `/private/tmp/glyph-execution-20260921` partial checkout was absent when inspected; it supplies no current authority. Clean reimplementation is safer. If a copy later appears, compare it read-only and accept no unverified bytes or stale queue edits.

Test empty, zero, first/last valid, out-of-range positive, maximal decoder-representable, malformed/overflow varints and repeated/packed encodings. Isolate sanitizer-failing cases in separate processes. Keep decode acceptance, stored value, helper UB and defined valid-mask bit positions distinct. Matrix all four call sites with input origin, validation, configured/decoded zero reachability, defaults, `HandleSetConfig`, `Persistence::LoadConfig`, menu/watchdog and physical status. Exact source/caller census, fixture tamper/omission and wrong dependency identity must fail closed. Run affected transaction/rebinding plus manifest/census/health/framework/sequence/navigation/surface checks and independent review. Firmware build and hardware are not required for this H1 artifact.

**Excluded:** helper/firmware repair, reject/no-binding/ignore selection, defaults/remaps rewrite, persistence/ABI edit, device write, runtime-loaded config, Nunchuk, exploitability, physical crash and root-cause claim. Stop if decoder provenance fails, source changes, policy is required, or exact-production binding cannot be proved. Any behavior repair needs a separate Curator order.

## GP-CONFIG-013 — USB default characterization

**Proposed Curator disposition: READY; Planner estimate CURATION_READY; risk H1.** Source: `get_usb_backend_config_default` (`HAL/pico/src/comms/backend_init.cpp:288-298`) writes only if the one-based `default_usb_backend_config` is nonzero and within backend count. `initialize_backends` at lines 23-90 declares `CommunicationBackendConfig usb_backend_config;`, calls the getter, immediately reads `usb_backend_config.backend_id` in `init_primary_backend`, and may copy the object for USB-detected IDs. This proves a source-level no-write/subsequent-read condition, not a physical symptom. `config/glyph/common/src/config.cpp:91-109` loads persisted config or saves defaults, registers the getter and initializes backends. Defaults use index 1 in `HAL/pico/include/config_defaults.hpp:167` and `config/glyph/common/include/glyph_overrides.hpp:1783`. `HandleSetConfig` (`HAL/pico/src/comms/ConfiguratorBackend.cpp:161-277`) validates `default_backend_config` at line 181, not this USB index, before SaveConfig. `Persistence::LoadConfig` decodes persisted bytes after size/CRC checks but supplies no USB-index semantic validation. Watchdog scratch can update the USB default at `backend_init.cpp:266-282`. Tracked generated member is `uint8_t` from protobuf `uint32` with `Config.default_usb_backend_config int_size:IS_8`; observe decoder outcomes rather than infer them.

**Candidate order:** Host-only exact getter/caller harness, actual decoder fixtures, checker and report. Sentinel destination cases must prove unchanged bytes on zero/out-of-range and exact full-object copy for valid 1..count. Record the source-level uninitialized production read separately; do not deliberately read uninitialized host storage or claim sanitizer proof without reliable evidence. Cover empty list, 0, 1, last, count+1, representable extremes, malformed/overflow encodings, callback registration, defaults, persisted load, SetConfig, menu/watchdog producers and downstream `backend_id`/`default_mode_config`. Bind exact bodies and caller census; schema/decoder/source tampering fails closed. Run the same relevant validation/review gates as 012. No firmware build or hardware is required.

012 and 013 share decoder/schema/provenance fixtures. They have no substantive dependency: 013 may add the identical closure if 012 has not landed. Serialize fixture edits; if 012 lands first, reuse its reviewed closure. **Excluded:** getter/caller repair, initialization choice, reject/fallback/retain choice, dispatch or persistence/schema change, device action or physical symptom claim. Stop on missing provenance, source drift or behavior-policy pressure. Later firmware repair needs a separate owner/Curator decision.

## GP-CONFIG-014 — custom modifier cache capacity repair

**Proposed Curator disposition: READY; Planner estimate CURATION_READY; risk H3.** Independent of 012/013. Current `include/modes/CustomControllerMode.hpp:18-23` has an uninitialized `_custom_mode_config` pointer and `_modifier_button_masks[10]`. `CustomControllerMode::SetConfig` (`src/modes/CustomControllerMode.cpp:10-27`) indexes through `modifiers_count`; `UpdateAnalogOutputs` at lines 86-110 reads through that count. Exact generated extent is 20. Preserved GP-CONFIG-011 fixture/checker records 0/10 PASS and 11/20 sanitizer failures for setup and processing; its direct exact-source checker passed on this canonical tree. GP-CONFIG-010 establishes a named capacity/schema equality pattern for a different array, not a mechanical copy of its runtime behavior.

**Candidate order:** Change only `include/modes/CustomControllerMode.hpp` and `src/modes/CustomControllerMode.cpp` in production: named fixed capacity 20 with compile-time equality to `std::extent_v<decltype(CustomModeConfig::modifiers)>`; initialize the existing pointer to null; refuse impossible `>20` before `InputMode::SetConfig`, accepted pointer/cache mutation, modifier indexing or later cached read. Handle in-place alias/count mutation before processing. Preserve valid 0..20 ordering, masks, arithmetic, null no-output path, combo/digital behavior and ABI. No truncation, clamp, reorder or coupling to GP-CONFIG-010. If refusal cannot preserve accepted state and valid behavior within these two files, return to Curator.

Create a separately named exact-production repair harness/checker/report. Preserve and authenticate GP-CONFIG-011 historical fixture/failure observations; make the current load-bearing gate recognize only reviewed repaired source, without rewriting history or demoting it. Compile exact SetConfig/digital/analog bodies with address/undefined/bounds sanitizers. Require 0/10/11/20 valid PASS, injected 21/max refusal before writes/reads, fresh/null and valid-invalid-valid, in-place mutation, index 19, mixed masks, noncommutative ordering and representable arithmetic. Prove unchanged combo/digital/axis paths; mutation negatives must catch source/schema/capacity drift. Run affected transaction/rebinding/custom-mode and manifest/census/health/framework/sequence/navigation/surface gates; obtain fresh independent source/safety review and review RAM/map delta.

This active critical-input repair is H3. From freshly verified canonical, commit the exact candidate before `pio run -e glyph_mk6` (quiet fallback only if unavailable). Preserve and readback-hash the exact UF2 at `local_backups/hardware-artifacts/<full candidate Git SHA>/<artifact SHA-256>/firmware.uf2`; publish a separate source-free canonical hardware-pending snapshot with both full identities and a bounded protocol. Physical protocol should cover custom mode with 0/10/11/20 valid modifiers, index 19, distinct masks and ordered modifiers, unchanged combo/digital/axis output, ordinary/default mode, reconnect, owner Config restoration and recovery. Use valid buttons and representable arithmetic; zero-button/USB policies are outside this gate. Operator rehashes preserved bytes immediately before update and reports exact observations. Stop before merge until exact candidate/artifact physical PASS. Builds, host tests and GP-CONFIG-010/X1 PASS do not transfer. On fail/inconclusive, isolate source, record gaps and restore known-good firmware/config under manual protocol; changed bytes require renewed review/build/hardware.

**Excluded:** zero-button, multiplier/sign, arithmetic/clamp, priority/routing, X1/current tables, active RuntimeConfigView publication, runtime-loaded config, schema/ABI/persistence, USB behavior, flashing automation, Nunchuk and root-cause change. Stop on such a need, changed valid behavior, checker weakening or missing review/build/custody/protocol/PASS.

## Broader audit, sequence, runway

The audit covered active `src/`, `include/`, `HAL/pico/`, Glyph config, generated schema/fixtures, persistence/SetConfig, mode selection, custom modes, source-owned tables, queue, roadmap and validation surfaces. `mode_activation_masks` now has 30 slots, schema equality and count guards (`src/core/mode_selection.cpp:33-42,179-211`), so no new GP-CONFIG-010 item is justified. Other repeated fields/production loops were spot-checked; no further proven cache/schema mismatch emerged. The known persistence write/recovery limitation does not supply a new repair policy. Default-menu byte and other TODOs do not supply complete safety orders. GP-VAL-011's nested `.pio` aggregate-isolation limitation remains; no aggregate-green claim. No fourth resilience/validation candidate is justified. GP-HW-002 stays research-gated. Official-configurator interoperability and Senscope decisions remain outside this cycle.

Read-only focused checks on the exact base passed: GP-CONFIG-011 eight-case exact-source sanitizer checker; nine-case exact SetConfig transaction checker; 13-case rebinding characterization; checker census 207; validation health with 207 census and 46 manifest entries; agent framework, agentic sequence and docs navigation. No firmware build, aggregate validation or hardware test was performed. One nonexistent manifest-checker filename was attempted during discovery; that command-selection error is not a repository failure, and the actual census/health gates passed.

Recommended execution sequence is **012 → 013 → 014**. This serializes decoder-fixture edits, lets 013 reuse reviewed closure and avoids changing CustomControllerMode while 012 binds that caller. It is scheduling guidance, not a substantive dependency. 014 is the highest-severity proven memory-safety issue and independently source-ready; Curator may move it first, refreshing 012/013 source bindings after integration. Prefer at most one H3 candidate awaiting physical test. Any future 012/013 firmware repair is USER_DECISION_GATED until accepted source/owner authority resolves externally visible behavior. If Curator finds 014 refusal or valid-path preservation incomplete, use REVIEW/EVIDENCE_GATED instead of inferring policy.

Proposed initial Curator dispositions are READY for all three after fresh independent adjudication and full replacement of stale queue orders. No PREAUTHORIZED item is proposed: no objective external prerequisite is outstanding, and shared-fixture serialization is an execution constraint. If accepted, proposed immediate/effective runway is 3, recorded Preauthorized 0; actual runway remains 0 until Curator publishes. No global wait. Packet publication supplies a fresh Planner refresh for Curator review, not canonical authorization. Any material production/schema/selector drift, conflicting canonical/hardware evidence, provenance failure, boundary crossing or new owner direction invalidates the affected proposal.
