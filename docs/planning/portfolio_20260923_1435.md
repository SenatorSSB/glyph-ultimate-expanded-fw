# Glyph Portfolio Candidate Packet — 2026-09-23 14:35 EEST

This immutable Revision-2 packet is non-authoritative candidate supply for an
independent Work-Order Curator. It authorizes no firmware implementation,
controller mutation, hardware result, queue publication, integration, or merge.

```yaml
packet_id: glyph-portfolio-20260923-1435
packet_state: FRESH
planning_branch: planning/portfolio-20260923-1435
base_configurator_sha: 47f9b122a7eb60073c114e6568b4d0005ce1edb2
base_live_verified: true
previous_planner_packet: glyph-portfolio-20260922-1327
previous_packet_commit: 83fef0c529db28b1505a224f5b3432dab5865d6a
previous_packet_freshness: CONSUMED
candidate_count: 1
curator_review_required: true
global_wait_proposed: false
material_events_since_publication: []
planner_authority: NON_AUTHORITATIVE_CANDIDATE_SUPPLY_ONLY
configurator_changed: false
```

## Basis, evidence, and limits

Fresh live GitHub verification resolved `origin/configurator` to exact commit
`47f9b122a7eb60073c114e6568b4d0005ce1edb2`. This planning branch begins at
that commit. The canonical queue reports zero Ready items, zero recorded or
mechanically activatable Preauthorized items, zero hardware-pending items, zero
effective authorized runway, and `PLANNING_REQUIRED`.

The preceding packet is consumed. Since its adjudication, the exact original
GP-CONFIG-010 candidate and artifact completed physical acceptance, immutable
PASS evidence was published at
`830f519e95a72c85abea8593c11f1097e5248d49`, and canonical hardware-result
publication advanced to this packet's base. GP-CONFIG-010 is therefore
`HARDWARE_VALIDATED / PASS`, with no evidence gaps, but normal integration now
stops because current canonical has critical input drift relative to the
historical tested snapshot. The owner's present direction is a material event
requesting architecture adjudication for that exact integration conflict.

This is a bounded issue-specific Planner analysis, not a claim of a full
portfolio-wide scarcity audit. It compares all plausible architectures for
closing GP-CONFIG-010 against the current correspondence contract. It proposes
one Horizon A candidate and no global evidence wait. Unrelated deferred or
gated portfolio work remains unchanged.

### Pinned historical evidence

- GP-CONFIG-010 original candidate:
  `f4771e17430fd1ea3f1e3e5339a83dfe648290a3`;
- original direct parent:
  `28426e4ba4763a99f0ca13491c023af77665c79c`;
- original candidate tree:
  `e18a7496125bf485a179d9b8e08f1f47f5090b80`;
- original physically tested 791040-byte UF2 SHA-256:
  `9be230cdce6b6ce0b97941da920ec8f043ff98c174ffb126cfcc654ad599209a`;
- latest immutable GP-CONFIG-010 PASS evidence:
  `git-json:830f519e95a72c85abea8593c11f1097e5248d49:docs/calibration/fixtures/gp_config_010_hardware_evidence_2026-09-23_reconnect_completion.json`;
- accepted GP-X1-002 candidate:
  `f657715b26d26587a931074ce7dd12c698785290`;
- accepted 792064-byte GP-X1-002 UF2 SHA-256:
  `00dc75b65a080830131c3c260558d4daeff45bb528e43d196b8ad94fcd06f451`;
- reviewed GP-X1-002 canonical integration:
  `3b4a7c950e1b14e24a88c951c19313f6134f4ea3`; and
- expected unchanged owner Config identity: 4201 bytes, SHA-256
  `f589317b59b3ab7543cad563e2e0877dc5f491227e6e35777bc732d402bb1480`.

The historical artifacts remain immutable evidence for their original exact
candidates. Neither transfers physical acceptance to newly built bytes.

## Architecture comparison

### Strategy A — compose independently accepted critical changes in correspondence

- Evidence strength: weaker than direct final-byte testing. It would prove
  that each critical change was separately accepted, then infer their combined
  behavior.
- Exact artifact correspondence: absent for the final combined bytes unless
  those bytes are also physically tested.
- Complexity: high. It requires a new dependency/independence model, explicit
  composition rules, negative cases, and a defensible definition of which
  critical changes may compose.
- Hardware burden: superficially lower, but only by replacing direct evidence
  with an inference.
- Unsafe inference risk: high. GP-CONFIG-010 and GP-X1-002 were tested in
  different source/build snapshots; separate PASS results do not establish
  behavior of their composition.
- GP-VAL-015/027 compatibility: incompatible without materially changing the
  current contract. That contract intentionally rejects every critical target
  difference, including changes untouched by the historical candidate. The
  generated active baseline, `.gitattributes`, and build workflow remain
  critical; the GP-X1-002 table is real active behavior.

Planner disposition: `REJECT`. Do not weaken critical-path fail-closed behavior
or publish an exception that separately tested firmware changes automatically
compose.

### Strategy B — fresh current-canonical integration candidate and exact-artifact test

- Evidence strength: strongest. The physical result attaches to the actual
  source snapshot and UF2 intended to become final canonical firmware.
- Exact artifact correspondence: direct. Candidate commit precedes build, the
  UF2 is content-addressed, and hardware observes those exact bytes.
- Complexity: moderate and bounded. Carry forward one already-reviewed
  `src/core/mode_selection.cpp` semantic delta, refresh its current host proof,
  build, preserve, and run a minimal integration protocol.
- Hardware burden: one new artifact and a bounded combined regression, not a
  mandatory replay of every prior observation.
- Unsafe inference risk: lowest. Historical results remain provenance while
  the final combined snapshot receives its own evidence.
- GP-VAL-015/027 compatibility: preserves the contract. It does not ask the old
  artifact to represent current canonical and does not reclassify any critical
  path as nonbehavioral.

Planner disposition: `CURATION_READY` and recommended.

### Strategy C — other repository-native governance mechanism

The existing native mechanism is exact candidate ancestry plus finite
critical/nonbehavioral correspondence under GP-VAL-015/027. It can integrate a
tested candidate only when every later critical input remains exact. It has no
repository-authorized compositional exception, semantic-equivalence waiver, or
rebuild-acceptance rule. `DIRECT_ANCESTRY` and exact replay completion evidence
prove source integration ancestry; they do not turn an untested integration
build into the physically tested artifact. No current governance mechanism
eliminates the final-byte hardware gap without either becoming Strategy A or
building and testing Strategy B.

Planner disposition: `REJECT` as a distinct alternative. Use the existing
governance, custody, review, and hardware lanes to execute Strategy B.

## GP-CONFIG-010 — exact current-canonical integration candidate

- Horizon: A.
- Classification: H3 firmware-safety integration candidate.
- Non-authoritative readiness estimate: `CURATION_READY` for independent
  architecture selection and a complete replacement candidate work order.
- Recommended strategy: Strategy B.
- Objective: from exact current canonical
  `47f9b122a7eb60073c114e6568b4d0005ce1edb2`, create one committed candidate
  that carries forward the exact meaningful GP-CONFIG-010 capacity repair while
  preserving GP-X1-002 and every later accepted canonical change, then build and
  physically test that exact combined artifact.
- Value: closes the mismatch between historical hardware evidence and the
  actual final firmware bytes without weakening GP-VAL-015/027.

### Source authority and exact semantic invariant

The original candidate changes production firmware only in
`src/core/mode_selection.cpp`: it adds `<type_traits>`, defines named capacity
30, statically equates it to
`std::extent_v<decltype(Config::game_mode_configs)>`, statically proves the
13-entry default fits, sizes `mode_activation_masks` from that capacity, and
returns before indexing when setup or selection sees a count above capacity.
No current canonical changes overlap those source lines; current canonical has
the original ten-slot cache and unguarded loops. Therefore the exact meaningful
repair can be carried forward without semantic redesign.

Implementation must produce a machine-readable original-candidate-to-new-
candidate source comparison. Each original firmware semantic must be one of
`EXACT_SOURCE_MATCH`, `SEMANTICALLY_IDENTICAL_WITH_CONTEXT_CHANGE`, or
`DIFFERENT`. Any behaviorally relevant `DIFFERENT` result stops before build or
hardware. Do not opportunistically refactor, rename, reorder, or generalize the
repair.

### Preservation invariant

Preserve all current canonical source, build, provenance, validation, and
governance changes outside the exact GP-CONFIG-010 carry-forward. In
particular, the active generated source-owned baseline must retain the accepted
ordered `kX1Table` rows:

```text
(93,51), (128,51), (163,51),
(93,128), (128,128), (163,128),
(93,205), (128,205), (163,205)
```

Preserve every other table byte, including `kMX1Table`, plus current LT5/non-
Mode routing, Mode behavior, precedence, active publication, profile/default
configuration, persistence, USB/backend behavior, `.gitattributes`, workflows,
build controls, host/checker fixes, protocol infrastructure, and historical
evidence. No regression to the prior offset-41 grid is permitted.

### Scope and exclusions

Scope is limited to the exact source-semantic carry-forward, a current
exact-production sanitizer/host proof, a machine-readable semantic comparison,
directly coupled candidate protocol/evidence metadata authorized by Curator,
canonical build, immutable custody, independent firmware-safety review, and one
exact-artifact physical integration test.

No schema/protobuf/persistence layout change; mode/profile addition, removal,
reordering, or rebinding; X1 or other table edit; routing/publication change;
runtime-loaded config; WebSerial/device/protobuf/backend write addition;
flashing automation; gameplay/angle/radius inference; Senscope data or solver
decision; Nunchuk claim; root-cause claim; GP-VAL-011 reopening; correspondence
weakening; historical evidence rewrite; or pre-PASS merge is included.

### Required validation and gates

1. Reverify live `configurator` immediately before candidate creation. Any
   relevant firmware/build-input advance returns to Curator; do not silently
   rebase this authorization.
2. Commit the exact integration candidate before building.
3. Independently review the committed source diff and machine-readable
   semantic classification before hardware.
4. Run exact-production counts 0, 10, 11, 13, and 30; reject above 30; exercise
   indices 0 through 12; use ASan/UBSan or the current equivalent; prove the
   generated schema/capacity relationship, current 13-entry correspondence,
   and unchanged ordering/bindings.
5. Run current source-sync, generated-baseline, GP-X1-002, GP-VAL-015/027,
   framework, agentic-sequence, navigation, agent-surface, manifest, census,
   health, provenance/build-input, focused Python/C++ tests, and
   `git diff --check`. Do not claim full aggregate green if the known nested
   `.pio` issue remains.
6. Run `pio run -e glyph_mk6` or the canonical approved equivalent. Record
   candidate SHA, tree, parent, RAM, flash, UF2 path, SHA-256, and size.
7. Preserve exact bytes at
   `local_backups/hardware-artifacts/<candidate>/<artifact>/firmware.uf2`,
   verify custody, and do not rebuild after handoff.

### Minimal exact-artifact hardware protocol requirements

Before mutation, capture the exact owner Config payload and require the expected
4201-byte SHA-256 identity above unless independently adjudicated otherwise.
Require deterministic decode/re-encode and intended-only mutation. Restore the
exact original payload after testing and prove immediate plus post-reboot hash
equality. Restoration failure stops and becomes the recovery priority.

For GP-CONFIG-010, recreate the established temporary thirteen-Ultimate Config,
verify all thirteen profiles are visible, exercise at least one low index plus
indices 9, 10, 11, and 12, require LT4-to-Ult13 after reconnect, and record
clean first startup and reconnect stability. Exercise all thirteen when the
incremental interaction cost is low. Any capacity/order/binding discrepancy or
repeated frozen-logo event stops.

With the owner Config restored, verify GP-X1-002 in Ultimate: auto-selection,
LT5-to-X1 selection, release to neutral, no stuck input/disconnect, and either
all nine accepted X1 outputs or a Curator-approved representative subset only
when exact unchanged-table proof and prior full physical evidence justify it.
Expected raw/miniscreen rows are:

| Dir | Raw | Miniscreen |
| ---: | ---: | ---: |
| 1 | (93,51) | (-35,-77) |
| 2 | (128,51) | (0,-77) |
| 3 | (163,51) | (+35,-77) |
| 4 | (93,128) | (-35,0) |
| 5 | (128,128) | (0,0) |
| 6 | (163,128) | (+35,0) |
| 7 | (93,205) | (-35,+77) |
| 8 | (128,205) | (0,+77) |
| 9 | (163,205) | (+35,+77) |

Also require representative buttons/directions, normal miniscreen behavior,
and normal reconnect. Give one physical instruction at a time and pause for the
owner's direct observation. Nunchuk remains `NOT_TESTED`.

### Evidence processing and completion

A fresh Hardware Evidence Processor must bind the exact integration candidate
SHA and exact integration artifact SHA-256, complete observations, owner Config
restoration, GP-CONFIG-010 PASS, GP-X1-002 retention, and an empty evidence-gap
list. On exact PASS, a fresh Implementation Supervisor may integrate only the
tested integration commit, verify canonical ancestry, run strict DONE
correspondence, and publish GP-CONFIG-010 DONE. Preserve both the original
historical GP-CONFIG-010 artifact and the new exact integration artifact; do not
replace or erase either evidence lineage.

### Invalidation and stop conditions

Stop and return to Curator if current canonical advances with relevant
firmware/build changes; the original semantics cannot be carried forward
exactly; any behaviorally relevant semantic comparison is `DIFFERENT`; the X1
table differs from the accepted rows; any unrelated active source or table
changes; a required checker, review, build, custody, or source-correspondence
gate fails; hardware fails; owner Config restoration fails; GP-VAL-015/027
would need weakening; or GP-VAL-011 would reopen.

## Rejected alternatives

Rejected: retroactively treating the original UF2 as evidence for current
canonical; treating GP-X1-002 as nonbehavioral; making separately accepted
critical changes automatically composable; adding a special-path exemption to
GP-VAL-015/027; accepting an untested integration rebuild; restoring the
offset-41 X1 grid; replaying every historical physical step without a bounded
integration rationale; opportunistic source refactoring; and rewriting or
discarding historical evidence.

## Publication and next action

Publish only this packet as a direct child of its exact live base. A fresh
independent Curator must choose the architecture, validate the exact semantic
and preservation invariants, define the complete authorized work order and
minimal protocol, and either authorize Strategy B or stop with its different
decision. Planner does not mark this candidate Ready or Preauthorized.

## Delegation

- guidance applicable: yes; material Planner output requires the repository's
  delegation preflight and independent Curator judgment.
- capability discovery: complete available runtime tool/collaboration catalog
  inspected.
- native capability available: yes.
- specialists used: none.
- reviewer used: none within this non-authoritative Planner pass; the parent
  supervisor owns the separately required fresh independent Curator pass.
- if none, reason: the owner bounded the audit to one already identified
  integration conflict, and the complete source/evidence/correspondence surface
  was small enough for one coherent read-only analysis; parallel specialists
  would duplicate the same narrow evidence set rather than create a cleanly
  separable audit partition.
