# Glyph Portfolio Candidate Packet — 2026-09-22 01:08 EEST

This immutable Revision-2 packet is non-authoritative candidate supply for an
independent Work-Order Curator. It authorizes no firmware implementation,
candidate replacement, device mutation, hardware result, queue publication, or
merge.

```yaml
packet_id: glyph-portfolio-20260922-0108
packet_state: FRESH
planning_branch: planning/portfolio-20260922-0108
base_configurator_sha: 2bb99fdc0ea4104793e0068846e442a82d76cd9e
base_live_verified: true
previous_planner_packet: glyph-portfolio-20260921-2232
previous_packet_commit: 6856dde0d11f027c42d00b64cce6f0b7d79f1639
previous_packet_freshness: CONSUMED
candidate_count: 1
curator_review_required: true
global_wait_proposed: false
material_events_since_publication: The owner explicitly directed a bounded recovery test for the unchanged GP-CONFIG-010 candidate after its exact prior INCONCLUSIVE result, using a temporary owner-derived 13-entry Ultimate configuration that makes every cache index physically exercisable through GameCube and requires exact configuration and firmware rollback.
planner_authority: NON_AUTHORITATIVE_CANDIDATE_SUPPLY_ONLY
configurator_changed: false
```

## Basis, evidence, and limits

Live GitHub verification resolved `origin/configurator` to
`2bb99fdc0ea4104793e0068846e442a82d76cd9e`. The exact live candidate ref
`glyph/gp-config-010-mode-activation-capacity` remains
`f4771e17430fd1ea3f1e3e5339a83dfe648290a3`, with direct parent
`28426e4ba4763a99f0ca13491c023af77665c79c` and tree
`e18a7496125bf485a179d9b8e08f1f47f5090b80`. Its preserved 791040-byte UF2
remains a regular non-symlink file with SHA-256
`9be230cdce6b6ce0b97941da920ec8f043ff98c174ffb126cfcc654ad599209a`, and
the custody verifier returned `PRE_HANDOFF_VERIFIED`.

The prior immutable evidence at
`git-json:6c858622657b70c8dc964db0cd4ffaac6ca4fb9f:docs/calibration/fixtures/gp_config_010_hardware_evidence_2026-09-21.json`
remains historical `INCONCLUSIVE` evidence. It found six visible GameCube
profiles, matching backend applicability, and did not exercise indices 3
through 12. Nothing in this packet reinterprets that run as PASS.

The accepted rollback authority is GP-X1-002 candidate
`f657715b26d26587a931074ce7dd12c698785290`, live at
`glyph/gp-x1-002-restore-normal-x1`, with its preserved regular non-symlink
792064-byte UF2 SHA-256
`00dc75b65a080830131c3c260558d4daeff45bb528e43d196b8ad94fcd06f451`.
The custody verifier returned `PRE_HANDOFF_VERIFIED`. No integration rebuild is
authorized as a physical rollback substitute.

Inspected authority includes current state, roadmap, workflow, runtime-config
boundary, authorization/runway, hardware evidence and custody contracts, the
GP-CONFIG-010 work order and protocol, prior result/evidence, protobuf schema,
current serial/config operator tooling, mode-selection source, matrix and
display button maps, and the owner's explicit recovery direction. The owner
has separately and explicitly approved the temporary persisted Config mutation
and exact restoration for this run. That approval does not authorize a general
runtime-loaded-config product capability or flashing automation.

## GP-CONFIG-010 — bounded physical-acceptance recovery addendum

- Readiness estimate: `CURATION_READY` as a source-free
  `REPAIR_REAUTHORIZATION` of the existing hardware protocol only. Existing
  work-order status remains `LOCAL_ACCEPTANCE_PENDING` until fresh evidence is
  processed.
- Objective: exercise GP-CONFIG-010 cache entries 0 through 12 directly on the
  unchanged exact candidate/artifact using a temporary persisted configuration
  with thirteen GameCube-visible Ultimate clones, then restore the owner's
  original Config byte-for-byte and restore the exact physically accepted
  GP-X1-002 artifact.
- Behavioral claim: none beyond the original GP-CONFIG-010 candidate claim.
  The temporary configuration is test stimulus, not firmware behavior,
  defaults, schema, product configuration, gameplay authority, or a candidate
  modification.
- Authorized test stimulus: capture the complete owner-original persisted
  Config before any mutation; preserve exact raw protobuf bytes plus decoded
  JSON, length, SHA-256, identity/status, and operator log; prove decode and
  deterministic re-encode equality when supported. Starting only from those
  captured bytes, clone the owner's currently accepted `MODE_ULTIMATE`
  `GameModeConfig` exactly into indices 0..12, name them `Ult01`..`Ult13`, set
  each clone GameCube-applicable (GameCube-only is permitted), and assign one
  unique non-empty source-backed physical activation binding to each.
- Binding safety: prefer thirteen single physical buttons from the authoritative
  Glyph matrix/display mapping. Exclude menu buttons and any binding involved
  in configurator, bootloader, firmware-reboot, reset, or the manual Senscope
  debug chord. If single buttons cannot satisfy the proof, chords are permitted
  only after proving uniqueness and that no earlier binding is a subset of a
  later binding. Empty, duplicate, unavailable, guessed, or ambiguous bindings
  stop the run.
- Offline proof before write: exactly thirteen mode entries and indices 0..12;
  exact names; all `MODE_ULTIMATE`; all GameCube-applicable; bindings non-empty,
  pairwise unique, representable by the production 64-bit mask, and free of
  subset ambiguity; every cloned field other than name, activation binding,
  and applicable backend equals the captured owner Ultimate entry; every other
  Config field equals the owner-original capture. Preserve a machine-readable
  plan and human-readable intended-only diff.
- Device transaction proof: re-read and require exact owner-original bytes
  immediately before the temporary write; use the existing custom backend
  `CMD_SET_CONFIG` path with ordered schema-v2 transaction stages; stop without
  automatic retry on any timeout or write ambiguity; require `CMD_SUCCESS`,
  follow-up `CMD_GET_CONFIG`, and exact intended payload equality; after one
  owner-performed reboot and return to Configurator mode require the same exact
  temporary payload again.
- Physical proof: through GameCube, require `Ult01`..`Ult13` visible in order;
  select each unique binding one row at a time with release between rows;
  record expected and observed name, connection and miniscreen stability,
  ordinary directional response, and row disposition. Explicitly identify
  `Ult11`/index 10, `Ult12`/index 11, and `Ult13`/index 12 as the former overflow
  boundary. After all rows pass, run only representative Ultimate sanity on
  `Ult13`, then power-cycle/reconnect and reselect `Ult13`.
- Mandatory recovery: whether PASS, FAIL, PARTIAL, or INCONCLUSIVE, reconnect in
  Configurator mode if safe; preserve the final temporary state; write the
  exact captured owner-original protobuf payload; require immediate and
  post-reboot `GET_CONFIG` byte equality and the original SHA-256. Only after
  that succeeds, manually flash the exact accepted GP-X1-002 custody artifact
  and confirm ordinary Ultimate, LT5 X1 activation, representative accepted X1
  output, display/menu state, and connection stability. A restore failure
  stops and takes priority over firmware rollback.
- Evidence: preserve the original INCONCLUSIVE record unchanged. Create a fresh
  evidence identity containing the unchanged GP-CONFIG-010 candidate/artifact,
  protocol-addendum authorization, backup and temporary hashes, all thirteen
  identities/bindings/results, explicit indices 10..12, reboot/reconnect,
  exact Config restoration, exact GP-X1-002 rollback identity, owner reports,
  anomalies, and `Nunchuk NOT_TESTED`. Only complete supplied observations may
  classify PASS, subject to a fresh Hardware Evidence Processor.
- Allowed host changes: the smallest docs/control-plane addendum, focused
  read-only/offline recovery validator/operator wrapper and tests needed to
  preserve raw bytes, derive/validate the temporary packet, record ordered
  transaction stages, and restore the exact raw payload. Such tooling may call
  only the existing source-supported configurator commands and must not add
  firmware commands or automate flashing.
- Explicit exclusions: no firmware, candidate, tree, UF2, protobuf/schema,
  compiled defaults, mode semantics, modifier tables, source-owned stick
  tables, persistence architecture, communication backend config, custom
  modes, keyboard modes, RGB tables, Senscope semantics, GP-VAL-011, Nunchuk
  claim, root-cause claim, integration rebuild substitution, automatic retry
  after ambiguous write, or merge before independent PASS processing.
- Stop conditions: any identity/custody mismatch; unverifiable original backup;
  post-flash Config drift; unrelated temporary diff; binding ambiguity;
  write timeout/ambiguity; readback or reboot mismatch; disconnect/regression
  at the overflow boundary; failed exact Config restore; or rollback identity
  mismatch.
- Validation: focused offline tests of exact raw backup/re-encode, cloning and
  intended-only diff, binding masks/subset rejection, staged transaction
  evidence, exact restoration, artifact identity, framework/navigation, and
  `git diff --check`. No firmware build is required because no firmware or
  build input may change.

## Rejected alternatives

Rejected: rebuilding GP-CONFIG-010; editing either firmware candidate; using
compiled defaults; treating menu visibility as stored-count proof; retesting
only visible legacy profiles; changing modifier values; using empty, duplicate,
guessed, menu, reset, configurator, or bootloader bindings; retrying an
ambiguous write; leaving temporary Config persisted; using a later integration
rebuild for rollback; overwriting the prior INCONCLUSIVE evidence; or allowing
the operator/supervisor to self-classify and publish PASS without a fresh
Hardware Evidence Processor.

## Publication and next action

Publish only this packet on `planning/portfolio-20260922-0108` as a direct
child of the verified canonical base. A fresh independent Work-Order Curator
must verify the user direction, exact identities, source-backed host path,
binding authority, recovery invariants, and exclusions. If accepted, it should
publish an immutable receipt and a bounded source-free protocol-addendum
authorization for the existing GP-CONFIG-010 work order. This planning branch
must never be merged as implementation.
