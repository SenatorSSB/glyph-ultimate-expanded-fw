# Glyph Portfolio Candidate Packet — 2026-09-21 00:52 EEST

This immutable Revision-2 packet is non-authoritative candidate supply for an
independent Work-Order Curator. It authorizes no implementation, queue change,
firmware behavior, device operation, or hardware acceptance.

```yaml
packet_id: glyph-portfolio-20260921-0052
packet_state: FRESH
planning_branch: planning/portfolio-20260921-0052
base_configurator_sha: 4351b951916abbfbcaffec6f4512571a17d15ca5
base_live_verified: true
previous_planner_packet: glyph-portfolio-20260920-1457
previous_packet_commit: ebdb7f3d728320c4c6ee229f040cb98a29a8b524
previous_packet_freshness: CONSUMED
candidate_count: 4
curator_review_required: true
global_wait_proposed: false
material_events_since_publication: []
planner_authority: NON_AUTHORITATIVE_CANDIDATE_SUPPLY_ONLY
configurator_changed: false
```

## Basis, freshness, delegation, and limits

Live `configurator` is `4351b951916abbfbcaffec6f4512571a17d15ca5`.
Ordinary `git ls-remote` failed on sandbox DNS; the permitted network-enabled
retry succeeded. This is not an authentication failure. The original checkout
is clean at that SHA; other pre-existing worktree records point to missing,
prunable directories. This planning checkout is isolated under `/private/tmp`.
No canonical mutation is proposed by this packet.

The previous packet is consumed. Its seven executable items are DONE or, for
GP-CONFIG-010, hardware pending; GP-HW-002 remains research gated. Current queue
has zero READY, zero recorded/mechanically activatable/invalidated
PREAUTHORIZED, one hardware-pending item, and effective runway zero against
target four. Primary liveness is PLANNING_REQUIRED. The target is not a quota.
The new owner campaign and cross-repository audit are material new evidence.

Inspected authority: AGENTS.md; complete current agent state, roadmap,
workflow, runtime boundary, framework README, authorization/runway, user
direction, hardware contract; current queue (including every item and exact
GP-CONFIG-010/011 contracts); latest immutable Planner packet; current source,
checkers, fixtures, and CI named below. Cross-repository audit source is
Senscope commit `b0140a194639b9e5953bebfc9372f58cf348cfa8`, document
`docs/project/research/CROSS_REPO_CONTROL_PLANE_AUDIT_2026-09-21.md` and its
complete `cross-repo-2026-09-21/glyph-findings.md` evidence. Audit conclusions
are evidence, not authorization.

Delegation:
- Guidance applicable: Revision-2 Planner and native subagent contracts.
- Capability discovery: runtime tool catalog and collaboration capability were
  inspected; native internal agents are available.
- Specialists used: this bounded Glyph planning specialist is delegated by the
  campaign supervisor alongside separate Senscope planning and PR #115 review
  scopes (three native delegated scopes).
- Reviewer used: separate campaign-supervisor adversarial packet review before
  immutable publication; a fresh Curator must independently adjudicate it.
- If none: no nested specialist was created because all four runtime slots are
  occupied by the already partitioned campaign; no user-owned task substituted.

## Broad current supply refresh

The audit refreshed plausible independent correctness, source authority,
validation, integration, configurator, safety, recovery, provenance, usability,
and documentation work, rather than inferring scarcity from pending hardware.

| Surface | Fresh evidence / bounded disposition |
| --- | --- |
| Validation/build | GP-CONFIG-011 checker hardcodes ignored `.pio` paths; CI validation precedes build. Track exact host dependency closure (candidate below); no GP-VAL-011 redesign. |
| Runtime/input safety | Four Pico production mask call sites lack content validation; default USB getter can leave destination untouched. Separate exact-source H1 characterizations below. |
| Custom-mode capacity | Existing GP-CONFIG-011 proved 10/20 mismatch; examine repair authority, without repeating discovery. |
| Source-owned realization | `source_owned_generator_modes.py` already owns 28-table full/preserve/reject semantics. `convert_coordinate_native_profile_to_source_owned_spec.py` deliberately rejects generic profiles without correspondence. Senscope assessment lane is separate; no invented routing. |
| Persistence/configurator | `HandleSetConfig` stages and publishes only after save; GP-CONFIG-008 runtime rebinding, GP-CONFIG-009 invalid menus, and persistence research already exist. No duplicate candidates or new disk-atomicity claims. |
| Provenance/CI | `.github/workflows/build.yml` already pins actions and checks pre/post tracked cleanliness. Moving dependency observations and unknown `glyph_nuker` semantics remain known research limits, not new authority. |
| Recovery/hardware | Exact GP-CONFIG-010 custody remains separate. GP-HW-002 protocol-history semantics remain unresolved. No new hardware evidence. |
| Usability/integration | Senscope draft/manual-guide fixes and read-only assessment are separately governed in Senscope. No Glyph browser implementation or generic plugin framework is useful here. |

Largest immediate bottleneck: clean-checkout load-bearing host validation cannot
consume its current undeclared dependencies. Horizon A is that H1 closure;
Horizon B is two independently bounded characterizations and the source-owned
cache repair candidate; Horizon C is owner/physical gates. Global wait is not
proposed: useful independent H1 work exists.

## GP-VAL-026 — clean-checkout exact-schema host dependency closure

- Readiness estimate: CURATION_READY. Risk H1. Confidence high.
- Objective/value: make the existing eight-case modifier-cache characterization
  executable from a clean checkout in the declared Python/C++ sanitizer host
  environment, including the isolated configurator category, without a firmware
  build or ignored caller state.
- Current evidence: `tools/check_glyph_custom_modifier_cache_characterization.py`
  requires `.pio/build/glyph_mk6/nanopb/generated-src/config.pb.h`,
  `.pio/libdeps/glyph_mk6/HayBox-proto/{config.proto,config.options}`, and Nanopb
  headers. The manifest makes it current/load-bearing; CI validation has no
  build dependencies and runs before build. Fresh-checkout audit reproduced
  `missing generated schema header` while canonical/isolated proofs matched.
- Selected smallest architecture: track a narrow, byte-identical host fixture
  closure (`config.proto`, `config.options`, generated `config.pb.h`, Nanopb
  `pb.h`, provenance and applicable license notices) under a dedicated
  `tools/fixtures` directory. Bind it mechanically to exact upstream identity
  and the unchanged tracked Glyph dependency selector. It is a host fixture,
  never a replacement firmware include path or a new production dependency pin.
- Exact source authority: GregTurbo/HayBox-proto commit
  `db4e2f68b5c4ddd407e7c11050a920c4b4ec54c8`, independently resolved from the
  clean local dependency Git repository; selector `GregTurbo/HayBox-proto#db4e2f6`
  remains tracked in `config/glyph/env.ini`. Proto SHA-256
  `2844d8fc8c78c9fbed00a6954a13d9826f4634cac152f8a9a707666f47bb893b`,
  options `6a53dc93a79027669a3990c3a785e386e02e063c1f3438744aac49c3ad074805`,
  generated header
  `532f7ac324a57895caf82950ee36c6900d883a42188e5d6bbc2d3507318538f3`,
  Nanopb `pb.h`
  `a2ecdca9fdaeef5f4972ed983540c0d6fb0a5c402a2e0b0349d7e1bc5e188d29`.
  Header identifies generator `nanopb-0.4.9.1`, protocol-header version 40;
  installed library metadata identifies package 0.4.91. Record their observed
  correspondence exactly; do not infer a transitive reproducible-build claim.
- Identity contract: verify immutable upstream blob identity during fixture
  intake, record full commit plus exact Git blob IDs and SHA-256, bind trusted
  constants in the checker to the source selector and fixture bytes, check
  generated extent is 20, and fail on wrong upstream/schema identity. A mutable
  JSON label by itself cannot establish authority; coordinated fixture/metadata
  edits cannot silently self-authorize a different schema. All closure paths
  must be tracked regular files, fail on absence/symlink/tamper, and appear in
  the manifest's source-dependency closure. No network is needed at test time.
- Scope: existing checker, dedicated fixture/provenance and notices, precise
  dependency/use documentation, bounded adversarial tests, deterministic
  manifest/census/health consequences. Production code and GP-CONFIG-011's
  historical source/expected-result evidence remain unchanged.
- Dependencies: existing compiler with ASan/UBSan/bounds; Python stdlib; verified
  local dependency bytes or immutable upstream fetch during intake. No firmware
  build, `.pio` bootstrap, or GP-VAL-011 dependency.
- Validation: clean isolated clone direct checker PASS; unchanged 0/10/11/20
  write/read corpus and exact production include preserved; configurator
  category PASS; missing header/pb.h/proto/options, content tamper, symlink,
  wrong commit/blob/hash/capacity and changed tracked selector all FAIL.
  Current source-hash tamper must still FAIL. Verify no `.pio` access during
  runtime. Run manifest/census/health, framework, sequence/navigation/surface,
  Python syntax, diff checks and fresh independent review. Report any unrelated
  aggregate defect explicitly; do not claim aggregate green from category PASS.
- Exclusions/stops: no checker skip/demotion; no broad `.pio` inclusion, build
  input change, package download during checks, aggregate isolation change,
  GP-VAL-011 work, firmware behavior, schema ABI, device/hardware claim. Stop on
  fixture provenance ambiguity instead of accepting a hand-written schema.
- Rejected alternatives: regenerate at each test requires a larger protoc/
  Python/generator closure; full firmware bootstrap is unnecessary and fails
  isolated-clone assumptions; an independently handwritten miniature schema
  weakens exact source correspondence; the AVR schema is a different target.

## GP-CONFIG-012 — invalid-button exact-production characterization

- Readiness estimate: PREAUTHORIZABLE after GP-VAL-026's tracked fixture closure.
  Risk H1. Confidence high for characterization, no repair selection.
- Objective/value: characterize actual schema decode and every exact Pico
  `make_button_mask` caller; establish the precise source decision still needed
  for invalid bindings rather than guessing reject/no-binding policy.
- Source seam: `HAL/pico/include/util/state_util.hpp` shifts by `button - 1`
  without zero guard; `get_button` and `set_button` explicitly exclude zero.
  Schema defines BTN_UNSPECIFIED=0 and named inputs 1..60. Nanopb decode has
  already accepted zero in the audit; exact helper UBSan reported -1 shift.
  Source inspection finds callers in `CustomControllerMode::SetConfig`
  (modifiers and combo masks), `setup_mode_activation_bindings`, and
  `backend_config_from_buttons`. Census AVR helper separately as non-Glyph
  target; never confuse an AVR implementation with Pico behavior.
- Scope: source-bound host harness/checker/fixtures/report only. Extend the
  tracked fixture with exact generated `config.pb.c` and Nanopb
  `pb_decode.{c,h}`, `pb_common.{c,h}` as needed, each pinned with package/source
  identity, licenses, hashes and manifest dependency declarations. Compile
  exact production helper/caller bodies, not a behavioral rewrite.
- Corpus: empty bindings, zero, first=1, last=60, 61..64, shift boundary 65,
  maximum representable decoder values, negative/overlong/malformed varints,
  repeated/packed encodings, and zero mixed with a valid binding. Distinguish
  decoder rejection, successful numeric storage, invalid C++ enum read,
  invalid shift, and completed defined behavior; do not let enum sanitizer
  termination masquerade as a helper observation.
- Required caller matrix: caller → possible input source → validation before
  helper? → zero reachable from decoded/configured state? → observed host result
  → physical reachability status. Audit `HandleSetConfig`, defaults,
  `Persistence::LoadConfig`, Glyph setup, menu/watchdog runtime paths and every
  config-producing call site. Persisted decode validates CRC/encoding but no
  post-decode binding semantics; record actual code rather than extrapolating
  successful SetConfig validation into boot coverage.
- Validation: exact current production identity and helper include; decoder
  positive/negative controls and independent sanitizer processes; source/caller
  census fail on drift/omission; valid masks preserve existing bit positions;
  clean-checkout direct/category checks; affected transaction/runtime/provenance
  checks, manifest/census/health and framework/navigation/surface; independent
  source/review validation. No firmware build required for H1-only changes.
- Gates/stops: host UB is not physical symptom or exploitability evidence.
  Keep physical reachability UNKNOWN unless directly proven, and hardware
  NOT_TESTED. A later repair requires a new complete H2/H3 order. Current
  `get_button` zero handling alone does not settle whether dropping zero from
  a mixed combo is correct: `[0, BTN_LF1]` becoming `[BTN_LF1]` can activate
  behavior. Reject invalid configuration, invalidate entire binding, or ignore
  only zero are externally different. Do not select among them here.
- Dependencies/mechanical activation: GP-VAL-026 integrated with exact same
  production source/schema identities; permitted intervening changes are its
  host fixtures and directly coupled docs/checker metadata only. Any runtime,
  schema, input-policy or upstream drift returns to Curator.
- Exclusions: no firmware helper fix, new SetConfig policy, writes, persisted
  config changes, new default/binding semantics, physical or root-cause claim.

## GP-CONFIG-013 — USB-default exact no-write characterization

- Readiness estimate: PREAUTHORIZABLE after GP-VAL-026 closure. Risk H1.
  Confidence high for source finding; no fallback authority.
- Objective/value: establish the real callback's defined/no-write behavior,
  caller initialization, downstream reads and input validation across decoded,
  compiled, persisted and menu/watchdog paths.
- Source: `HAL/pico/src/comms/backend_init.cpp` declares uninitialized
  `CommunicationBackendConfig usb_backend_config`, calls the getter then reads
  `.backend_id` for early descriptor initialization. The current default getter
  assigns only for index >0 and <= backend count. When detection selects a USB
  backend it also copies that object into `backend_config`, later consuming
  `.backend_id` and `.default_mode_config`. Glyph setup supplies this exact
  callback. SetConfig checks `default_backend_config`, not this USB field.
- Scope: exact-production getter plus source-bound caller-path harness, decoder
  fixture reuse, checker, caller/initialization matrix and findings only. If
  GP-CONFIG-012 has not yet supplied decoder closure, this item may independently
  add the identical pinned generated `config.pb.c` and Nanopb
  `pb_decode.{c,h}` / `pb_common.{c,h}` closure under the same exact provenance,
  license, hash and manifest rules; otherwise reuse those exact files. Serialize
  shared fixture edits. These named nonsemantic fixture/checker declarations
  are permitted intervening changes; production/schema identities stay exact.
  Byte-exact extracted bodies are acceptable only when mechanically compared
  with current source; no unbound substitute function.
- Corpus: empty list; zero; first/last entry; index=count and count+1; decoder
  representable extrema and malformed/overflow values. Distinguish generated
  uint8 field limits from protobuf wire values. Sentinel-initialize outputs to
  prove no-write branches without inducing an artificial uninitialized read;
  characterize uninitialized production destination as a separate source
  fact unless a reliable supported memory sanitizer proves it dynamically.
- Required matrix: every getter/callback caller and argument initialization;
  compiled defaults, persisted `LoadConfig`, SetConfig, menu/default updates,
  watchdog scratch override; values allowed before initialization; downstream
  field reads. Include custom callback API contracts and actual registrations.
- Validation: known sentinel remains byte-identical for no-write cases; valid
  entries copied exactly, including non-first last entries; exact enum/backend
  values and read paths recorded; source/closure tamper rejected; clean-checkout
  host/category and affected transaction/runtime checks; normal manifest,
  census, health, framework/navigation/surface; independent review.
- Dependencies/invalidation: same closed GP-VAL-026 mechanical prerequisite as
  GP-CONFIG-012; the two H1 characterizations otherwise independent. Production
  backend/default/schema policy drift needs Curator judgment.
- Repair disposition: current source does not specify reject-invalid-config,
  a particular fallback backend, or retain-previous selection. Zero-initializing
  alone is not necessarily policy-neutral: `COMMS_BACKEND_UNSPECIFIED` reaches
  concrete switch branches. Finish engineering/matrix, then ask one precise
  policy question unless additional authoritative source settles it.
- Exclusions/stops: no USB getter/caller repair, fallback selection, firmware,
  persistence schema, device I/O, physical symptom claim or acceptance.

## GP-CONFIG-014 — schema-corresponding custom-modifier cache repair

- Readiness estimate: CURATION_READY for bounded H3 authority adjudication;
  implementation prerequisite GP-VAL-026. Confidence high. GP-CONFIG-011 is
  completed evidence and must not be rerun as a new discovery/work order.
- Objective: eliminate 10-slot cache indexing by schema-valid 11..20 modifiers
  while preserving all currently valid ordering, masks, arithmetic and ABI.
- Source authority: exact `CustomModeConfig.modifiers[20]` and option
  `max_count:20` at the schema identity above; `CustomControllerMode.hpp`
  declares ten masks; its `SetConfig` and analog loop iterate the count. The
  protocol/compiler array capacity is already 20; reducing accepted schema to
  ten would select new product policy. Existing order and arithmetic are the
  source contract. This repair creates no new modifier model.
- Selected architecture: named fixed 20-mask capacity with compile-time equality
  to the actual generated member extent, no Config-sized temporary; initialize
  the existing config pointer to null explicitly. Check count against that
  capacity before `SetConfig` touches base/config/cache state; invalid impossible
  count returns with accepted state unchanged. Check count at every processing
  entry that can reach cached modifier reads, before accessing the array or
  producing custom output from that invalid record. Preserve the existing
  null-config no-output behavior. Do not truncate, reorder, partially apply,
  clamp arithmetic, change masks, change combo capacity, or select zero-binding
  semantics. Schema-valid counts 0..20 retain existing ordered interpretation.
- Rationale: out-of-schema count >20 is not a valid configurable semantic; local
  refusal before indexing is the same bounded invariant already accepted for
  GP-CONFIG-010. Guarding it cannot reject a currently schema-valid count. The
  capacity change is internal cache storage only; Config/protobuf/persistence
  ABI, activation/routing and source-owned 28 tables remain identical. Review
  pointer alias/mutated-in-place state so no stale cache reads bypass guards.
- Scope: `include/modes/CustomControllerMode.hpp`,
  `src/modes/CustomControllerMode.cpp`, exact-production repair harness/checker,
  repair-specific fixtures/report, hardware protocol and directly affected
  correspondence/manifest/census/health metadata. Add precise RAM/map review.
  `src/core/mode_selection.cpp` and GP-CONFIG-010 candidate/branch/artifact are
  excluded. No cherry-pick, rebuild, rebase, contamination or shared candidate.
- Existing load-bearing gate transition is explicit: preserve original
  GP-CONFIG-011 fixture and expected failure record as historical immutable
  evidence. Add separately identified repair fixture and exact-source passing
  harness for current repaired code. Keep the existing modifier entry current
  and load-bearing, with explicit source-bound repair enforcement and original
  evidence identity verification; add a separately named repair gate if needed.
  Do not silently overwrite 11/20 failure history with PASS, skip source-hash
  checks, or demote the checker. A failed/unrecognized source identity fails.
- Validation: exact production SetConfig and processing at 0,10,11,20 all PASS
  under ASan/UBSan/bounds; 21 and maximal injected counts rejected before both
  cache writes and reads. Test fresh/null, valid→invalid→valid, pointer alias/
  in-place count mutation, modifier index 19, mixed masks and noncommutative
  modifier order using representable arithmetic, and unchanged combo/digital/
  axis behavior for valid configurations. Source hashes and generated-capacity
  mismatch mutations FAIL. No unknown enum probe is used to mask a capacity
  result. Run affected runtime/transaction checks and current load-bearing gate,
  normal manifest/census/health/framework/navigation/surface; source-authority
  specialist and fresh independent firmware-safety review.
- Candidate lifecycle: complete exact source commit before canonical
  `pio run -e glyph_mk6` (documented quiet fallback only if unavailable), review
  RAM/map delta, preserve/readback-hash UF2 under candidate-SHA/artifact-SHA
  custody, publish exact pinned candidate and separate source-free canonical
  hardware-pending metadata/protocol. STOP before merge. No hardware result
  is supplied by this packet.
- Hardware protocol required: exact artifact identity; accepted normal layout
  and connection regressions; existing custom-mode selection/configured
  0/10/11/20 modifier cases with explicit expected source-derived outputs and
  order, neutral/release, reconnect/power cycle; rollback to preserved accepted
  firmware. If exercising custom modes requires owner configuration not already
  supplied, record that exact physical prerequisite; do not create device-write
  automation. Nunchuk stays NOT_TESTED unless explicitly reported.
- Dependencies/gates: GP-VAL-026 closure; no semantic source drift; exact build,
  custody, review, protocol and physical PASS. User explicitly requested a
  separate source-authorized candidate despite the existing independent pending
  mode-cache candidate; no hardware acceptance transfers between them.
- Stops: any need to alter binding/override/clamp/priority, ABI, persistence,
  routing, gameplay, or forbidden publication; inability to preserve current
  exact load-bearing correspondence; any unresolved physical prerequisites
  remain handoff gates, never invented observations.

## Preserved pending lane and rejected/deferred alternatives

GP-CONFIG-010 remains HARDWARE_TEST_REQUIRED at live branch
`glyph/gp-config-010-mode-activation-capacity`, candidate
`f4771e17430fd1ea3f1e3e5339a83dfe648290a3`, UF2 SHA-256
`9be230cdce6b6ce0b97941da920ec8f043ff98c174ffb126cfcc654ad599209a`.
This planning run neither rebuilds nor modifies that snapshot or its artifact.
The campaign supervisor separately verifies custody and returns its existing
exact physical protocol. Neither pending hardware item is effective runway.

Rejected: reopening GP-VAL-011; duplicating GP-CONFIG-011; hiding the dependency
failure by skipping/demoting checks; shrinking schema capacity; coupling new
firmware to GP-CONFIG-010; silently choosing zero-button or USB fallback policy;
a production Profile exporter without binding authority; generic shared backend
abstractions without a second validated consumer; runtime-loaded config,
WebSerial/device/binary/backend writes, persistence additions or flashing;
rewriting historical hardware evidence; speculative GRAM firmware port.

Deferred with concrete boundary: button/USB repairs require completed H1
characterization plus either authoritative contract or an owner choice; first
real profile correspondence needs exact modifier/table/physical route, priority,
side-effects and ownership evidence; hardware requires physical observations.
No new game-semantic, Nunchuk, support, physical-symptom or root-cause claim.

## Publication and verification

Only this packet is published on its planning branch, as a direct child of the
verified canonical base. The immutable packet inventory has four unique GP-ID
candidate headings. Independent Curator must record the complete initial
inventory/dispositions in an immutable receipt at the same base, then a later
adoption commit, before any new implementation. Planning output must never be
merged into `configurator` as an implementation change.

Packet validation: exact base/parent identity, unique inventory/frontmatter,
`git diff --check`, one regular non-executable document only; fresh adversarial
packet review; exact live planning ref and canonical ref verified on publish.
Firmware build and hardware acceptance are NOT_APPLICABLE to this planning
publication. Independent campaign-supervisor adversarial review found two documentation
corrections (actual delegation scopes and independent decoder-closure ordering);
both are applied. It found no other actionable packet defect. The campaign
supervisor retains the exact review response.
