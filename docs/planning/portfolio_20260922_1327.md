# Glyph Portfolio Candidate Packet — 2026-09-22 13:27 EEST

This immutable Revision-2 packet is non-authoritative candidate supply for an
independent Work-Order Curator. It authorizes no controller mutation, hardware
result, firmware implementation, candidate replacement, queue publication,
integration, or merge.

```yaml
packet_id: glyph-portfolio-20260922-1327
packet_state: FRESH
planning_branch: planning/portfolio-20260922-1327
base_configurator_sha: 4b2d16e555a5f01ea06955785891ba1506c23162
base_live_verified: true
previous_planner_packet: glyph-portfolio-20260922-0108
previous_packet_commit: 417f164eec7a92e5499e6713a314907ce750c4a9
previous_packet_freshness: CONSUMED
candidate_count: 1
curator_review_required: true
global_wait_proposed: false
material_events_since_publication: []
planner_authority: NON_AUTHORITATIVE_CANDIDATE_SUPPLY_ONLY
configurator_changed: false
```

## Basis, evidence, and limits

Live GitHub verification resolved `origin/configurator` to
`4b2d16e555a5f01ea06955785891ba1506c23162`. The restricted default lookup
could not resolve GitHub; the permitted network-enabled retry succeeded, so
this is not an authentication incident. The current canonical queue has zero
immediate Ready items, zero recorded or mechanically activatable
Preauthorized items, one hardware-pending item, zero effective authorized
runway, and `PLANNING_REQUIRED` as its primary liveness state.

The prior packet `glyph-portfolio-20260922-0108` is materially consumed. Its
one-run authorization was used, the temporary configuration was removed, the
owner-original Config was restored exactly, and the accepted GP-X1-002
firmware was restored. The new owner direction and the resulting immutable
recovery evidence are material events requiring fresh Planner and independent
Curator correspondence; this packet does not duplicate unused supply.

The normal zero-runway Planner prompt calls for a broad Horizon A/B/C refresh.
The owner's current instruction expressly narrows this invocation and requires
this packet's only proposal to be the GP-CONFIG-010 reconnect-completion retest.
Accordingly, the largest in-scope bottleneck is the single missing reconnect
completion observation; GP-CONFIG-010 is the sole Horizon A candidate, Horizons
B and C are outside this owner-bounded packet, and no claim of portfolio-global
candidate scarcity or global evidence wait is made.

The unchanged GP-CONFIG-010 firmware identity is:

- candidate branch: `glyph/gp-config-010-mode-activation-capacity`;
- candidate Git SHA: `f4771e17430fd1ea3f1e3e5339a83dfe648290a3`;
- candidate base: `28426e4ba4763a99f0ca13491c023af77665c79c`;
- candidate tree: `e18a7496125bf485a179d9b8e08f1f47f5090b80`;
- preserved UF2 SHA-256:
  `9be230cdce6b6ce0b97941da920ec8f043ff98c174ffb126cfcc654ad599209a`;
- preserved UF2 size: `791040` bytes; and
- preserved locator:
  `local_backups/hardware-artifacts/f4771e17430fd1ea3f1e3e5339a83dfe648290a3/9be230cdce6b6ce0b97941da920ec8f043ff98c174ffb126cfcc654ad599209a/firmware.uf2`.

The accepted rollback identity remains GP-X1-002 candidate
`f657715b26d26587a931074ce7dd12c698785290`, preserved 792064-byte UF2
SHA-256 `00dc75b65a080830131c3c260558d4daeff45bb528e43d196b8ad94fcd06f451`.
Its preserved locator is
`local_backups/hardware-artifacts/f657715b26d26587a931074ce7dd12c698785290/00dc75b65a080830131c3c260558d4daeff45bb528e43d196b8ad94fcd06f451/firmware.uf2`.
The integration rebuild is not a permitted rollback substitute.
Live remote verification resolved both named candidate branches to those exact
SHAs. Local custody inspection independently found both UF2s to be regular
non-symlink files with their exact recorded sizes and SHA-256 values; no build
was run.

Fresh evidence at
`git-json:d69b1694c84f803c278b532dbd20a8725eb17a81:docs/calibration/fixtures/gp_config_010_hardware_evidence_2026-09-22_recovery_13mode.json`
is authoritative historical evidence for that exact unchanged candidate and
artifact. It proves exact custody and restoration, all thirteen activation
rows, direct PASS observations at indices 10, 11, and 12, and representative
Ult13 operation. It remains `INCONCLUSIVE` only because the first final normal
reconnect froze at the initial Glyph logo and the second reconnect did not
complete the protocol-required menu visibility and LT4 activation checks.
This packet preserves those observations exactly and does not downgrade or
reinterpret the successful 13-entry evidence.

The owner explicitly supplied the bounded retest objective and protocol in the
2026-09-22 task that caused this packet. That current direction is source
authority for the proposed physical procedure, but only Curator may publish a
new authorization. Existing `GLYPH-UD-019` is consumed and cannot authorize a
second run by itself.

## GP-CONFIG-010 — reconnect-completion retest

- Classification: hardware-evidence continuation for the existing H3
  candidate; source-free `REPAIR_REAUTHORIZATION` only.
- Readiness estimate: `CURATION_READY` for independent adjudication. Existing
  work-order status remains `LOCAL_ACCEPTANCE_PENDING / INCONCLUSIVE` until a
  fresh Hardware Evidence Processor publishes a disposition.
- Objective: close only the missing post-reconnect evidence for the exact
  unchanged GP-CONFIG-010 candidate/artifact while preserving the prior 13/13
  activation evidence as admissible historical evidence.
- Behavioral claim: none beyond the original GP-CONFIG-010 work order. The
  temporary persisted Config is controlled test stimulus, not a firmware,
  schema, default, persistence-architecture, or product-capability change.

### Required unchanged identities and evidence reuse

The candidate Git SHA, tree, UF2 bytes, artifact hash, and artifact size must
remain exact. No build, rebuild, source edit, patch, rebase, replacement
artifact, or integration-build substitution is permitted. The prior thirteen
activation selections, including indices 10, 11, and 12, remain admissible and
must not be repeated merely because this completion retest exists. Repeat the
full matrix only if a new discrepancy directly invalidates or calls those
observations into question; otherwise stop and return the discrepancy for
independent adjudication.

### Fresh backup and temporary stimulus

Before any mutation, use the existing source-supported Configurator path to
capture the complete current owner Config as raw protobuf payload, decoded
representation, length, SHA-256, identity/status, and operator log in a new
timestamped evidence directory. Require deterministic decode/re-encode exact
equality when supported. The current bytes must equal the previously restored
owner-original identity: 4201 bytes and SHA-256
`f589317b59b3ab7543cad563e2e0877dc5f491227e6e35777bc732d402bb1480`.
Any drift stops before mutation.

Reverify both preserved artifacts as regular non-symlink files with exact size,
hash, and custody. After the owner manually flashes only the exact
GP-CONFIG-010 UF2, require the persisted Config still equals the fresh owner
backup before any temporary write.

Recreate the same logical temporary test Config used by the prior successful
run, derived only from the freshly captured owner Ultimate profile:

| Profile | Index | Activation |
| --- | ---: | --- |
| Ult01 | 0 | LF6 |
| Ult02 | 1 | LF7 |
| Ult03 | 2 | LF8 |
| Ult04 | 3 | RF9 |
| Ult05 | 4 | RF10 |
| Ult06 | 5 | RF11 |
| Ult07 | 6 | RF12 |
| Ult08 | 7 | RF13 |
| Ult09 | 8 | RF14 |
| Ult10 | 9 | RF15 |
| Ult11 | 10 | RF16 |
| Ult12 | 11 | LT3 |
| Ult13 | 12 | LT4 |

Require exactly thirteen `MODE_ULTIMATE` entries, exact ordered names
`Ult01` through `Ult13`, GameCube-only applicability, and clone equality outside
name, activation binding, and applicability. Preserve all unrelated Config
fields, source-owned modifier tables, X1, backend behavior, and firmware.
Offline-validate and preserve a new machine-readable plan and intended-only
diff. Because the required fresh owner payload must match the prior exact owner
identity and the same deterministic logical derivation is required, the
intended temporary payload must re-establish the prior exact 6691-byte payload
with SHA-256
`02a456e4868d659e6c54bff7f2b5d26d069755496089c4ab2cd266b084e5e589`.
Any deterministic recreation difference stops before write. Write only through
the tested existing `CMD_SET_CONFIG` path; require complete transaction-stage
evidence, decoded `CMD_SUCCESS`, exact immediate readback, exact decoded intent,
and one Configurator-mode reboot with exact byte-for-byte temporary-payload
persistence. A timeout, ambiguous completion, or mismatch stops without
automatic retry.

### Exact completion observations

Give the owner one physical instruction at a time and record each direct
observation. After moving from Mac Configurator mode to the normal GameCube
cable/adapter path, the required first attempt must get past the Glyph logo.
Do not retry a frozen first attempt until it eventually works and then classify
the cycle as PASS.

Only after first-attempt startup passes:

1. Open the Profile menu and require the exact ordered visible sequence
   `Ult01` through `Ult13`, with count, order, missing entries, and extras
   recorded.
2. Return to the normal dashboard/state as needed, press and release LT4, and
   require exact `Ult13` display for config index 12 with stable connection,
   stable miniscreen, and ordinary directional response.
3. With Ult13 active, perform only a representative horizontal direction,
   vertical direction, face button, LT5/X1 input, release to neutral, stuck-
   input check, and disconnect check. This confirms continued operation and
   does not re-accept X1 or infer gameplay semantics.
4. After that first required startup, perform exactly three additional clean
   consecutive normal GameCube disconnect/reconnect startup cycles. For each,
   record logo passage and the selected profile/dashboard. LT4-to-Ult13 may be
   repeated on the final cycle. This is bounded confidence testing, not a
   larger endurance test.

If the frozen/stuck Glyph logo recurs on the required first startup or any of
the three later controlled reconnect cycles, stop the hardware-acceptance
portion immediately and classify the candidate `FAIL` unless the evidence
clearly proves an unrelated external cause. Preserve the recurrence and route
it for independent processing; later successful retries must not hide it. A
different genuinely ambiguous external issue may remain `INCONCLUSIVE` only
with its exact reason; ambiguity alone is not an exception for a controlled
frozen-logo recurrence.

If first startup, exact all-profile visibility, LT4-to-Ult13 post-reconnect
selection, representative Ult13 sanity, and all three additional reconnects
pass without a repeated freeze, the operator may record only a provisional
`PASS`. A fresh independent Hardware Evidence Processor must combine this run
with the prior exact 13-entry evidence before hardware PASS may be published.

### Mandatory restoration

Regardless of PASS, FAIL, or INCONCLUSIVE, reconnect in Configurator mode when
safe and capture the final temporary state for evidence. Restore the exact raw
owner Config captured at the start of this run. Require successful write,
immediate exact hash equality, one reboot, and exact post-reboot hash equality.
The expected SHA-256 remains
`f589317b59b3ab7543cad563e2e0877dc5f491227e6e35777bc732d402bb1480`
when the initial required identity matched. Restoration failure stops all
other work and becomes the recovery priority.

Only after exact Config restoration, the owner may manually flash the exact
accepted GP-X1-002 UF2. Require normal GameCube reconnection, Ultimate
auto-selection, LT5 X1 activation, one representative accepted X1 coordinate,
release/neutral behavior, and no stuck input or disconnect. Do not rerun or
re-accept the complete X1 scope.

### Evidence processing and integration boundary

Fresh evidence must reference both the immutable prior record proving 13/13
activation, indices 10/11/12, and representative Ultimate sanity, and the new
record proving or disproving first-attempt startup, thirteen-profile
visibility, LT4-to-Ult13 post-reconnect activation, representative operation,
and every reconnect cycle. It must also prove the fresh owner backup, exact
restoration before and after reboot, and accepted GP-X1-002 rollback.

Only a fresh Hardware Evidence Processor may promote the result to PASS. On
PASS, hand off to a fresh Implementation Supervisor; neither Planner, Curator,
operator, nor evidence processor may directly merge. The supervisor must
verify live canonical ancestry and preserve every hardware-correspondence-
critical candidate change exactly under the current GP-VAL-015/027 model.
Because canonical advanced beyond candidate base, inability to preserve exact
tested correspondence stops integration; do not recreate equivalent firmware.
The original GP-CONFIG-010 UF2 remains the physical acceptance artifact, not
an integration rebuild.

### Explicit exclusions and stop conditions

No firmware build, firmware patch, source/schema/default change, modifier-table
or X1 change, runtime-loaded profile/config product capability, persistence
architecture, WebSerial/device/protobuf/backend write addition, flashing
automation, GP-VAL-011 work, Nunchuk claim, game-semantic inference, root-cause
claim, full 13-row repetition absent discrepancy, evidence overwrite, or
pre-PASS merge is proposed.

Stop on any candidate, tree, artifact, custody, backup, deterministic re-encode,
post-flash preservation, intended-diff, binding, transaction-stage, response,
readback, reboot-persistence, first-startup, profile-visibility, LT4 selection,
reconnect, restoration, rollback-identity, or correspondence mismatch at its
stated gate. A physical anomaly does not authorize diagnosis by repeated
cycling or candidate modification in this run.

## Rejected alternatives

Rejected: a fresh full thirteen-profile acceptance; repeating the successful
13-row activation matrix without discrepancy; rebuilding or patching
GP-CONFIG-010; substituting an integration rebuild; changing the temporary
profile design or bindings; omitting a fresh owner backup; treating the second
prior reconnect as completion; retrying a frozen first startup until it works;
accumulating successes after any recurrence; leaving the temporary Config or
GP-CONFIG-010 firmware installed; reopening GP-VAL-011; and allowing this
Planner packet or the hardware operator to self-authorize, publish PASS, or
merge.

## Publication and next action

Publish only this packet on `planning/portfolio-20260922-1327` as a direct
child of the verified canonical base. A fresh independent Work-Order Curator
must verify the current owner direction, exact immutable identities, prior
evidence admissibility, bounded completion scope, recurrence disposition,
restoration envelope, and exclusions. If accepted, Curator should publish an
immutable receipt and exact source-free reconnect-completion authorization for
the existing GP-CONFIG-010 work order. This planning branch must never be
merged as implementation.
