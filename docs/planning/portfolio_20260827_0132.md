# Glyph Portfolio Candidate Packet — 2026-08-27 01:32 EEST

```yaml
packet_id: glyph-portfolio-20260827-0132
packet_state: FRESH
planning_branch: planning/portfolio-20260827-0132
base_configurator_sha: a747dd54b02b207483142331d8b5be1113fc951e
base_live_verified: true
previous_planner_packet: glyph-portfolio-20260824-2349
previous_packet_branch: planning/portfolio-20260824-2349
previous_packet_commit: ffba28772d8559df4b356de9b3a3f02248d16c07
previous_packet_freshness: MATERIALLY_CONSUMED
candidate_count: 4
new_candidate_count: 4
carried_forward_candidate_count: 0
curator_review_required: true
global_wait_proposed: false
planner_authority: NON_AUTHORITATIVE_CANDIDATE_SUPPLY_ONLY
configurator_changed: false
```

## Result and freshness

This is a fresh broad audit of live `configurator`, not a timer- or SHA-drift
refresh. The previous packet is materially consumed:

- `GP-PROV-003` implementation tip
  `2d468884e5fa812d33886e2520b8251d9ca970be` is an ancestor of the live
  canonical base; only truthful completion-status publication remains.
- `GP-AUTH-001`'s user-decision gate is satisfied by the
  production-authorized, baseline-equivalent sole-`kX1Table` intake.
- `GP-CONFIG-002` is invalidated by `GLYPH-UD-007`; no capture or renamed
  official-app dependency may survive.
- The X1 intake SHA-256 is
  `3966f5fa1d7b0181ff7a4ecea847635b1fe71d843a5925d126d86ae51fb41eea`.
- The active baseline header blob remains
  `40a0b4703b3cbc5800acca5f2fdc3229bd82844a`; no active source or table byte
  changed in the recovery.

No current queue item is hardware-pending, and there is no executable active
X1 delta. The largest remaining bottleneck is build-and-artifact provenance,
not source-owned table realization. The new declared-input inventory contains
69 selectors: 39 movable/unresolved declarations, four unresolved external
declarations, 22 runtime-resolved declarations, three tracked-byte identities,
and one exact-but-unfetched selector. Ten claims remain explicitly unresolved,
including dependency/action identity, build-route ownership, `glyph_nuker`
purpose/effect, immutable artifact custody, and reproducibility.

## Live base and broad-audit evidence

Ordinary read-only live verification established
`refs/heads/configurator` at
`a747dd54b02b207483142331d8b5be1113fc951e`. The audit inspected the current
queue and framework, current-state and roadmap surfaces, recent hardening,
source-owned table inclusion/publication, generator and intake contracts,
official-configurator retirement records, tracked workflows, build-input
inventory, artifact/hardware contracts, current and historical hardware
evidence, validation manifest/census, and plausible firmware, configurator,
backend, export, governance, recovery, and usability seams.

Observed current facts:

- all 24 current load-bearing runtime-config checks pass on the exact canonical
  tree; checker census count is 191 and manifest count is 29;
- `GP-PROV-003` is canonically integrated but not yet truthfully status-published
  `DONE`;
- the X1 authority intake is an offline `NO_OP`, owns exactly one table, and
  cannot become a hardware candidate;
- the official-configurator lane is retired historical evidence with no
  operator capture required;
- no new exact-snapshot physical result exists, the canonical-grid candidate
  remains failed, and Nunchuk remains `NOT_TESTED`.

No repository product file, active table byte, workflow, firmware artifact, or
hardware state was changed during this audit.

## Candidate supply

### GP-PROV-004 — Record observed resolutions for canonical build-input selectors

- **Evidence gap:** package/action content identity, toolchain resolution, and
  `build-device-config` live caller/ownership remain unresolved.
- **Objective:** create a timestamped, exact-base observational resolution
  record for every network-addressable Git, action, package, and reusable-
  workflow selector reached by `glyph_mk6` or either tracked build workflow.
- **Scope:** bind inventory selector ID/raw value, upstream locator, lookup
  method, observed full commit/version/content identity, mutability
  classification, and resolved/unresolved result. Distinguish visible external
  reusable-workflow source from actual invocation, secrets, permissions, or
  ownership.
- **Exclusions:** no dependency installation, local-cache authority, pin
  changes, workflow mutation, builds, postprocessor execution, artifact
  publication, reproducibility claim, or device operation.
- **Source authority:** canonical `GP-PROV-003` declarations plus live upstream
  read-only observations, classified as observations rather than immutable
  guarantees.
- **Dependencies:** publish truthful `GP-PROV-003` completion evidence first.
- **Risk:** H0.
- **Validation:** exact correspondence with every eligible inventory selector;
  deterministic schema and completeness checker; explicit unknown/unreachable
  states; no network in the ordinary checker; census, aggregate, framework,
  navigation, and surface checks.
- **Activation condition:** canonical declaration bytes still match the
  `GP-PROV-003` inventory.
- **Invalidation:** selector/declaration drift or any request to change pins or
  build inputs.
- **Planner estimate:** `CURATION_READY`; confidence high.

### GP-PROV-005 — Establish source lineage for `glyph_nuker`, or prove it remains unavailable

- **Evidence gap:** the tracked postprocessor is a stripped static x86-64 ELF
  with SHA-256
  `8c488005c1ae7676518a0f8e048ff7d2fb51b71b743fdb785aeed3d8cf9f56ae`.
  Current history shows only its import in squash commit
  `cc57c4fcbcf25c5e33fab21fd5b8312e0543c8dd`; no source/build recipe is
  present, and purpose plus byte transformation remain `UNKNOWN`.
- **Objective:** inspect repository history and named upstream
  repositories/releases for authoritative source, immutable source commit,
  documented purpose, and build provenance; record exact evidence or a bounded
  negative result.
- **Scope:** docs, deterministic evidence fixture, and correspondence checker.
  Binary strings or disassembly may be observations/inferences only, never
  source authority.
- **Exclusions:** no execution of `glyph_nuker`, real UF2, pre/post artifact
  experiment, rebuild/equivalence claim, replacement, workflow edit, hardware,
  upload, or device action.
- **Source authority:** canonical binary/workflow identity and independently
  resolvable upstream source evidence, if any.
- **Dependencies:** canonical `GP-PROV-003` inventory and unchanged
  postprocessor/workflow identity.
- **Risk:** H0.
- **Validation:** exact binary hash/mode, Git-object-backed source references
  where found, explicit absence/unknown classifications, and unchanged
  artifact/runtime non-claims.
- **Activation condition:** current binary hash and workflow invocation remain
  unchanged.
- **Invalidation:** binary/workflow drift or a task requiring execution or
  replacement.
- **Planner estimate:** `CURATION_READY` research; an explicit `UNKNOWN`
  outcome is acceptable; confidence medium.

### GP-ART-001 — Establish approved immutable firmware-artifact custody

- **Evidence gap:** current provenance records
  `artifact_store_established: false` and `immutable_locator: null`.
  `HARDWARE_EVIDENCE.md` requires a candidate-SHA/artifact-SHA-addressed durable
  locator and mandates `BLOCKED_EXTERNAL` when none exists.
- **Objective:** after owner selection, define and verify one approved immutable
  store, content-addressed locator convention, retention/custody rules,
  readback/re-hash procedure, expiry/deletion behavior, and failure handling.
- **Scope:** repository contract, synthetic fixture/verifier, and externally
  approved storage integration only after separate authorization.
- **Exclusions:** no firmware build, real artifact upload, candidate creation,
  device update, flashing, hardware PASS, or claim that storage is immutable
  before evidence.
- **Source authority:** hardware-evidence contract plus an explicit
  project-owner store/retention/access decision.
- **Dependencies:** owner names the store, custody, retention, immutability,
  and access model; external permissions must be verifiable.
- **Risk:** H1/external state.
- **Validation:** synthetic content-addressed upload/readback/tamper/expiry
  cases followed by a separately authorized real-store proof.
- **Activation condition:** durable owner decision supplies the exact store and
  policy.
- **Invalidation:** mutable/expiring storage, unverifiable retention, or
  access/custody ambiguity.
- **Planner estimate:** `USER_DECISION_GATED`; not executable or
  Preauthorizable; confidence high on the gap and unknown on the store choice.

### GP-X1-001 — Realize a separately authorized behavior-changing X1 candidate

- **Evidence gap:** no different X1 coordinates are authorized. The canonical
  intake owns only `kX1Table` and is intentionally `NO_OP`.
- **Objective:** only after a new exact production-authorized intake exists,
  generate an active source-owned candidate changing exactly `kX1Table`,
  preserve the other 27 tables, build the exact SHA, preserve the exact
  artifact, and enter the Revision-2 physical hardware lifecycle.
- **Scope:** sole-X1 overlay, nine ordered raw-byte points, existing source-owned
  include/publication path, exact semantic manifest, canonical build,
  immutable artifact, candidate-specific manual protocol, and physical result.
- **Exclusions:** no second table, routing/modifier/game semantics, alternate
  active publication, runtime-loaded config, persistence, WebSerial/device
  write, protobuf write, flashing automation, Nunchuk claim, or inference from
  the baseline-equivalent pilot.
- **Source authority:** a later explicit project-owner/Senscope production-
  authorized intake containing exact nine changed points and current baseline
  identity.
- **Dependencies:** exact new intake; unchanged accepted active architecture;
  immutable artifact custody; exact hardware protocol.
- **Risk:** H3.
- **Validation:** exactly one changed table; exactly directions 1–9; byte-range
  coordinates; all 27 other digests unchanged; active diff limited to X1;
  `pio run -e glyph_mk6`; candidate/artifact SHA-256 binding; physical PASS
  before merge.
- **Activation condition:** a new canonical valid intake with exact changed X1
  bytes and non-placeholder approval.
- **Invalidation:** baseline drift, any second table, missing/ambiguous
  authority, unexpected active delta, publication-path change, or unavailable
  durable artifact locator.
- **Planner estimate:** `USER_DECISION_GATED`; unknown bytes prevent
  Preauthorization; confidence high on lifecycle and zero on future content.

## Hardware and rejected supply

Current hardware-pending queue count is zero. The canonical-grid candidate
remains `HARDWARE_FAILED` and must not merge; no source-authorized repair
content exists. Legacy Y2 and Alternative-B results support their already-
merged mechanism only and cannot satisfy a new Revision-2 candidate. Nunchuk
remains `NOT_TESTED`.

Rejected or not worth queuing now:

- any resurrection or renamed equivalent of `GP-CONFIG-002`;
- official-configurator export/capture automation or compatibility work;
- a Senscope/backend export adapter before a real neutral-profile schema/input
  and ownership mapping exist;
- additional table ownership, all-28 replacement, or generic X1 candidate
  plumbing before real changed X1 bytes exist;
- runtime-loaded config, protobuf persistence/write, WebSerial/device write,
  flashing, `candidate.view`, `active_storage.view`, or RAM publication;
- repair of the failed canonical-grid candidate without authorized replacement
  semantics;
- build-input pin changes or reproducibility claims before observational
  evidence; and
- root-cause or Nunchuk work without direct evidence and explicit scope.

`GLOBAL_EVIDENCE_WAIT_SUPPORTED` is not proposed: two independent bounded H0
research candidates exist. A truthful effective runway after skeptical Curator
review is at most two, not the nominal target four.

This packet is candidate supply only. It grants no implementation, queue,
table, firmware, external-storage, or hardware authority.
